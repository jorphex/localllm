# Active
[repo model store migration] outcome — the durable GGUF assets were moved out of `~/.cache/openwendy/gguf` into `~/projects/localllm/models`, and `scripts/common.sh` now prefers that repo-local model store before any legacy cache roots. Impact: the repo now has a stable, correctly named model location, while legacy fallback still exists only for compatibility rather than as the primary store.
[remaining durable tunings] outcome — the kept main-model presets now have one clear tuned profile each on this host. `qwen-3.5` and `qwen-3.5-abl` both now use `131072` context with `-b 512 -ub 256 -ctk q4_0 -ctv q4_0 --image-max-tokens 12288`, because the parity batch sweep showed that `512/256` materially improved large-prompt preload speed over the older `256/128` profile. `glm-4.6v` stays on `131072` with `-b 512 -ub 256 -ctk q4_0 -ctv q4_0 --image-max-tokens 1792`, because raising batch beyond that did not help and only reduced safety margin. Impact: README, preset env files, and the current service defaults now all point at the same tuned values for the three retained models.
[deepseek removal] outcome — `DeepSeek-Coder-V2-Lite-Instruct-Q4_K_M.gguf` was deleted after the parity sweep. Impact: the rejected DeepSeek candidate is no longer available for accidental relaunch, which matches the measured conclusion that it was not a practical fit for this host and workload.
[main preset workflow] outcome — the main user service now reads model-specific settings from `config/localllm-main.env`, with durable preset files under `config/presets/` and helper scripts `scripts/load-main-preset.sh`, `scripts/load-qwen-3.5.sh`, `scripts/load-qwen-3.5-abl.sh`, `scripts/load-glm-4.6v.sh`, and `scripts/unload-main.sh`. The active default preset was switched to `qwen-3.5-abl` at `131072` with `-b 512 -ub 256`, and an end-to-end verification successfully switched `qwen-3.5 -> glm-4.6v -> qwen-3.5-abl` through those scripts while `/props` reflected each alias and model path correctly. Impact: future model swaps no longer require hand-editing the one launch script or service unit; the loader scripts stop the current service first, then start the chosen preset cleanly.
[benchmark promotion] outcome — the useful ad hoc benchmark ideas were promoted into repo-managed scripts instead of copied wholesale. `benchmarks/coding_compare.sh` now supports budget matrices through `THINKING_BUDGETS` and includes the extra `merge_intervals` coding prompt, while `benchmarks/final_decision_round.sh` captures the useful mixed-scenario tie-breaker pass from `/tmp`. Impact: future model evaluations can stay inside the repo without depending on stale hardcoded `/tmp` scripts.
[qwen-3.5-abl restored from repo models] outcome — after the cleanup and preset migration, the managed `localllm-main.service` was brought back on `qwen-3.5-abl` using the repo-local `models/` path, `131072` context, and the tuned `-b 512 -ub 256` profile with `--image-max-tokens 12288`. The live service on `8091` is healthy and idles around `7108 MiB` used / `2900 MiB` free. Impact: the default local coding/vision service is back up on the preferred preset and no longer depends on the old OpenWendy cache path.
[deepseek higher-context probe] outcome — `DeepSeek-Coder-V2-Lite-Instruct-Q4_K_M.gguf` can load above `64k` on this 10 GB card with the current q4 KV profile, but the fit changes materially. At `131072` with `-b 64 -ub 32`, it loaded successfully with only `22/28` layers on GPU, `CPU_Mapped model buffer = 9697.81 MiB`, `CUDA0 model buffer = 856.65 MiB`, and `CUDA0 KV buffer = 7560.00 MiB`; a tiny `OK` probe still worked. At `90112`, it kept `28/28` layers on GPU with `CUDA0 model buffer = 1646.88 MiB` and `CUDA0 KV buffer = 6682.50 MiB`. Impact: `128k` technically fits, but it does so by spilling more of the model to CPU, which makes the large-prompt latency story worse rather than better.
[parity batch sweep] outcome — a one-model-at-a-time batch sweep on the shared practical preload transcript established the current best per-model launch profile for prompt ingest. `deepseek-coder` at `128k` remained the clear outlier: baseline `-b 64 -ub 32` timed out after `180s` at only `43.52%` of the preload, while higher `-b 128 -ub 64` improved to `52.48%` but still returned no bytes. `qwen-3.5` and `qwen-3.5-abl` both improved meaningfully when moving from `256/128` to `512/256`, dropping the preload request from about `3.7s` to about `3.2s` while keeping roughly `2.5 to 2.9 GiB` free. `glm-4.6v` did not benefit from a larger batch: baseline `512/256` completed in about `2.97s`, while `640/320` slowed slightly to about `3.10s` and cut free VRAM to about `796 MiB`. Impact: the best current parity profiles are `deepseek 128k @ 128/64`, `qwen-3.5 128k @ 512/256`, `qwen-3.5-abl 128k @ 512/256`, and `glm-4.6v 128k @ 512/256`.
[preload follow-up parity] outcome — the practical preload-plus-follow-up comparison now has comparable data for the three non-DeepSeek baselines and negative-result data for DeepSeek at higher contexts. `qwen-3.5` completed the first preload turn in `3.19s`, then answered the follow-up in `42.16s` with `3196` completion tokens, about `4.2k` chars of hidden reasoning, and correct inclusion of both failing test names plus the exact backlog log phrase. `qwen-3.5-abl` completed the preload in `3.24s`, then answered in `14.26s` with `1089` completion tokens, about `4.0k` chars of hidden reasoning, and the same required evidence. `glm-4.6v` completed the preload in `2.99s`, then answered in `5.00s` with `318` completion tokens, about `1.2k` chars of hidden reasoning, and the required evidence. `deepseek-coder` at `128k` with `128/64` did not finish the first preload turn even with a `450s` timeout; at `96k` and `88k` it still timed out after `360s`, though both reached about `81.92%` of the prompt. Impact: DeepSeek can still not be called “practically usable” for an OpenCode-like first-turn preload on this box at `88k+`, while the Qwen and GLM baselines all remain workable on the same raw transcript and follow-up prompt.
[agentic compare pass budget-500] outcome — the four-model barrage was rerun one-model-at-a-time at thinking budget `500` across `plan_then_revise`, `review_then_retry`, `codex_workflow`, `evidence_triage`, `tool_restraint`, and `tool_followthrough`. The ranking stayed consistent with the earlier `1000` pass. `deepseek-coder` remained the cleanest output model with `0` hidden reasoning and no tool calls, but it still preferred to narrate intended actions instead of using tools. `qwen-3.5` remained the best tool user: it called tools in `codex_workflow` and `tool_followthrough`, then produced strong follow-up answers, but still leaked substantial hidden reasoning and occasionally bloated visible output. `qwen-3.5-abl` behaved similarly but with more drift and much longer visible answers in several planning scenarios. `glm-4.6v` again used tools, but was less reliable and sometimes duplicated or prematurely repeated content. Impact: the equalized barrage evidence now covers both `500` and `1000` budgets and still points to the same split: DeepSeek for clean direct coding output, plain Qwen for more reliable tool-taking behavior.
[one-model benchmark rule] outcome — the repo-managed user service auto-restarts the main model, so “kill the process” is not sufficient when a benchmark requires only one model on the GPU. The correct control path is to export `XDG_RUNTIME_DIR=/run/user/$(id -u)` and `DBUS_SESSION_BUS_ADDRESS=unix:path=$XDG_RUNTIME_DIR/bus`, then use `systemctl --user stop localllm-main.service` before scratch runs and `systemctl --user start localllm-main.service` afterward. Impact: future parity sweeps should stop the service explicitly to avoid contaminating scratch results or misreading restart behavior as duplicate model loads.
[fair 10k preload comparison] outcome — the practical large first-turn payload under `/tmp/deepseek-preload-bench3/request_body.json` was rerun fairly on an otherwise empty GPU across all four main models by swapping only the request `model` alias and using each model’s current tuned launch profile. `deepseek-coder` at `64k` with `-b 64 -ub 32` again timed out after `180s` with no bytes returned and only `4928 / 10000` prompt tokens ingested (`49.28%`). `qwen-3.5` at `128k` with `-b 256 -ub 128` completed the same raw transcript in `39.31s` with `9884` prompt tokens. `qwen-3.5-abl` at `128k` with the same batch profile completed in `3.74s` with `9884` prompt tokens. `glm-4.6v` at `128k` with `-b 512 -ub 256` completed in `2.97s` with `8904` prompt tokens. Impact: for large first-turn preload behavior, DeepSeek is the outlier laggard on this box, while the Qwen and GLM baselines are dramatically faster on the same transcript even after tokenizer differences are accounted for.
[scratch preload fairness] outcome — scratch preload comparisons must be run with the live main model unloaded first. An earlier attempt left `deepseek-coder` resident on `8091`, which caused the scratch `qwen-3.5` startup to fail with CUDA OOM while allocating compute buffers. Impact: future cross-model fit or preload sweeps should explicitly clear the live GPU occupant before interpreting any scratch-server startup failure as a model limitation.
[batch size tradeoff] outcome — larger `-b` / `-ub` values improved DeepSeek’s large-prompt ingest rate but did not fix the first-turn preload bottleneck. On the same practical `10k` preload payload, `deepseek-coder` moved from `4928 / 10000` prompt tokens in `180s` at `-b 64 -ub 32` to `5888 / 10000` at `-b 128 -ub 64`, but still returned no bytes before timeout. The empty-GPU comparison also showed why aggressively raising batch sizes is risky: `glm-4.6v` already runs its `128k` profile with only about `440 MiB` free after the preload request, so any further batch growth would eat directly into the small remaining safety margin for compute buffers and multimodal spikes. Impact: higher `-b` / `-ub` can help prompt ingest, but only while enough VRAM headroom remains; on this 10 GB card, the safe rule is to treat extra batch as a headroom-for-throughput trade rather than a free speed knob.
[deepseek 10k preload benchmark] outcome — using the live `deepseek-coder` service itself, a practical large first-turn payload was built from repeated model-generated outputs until the probe request reached exactly `10000` prompt tokens. That payload was then replayed against scratch DeepSeek servers at `-c 65536` with two batch profiles. The current live profile `-b 64 -ub 32` did not finish prompt ingestion within a `180s` client timeout; server logs showed it reached only about `49.3%` of the `10k` prompt by then (`4928/10000` prompt tokens processed). A larger batch profile `-b 128 -ub 64` improved ingest materially but still did not finish within `180s`; it reached about `58.9%` of the prompt (`5888/10000`). Impact: the user’s OpenCode observation is confirmed as a real first-turn preload bottleneck on DeepSeek. Raising batch size helps, but not enough to make a `~10k` first prompt comfortable on this `10 GiB` box; the strongest practical lever is reducing the initial injected context, with `-b 128 -ub 64` worth keeping in mind only if further fit checks and live-turn testing are acceptable.
[deepseek opencode first-turn note] outcome — the user reports that in OpenCode, `deepseek-coder` was mainly slow on the first message while subsequent turns were workable. The likely explanation is prompt-ingest cost on the large initial context payload (around `10k` tokens) rather than uniformly slow generation on every turn. Impact: future DeepSeek tuning should distinguish prompt preload latency from steady-state turn latency; improving the first-turn ingest path may matter more than chasing a few extra generation tok/s.
[agentic compare pass budget-1000] outcome — the first full compare pass of `benchmarks/agentic_barrage_compare.sh` completed across `deepseek-coder`, `qwen-3.5`, `qwen-3.5-abl`, and `glm-4.6v` with scenarios `plan_then_revise review_then_retry codex_workflow evidence_triage tool_restraint tool_followthrough` at thinking budget `1000`. `deepseek-coder` was the cleanest harness fit: visible-only answers on every turn, no hidden reasoning, no tool calls even when tools were available, and about `28` to `40` tok/s. `qwen-3.5` was the best tool-user of the group: it called tools in both `codex_workflow` and `tool_followthrough`, then produced focused follow-up answers, but it also emitted large hidden-reasoning payloads on most non-tool scenarios (`~4k` tokens of reasoning was common) and sometimes over-produced visible text. `qwen-3.5-abl` behaved similarly but was less disciplined, with several very long visible answers (`~9.4k` chars on `plan_then_revise` turn 2 and `~8.7k` chars on `review_then_retry` turn 1) alongside the same heavy hidden reasoning. `glm-4.6v` also used tools in the Codex-style scenarios, but it was the least reliable there: `codex_workflow` turn 2 ended in another `tool_calls` finish instead of a final answer, and `tool_restraint` duplicated its sentence. Impact: for this barrage at budget `1000`, `deepseek-coder` is the best clean-output harness fit, `qwen-3.5` is the most promising if tool-use realism matters more than hidden reasoning verbosity, `qwen-3.5-abl` is usable but still drifty, and `glm-4.6v` remains the weakest fit for sustained coding-agent workflow simulation.
[agentic compare runner] outcome — the barrage has a repo-managed comparison entry point at `benchmarks/agentic_barrage_compare.sh`. It originally covered the four-model DeepSeek/Qwen/GLM comparison; after DeepSeek removal, its default candidate set was reduced to the three retained durable presets `qwen-3.5`, `qwen-3.5-abl`, and `glm-4.6v`. Impact: future agents can compare the currently retained local models with the same barrage without hand-editing `/tmp` scripts or reimplementing launch logic.
[agentic barrage scenario extension] outcome — `benchmarks/agentic_barrage.sh` now includes `review_then_retry` in addition to the earlier `plan_then_revise`, `evidence_triage`, `tool_restraint`, and `tool_followthrough` scenarios. The new scenario specifically tests whether a model can accept review feedback about an overly broad patch, narrow the next move, reuse existing evidence, and stay scoped instead of restarting from scratch. A live smoke on `deepseek-coder` at budget `1000` passed for both turns with visible-only output and the expected scope/evidence/validation cues. Impact: the barrage now probes one more failure-recovery pattern that is closer to real coding-agent harness behavior.
[agentic barrage benchmark] outcome — a repo-managed live-endpoint benchmark was added at `benchmarks/agentic_barrage.sh` to test coding-agent behavior more directly than the earlier one-shot harnesses. It runs multi-turn scenarios against the currently loaded model on a target port, avoids `max_tokens` caps by default, supports uncapped or explicit thinking budgets, and emits compact summaries plus raw JSON files under `OUT_DIR`. The initial smoke on live `deepseek-coder` passed for `plan_then_revise`, `tool_restraint`, and `tool_followthrough` at budget `1000`: the revision scenario stayed visible-only with evidence/validation terms present, the restraint scenario avoided tool calls, and the tool-followthrough scenario completed cleanly even though DeepSeek answered directly before the synthetic tool reply. Impact: future agents now have a durable benchmark for harness-adjacent behavior without relying on ephemeral `/tmp` scripts, but should still treat it as a proxy barrage rather than a full filesystem-editing autonomous harness.
[deepseek temporary cutover] outcome — the active user `localllm-main.service` was temporarily switched from `qwen-3.5-abl` to `DeepSeek-Coder-V2-Lite-Instruct-Q4_K_M.gguf`, with no `mmproj`, tuned `64k` profile `-np 1 -tb 4 -b 64 -ub 32 -cram 0 -fa on --threads-http 4 -ctk q4_0 -ctv q4_0 -rea on --metrics --no-warmup`, and live alias `deepseek-coder`. This was a benchmark-only state; DeepSeek was later removed from disk after the parity sweep. Impact: keep this note only as the historical starting point for the later DeepSeek benchmark findings, not as a current service description.
[mistral candidate cleanup] outcome — the rejected `Mistral-Small-3.2-24B-Instruct-2506` and `Ministral-3-14B-Reasoning-2512` GGUF packages and their projector files were removed from `~/.cache/openwendy/gguf`. Impact: the staged model cache now keeps the stronger `DeepSeek` candidate without spending disk on the two slower or failed Mistral-family packages.
[corrected 64k candidate comparison] outcome — the earlier capped-output comparison understated the better models and did not rescue `Ministral`. A corrected rerun at the same `64k` baselines removed `max_tokens` limits, kept the existing tunings, and exercised uncapped, `500`, and `1000` thinking-budget modes. `DeepSeek-Coder-V2-Lite-Instruct-Q4_K_M.gguf` stayed decisively strongest: coding pass outputs were all visible with `0` hidden reasoning, about `31.1` to `37.5` gen tok/s, and substantial answer sizes across all nine coding cases; the uncapped/`500`/`1000` quality prompts also all returned clean visible answers with `0` hidden reasoning. `Mistral-Small-3.2-24B-Instruct-2506-Q4_K_M.gguf` remained usable but very slow: visible coding outputs across all nine cases, typically `4.26` to `4.52` gen tok/s, with long uncapped answers up to about `3.8k` chars; the quality rerun was consistent before being stopped for time after the same pattern held. `Ministral-3-14B-Reasoning-2512-Q4_K_M.gguf` remained a poor fit even after removing `max_tokens`: its first uncapped coding answer produced only `179` visible chars while spending about `6082` chars in hidden reasoning, then the next prompt timed out after `300s`. Impact: the correction strengthens `DeepSeek` and confirms `Mistral Small` as a slow fallback, but it does not change the ranking or rehabilitate `Ministral`; future comparisons should prioritize DeepSeek first, Mistral Small second, and skip Ministral for coding-agent workloads unless the evaluation goal specifically values hidden reasoning over visible delivery.
[evaluation guardrails] outcome — future model-comparison and harness passes must not set `max_tokens` caps by default, because that truncation materially distorted behavior and was mistaken for model/runtime bugs. If a thinking budget is needed, keep it explicit and conservative; the user reports a lower safe bound around `500`, with `1000` acceptable for controlled tests, and uncapped runs are preferred when evaluating natural behavior. Impact: future agents should treat `max_tokens` caps as opt-in stress conditions rather than default evaluation settings.
[64k candidate baselines] outcome — three newly staged candidates were baseline-tuned at `-c 65536` with quantized KV and then run through the shared coding harness. `DeepSeek-Coder-V2-Lite-Instruct-Q4_K_M.gguf` was best on the leaner `-np 1 -tb 4 -b 64 -ub 32 -cram 0 -fa on --threads-http 4 -rea on --metrics --no-warmup -ctk q4_0 -ctv q4_0` profile, leaving about `1038 MiB` free and reaching about `70.5` prompt tok/s on the tiny OK probe plus `31.7` to `38.3` gen tok/s across the coding prompts; it produced substantial visible coding answers on all three harness prompts. `Mistral-Small-3.2-24B-Instruct-2506-Q4_K_M.gguf` with `mmproj-F16-Mistral-Small-3.2-24B-Instruct-2506.gguf` was best on the mid profile `-np 1 -tb 8 -b 128 -ub 64 -cram 0 -fa on --threads-http 4 -rea on --metrics --no-warmup -ctk q4_0 -ctv q4_0`, leaving about `414 MiB` free and running at only about `4.35` to `4.65` gen tok/s on the coding harness, but it did return visible content on all three prompts. `Ministral-3-14B-Reasoning-2512-Q4_K_M.gguf` with `mmproj-F16-Ministral-3-14B-Reasoning-2512.gguf` stayed too reasoning-heavy on every tested profile; even the least-bad wide profile left only about `168 MiB` free and still spent the direct prompt budget in hidden reasoning, and the coding harness outputs were effectively empty visible answers. Impact: for tomorrow’s testing, DeepSeek Coder V2 Lite is the strongest coding-first candidate by a large margin, Mistral Small 3.2 24B is viable but very slow on this box, and Ministral 14B Reasoning is a poor fit for visible-output coding/agent work under the current harness assumptions.
[benchmark harness scope] outcome — there is no repo `tmp/` benchmark directory in this checkout, so the relevant built-in evaluation harnesses for this pass were `benchmarks/sweep_profiles.sh`, `benchmarks/coding_compare.sh`, and the runtime unit test `tests/test_llama_runtime.py`. The unit test suite still passed cleanly after the launcher changes (`11` tests, `OK`). Impact: future agents should not waste time looking for a non-existent repo `tmp/` suite here; the useful local model-evaluation scripts live under `benchmarks/`.
[tomorrow staging models] outcome — three additional model candidates were originally downloaded into the legacy cache for later testing: `DeepSeek-Coder-V2-Lite-Instruct-Q4_K_M.gguf`, `Mistral-Small-3.2-24B-Instruct-2506-Q4_K_M.gguf` with `mmproj-F16-Mistral-Small-3.2-24B-Instruct-2506.gguf`, and `Ministral-3-14B-Reasoning-2512-Q4_K_M.gguf` with `mmproj-F16-Ministral-3-14B-Reasoning-2512.gguf`. Those staging decisions were later superseded by the repo-local `models/` store migration and by removing the rejected DeepSeek and Mistral-family candidates. Impact: keep this note as historical context only; do not treat `~/.cache/openwendy/gguf` as the current durable model store.
[qwen-3-coder removal and qwen-3.5-abl restore] outcome — `Qwen3-Coder-30B-A3B-Instruct-Q4_K_M.gguf` was deleted from `~/.cache/openwendy/gguf`, the temporary `qwen-3.5-35b` service was unloaded, and the active user `localllm-main.service` was restored to the default `qwen-3.5-abl` profile. The live service on `8091` again points at `Huihui-Qwen3.5-9B-abliterated-Q4_K_M.mradermacher.gguf` with `mmproj-Huihui-Qwen3.5-9B-abliterated-Q8_0.mradermacher.gguf`, `-c 131072`, and `--image-max-tokens 12288`, and it is back at about `6978 MiB` idle VRAM. Impact: the box is back on the default Qwen3.5 abliterated state, with the large coder checkpoint removed from disk.
[qwen-3.5-35b active on 8091] outcome — the active user `localllm-main.service` was switched from the larger coder model to `Qwen3.5-35B-A3B-Q4_K_M.gguf` with `mmproj-F16-Qwen3.5-35B-A3B.gguf` and alias `qwen-3.5-35b`. A quick `64k` scratch tune found the faster `-np 1 -tb 8 -b 128 -ub 64 -cram 0 -fa on --threads-http 4 -rea on --metrics --no-warmup -ctk q4_0 -ctv q4_0` profile to be slightly lighter and materially faster than the more conservative `b64/ub32` option, so that profile is now live at `-c 65536` on `8091` with about `9668 MiB` process VRAM. Impact: current traffic on `8091` is hitting the new 35B hybrid model on the highest quickly validated context target requested for this cutover.
[qwen-3-coder 96k context] outcome — after the initial `8192` service context proved unusable for a first-turn `~10k` prompt, the active `qwen-3-coder` service was re-tuned and reloaded at `-c 98304`. A scratch probe confirmed `98304` fits on this `10 GiB` box when the service uses smaller batches plus quantized KV (`-b 64 -ub 32 -ctk q4_0 -ctv q4_0`), and the live service now runs with that exact profile on `8091` at about `8946 MiB` process VRAM. Impact: the coder service now has materially more usable context for the agent harness without dropping below the requested `64k` floor.
[qwen-3-coder active on 8091] outcome — the active user `localllm-main.service` was switched from `qwen-3.5-abl` to `Qwen3-Coder-30B-A3B-Instruct-Q4_K_M.gguf` with alias `qwen-3-coder`. The live service now runs at `-c 8192` with `--gpu-layers auto --fit on` and the tested smaller-batch hybrid profile, and it came back healthy on `8091` at about `9110 MiB` process VRAM. Impact: current harness traffic on `8091` is hitting the larger coder model rather than the earlier 9B multimodal stack.
[launcher empty-mmproj fix] outcome — `scripts/serve-main.sh` now preserves an explicitly empty `MAIN_MMPROJ` instead of silently falling back to the GLM default projector. Impact: text-only model launches through the shared service script can now disable multimodal projector loading correctly, which was required for the `qwen-3-coder` cutover.
[35B base cleanup and unsloth 35B download] outcome — the mistaken `Qwen3.5-35B-A3B-Base.Q4_K_M.gguf` and `Qwen3.5-35B-A3B-Base.mmproj-Q8_0.gguf` files were deleted from `~/.cache/openwendy/gguf`, and the intended `unsloth/Qwen3.5-35B-A3B-GGUF` artifacts were downloaded instead as `Qwen3.5-35B-A3B-Q4_K_M.gguf` plus `mmproj-F16-Qwen3.5-35B-A3B.gguf`. Impact: the local model cache now matches the requested non-base 35B repo and no longer carries the mistaken base checkpoint.
[large-model hybrid probes] outcome — two newly downloaded larger models were probed one at a time on a scratch port with `--gpu-layers auto --fit on` and a reduced `-c 8192` context to maximize practical GPU offload while allowing CPU/RAM spill. `Qwen3.5-35B-A3B-Base.Q4_K_M.gguf` plus `Qwen3.5-35B-A3B-Base.mmproj-Q8_0.gguf` loaded successfully, reported `projected to use 20189 MiB`, `offloaded 41/41 layers to GPU`, kept a `CPU_Mapped model buffer` of `19779.53 MiB`, used a `CUDA0 model buffer` of `8168.59 MiB`, idled around `9444 MiB` GPU, and answered a tiny raw completion at about `35.9` prompt tok/s and `45.2` gen tok/s. `Qwen3-Coder-30B-A3B-Instruct-Q4_K_M.gguf` also loaded successfully, reported `projected to use 18330 MiB`, `offloaded 49/49 layers to GPU`, kept a `CPU_Mapped model buffer` of `17447.91 MiB`, used a `CUDA0 model buffer` of `7758.42 MiB`, idled around `8938 MiB` GPU, and returned a correct tiny coding answer at about `54.6` prompt tok/s and `38.0` gen tok/s. Impact: both large models are technically viable on this host only as hybrid GPU+RAM loads, but the coder model is the more practical fit; the 35B base model is heavier and not obviously worth the extra cost for general interactive use.
[qwen-3.5-abl reloaded on 8091] outcome — after returning briefly to `glm-4.6v`, the active user `localllm-main.service` was switched back to `qwen-3.5-abl` for more bot testing. The live argv again points at `Huihui-Qwen3.5-9B-abliterated-Q4_K_M.mradermacher.gguf` with `mmproj-Huihui-Qwen3.5-9B-abliterated-Q8_0.mradermacher.gguf`, `-c 131072`, and `--image-max-tokens 12288`, and the service returned healthy on `8091` at about `6978 MiB` idle VRAM. Impact: current traffic on `8091` is once again hitting the Qwen3.5 abliterated profile rather than GLM.
[glm-4.6v reloaded on 8091] outcome — after temporary bot testing on `qwen-3.5-abl`, the active user `localllm-main.service` was switched back to `glm-4.6v`. The live argv again points at `Huihui-GLM-4.6V-Flash-abliterated-Q4_K_M.gguf` with `Huihui-GLM-4.6V-Flash-abliterated-mmproj-f16.gguf`, `-c 131072`, and `--image-max-tokens 1792`, and the service returned healthy on `8091` at about `9140 MiB` idle VRAM. Impact: current traffic on `8091` is once again hitting the GLM profile rather than the temporary Qwen3.5 abliterated test load.
[qwen-3.5-abl active on 8091] outcome — the active user `localllm-main.service` was switched from `glm-4.6v` to `qwen-3.5-abl` for bot testing without changing the repo’s durable GLM defaults. The live argv now points at `Huihui-Qwen3.5-9B-abliterated-Q4_K_M.mradermacher.gguf` with `mmproj-Huihui-Qwen3.5-9B-abliterated-Q8_0.mradermacher.gguf`, `-c 131072`, and `--image-max-tokens 12288`, and the service came back healthy on `8091` at about `6978 MiB` idle VRAM. Impact: current bot traffic on `8091` is hitting the Qwen3.5 abliterated vision stack, but the repo launch defaults still describe GLM until a deliberate durable cutover is requested.
[Qwen3.5 README launch args] outcome — the README now includes explicit `MAIN_EXTRA_ARGS` lines for both `qwen-3.5` and `qwen-3.5-abl` at `128k`, and both examples include the measured `--image-max-tokens 12288` cap. Impact: future manual Qwen3.5 launches from this repo no longer need to infer the recommended vision cap from prose alone.
[Qwen3.5 vision-token cap] outcome — both `Qwen3.5-9B` vision-capable pairs kept the full `131072` context without needing a context cut for vision safety on this `10 GiB` RTX `3080`. Using a `4096x4096` stress image, plain `qwen-3.5` rose from a `7308 MiB` base to about `8738 MiB` at `8192`, `9082 MiB` at `10240`, `9390 MiB` at `12288`, and `9726 MiB` at `14336`; `qwen-3.5-abl` rose from a `6978 MiB` base to about `8408 MiB`, `8752 MiB`, `9060 MiB`, and `9396 MiB` at the same caps. Impact: future manual Qwen3.5 launches on this host should keep `128k` context and use `--image-max-tokens 12288` as the sensible default cap; `14336` still works, but on plain `qwen-3.5` it is already too close to the VRAM danger band and too slow to treat as healthy.
[GLM durable vision-token cap] outcome — the durable `GLM` launch defaults now include `--image-max-tokens 1792` in `scripts/serve-main.sh`, the repo systemd unit, and the active user systemd unit, and the managed main service was reloaded with that flag live on `8091`. The verified running PID now advertises `--image-max-tokens 1792` in its argv and is back at the clean `~9140 MiB` main-process idle footprint. Impact: future `glm-4.6v` launches from this repo now carry the measured healthy server-side vision cap by default instead of relying only on clients to remember it.
[GLM image-max-tokens sweep] outcome — a controlled temporary-launch sweep tested `--image-max-tokens` against a worst-case `1792x1792` image while keeping the full `131072` context. At `128k`, retained main-process VRAM stepped from about `9520 MiB` at `1024` tokens to `9736 MiB` at `1792`, `9812 MiB` at `2048`, `9892 MiB` at `2304`, `9978 MiB` at `2688`, and then failed with `CUDA error: out of memory` at `2816`. Impact: the practical healthy server-side cap for this host is `--image-max-tokens 1792`; it keeps large images working at full context while staying below the earlier `9800` to `9850 MiB` restart-trigger band, and it avoids sacrificing `128k` context just to buy vision headroom.
[README multimodal OOM guidance] outcome — the README now records the measured `GLM` vision VRAM envelope, the sticky post-image CUDA workspace behavior, concrete app-side image limits, and the single-digit-second restart timing needed to reclaim VRAM. Impact: future agents no longer need to rediscover that text turns stay inside the reserved context budget while large image turns can ratchet VRAM upward until restart on this host.
[GLM restart timing] outcome — timing a fresh reload from an already loaded `GLM` state showed the managed restart is short but not free. After terminating the live PID, the replacement PID appeared in about `3.48 s`, `/health` was reachable again in about `8.41 s`, and the service logs put `main: model loaded` plus `server is listening` at roughly `6` to `7 s` after the kill. Impact: an automated VRAM-ceiling restart is operationally plausible on this host because the outage window is single-digit seconds rather than tens of seconds, but it still creates a visible brief brownout and will drop any in-flight request.
[GLM multimodal VRAM envelope] outcome — a cold restart confirmed the true clean idle for the live `GLM` service is about `9140 MiB` for the main `llama-server` process and about `9146 MiB` total on the GPU, matching the earlier `~862 MiB free` baseline. A single `1024x1024` image request then left the same PID resident at about `9622 MiB`, and the logs show why: the multimodal path raised `alloc_compute_meta: CUDA0 compute buffer size` from the startup `208.01 MiB` to `333.23 MiB`, and that larger allocation stayed resident after the request instead of shrinking back. Impact: yes, the observed growth comes from multimodal allocations that are not released after use on this build.
[GLM image-size failure boundary] outcome — bounded restart-between-tests found a narrow practical image ceiling for the current `GLM` deployment on this `10 GiB` card. From a clean `9140 MiB` baseline, `1280x1280` succeeded and left the process at about `9838 MiB` with `alloc_compute_meta` at `515.05 MiB`; `1408x1408` also succeeded and left the process at about `9950 MiB` with `alloc_compute_meta` at `608.52 MiB`; but `1472x1472` already hit `CUDA error: out of memory` after `alloc_compute_meta` rose to `683.73 MiB`, and `1536x1536` likewise OOMed at `736.31 MiB`. Impact: for this exact build and launch profile, the largest tested safe square image from a cold start is `1408x1408`, and the remaining margin above that point is only about `60 MiB` of process VRAM, so multimodal use on this card must be treated as operating very close to the hard limit.
[GLM idle VRAM increase] outcome — the live `GLM` main service on `8091` is still running the same `131072` context, the same `ebbf544` `llama.cpp` build, and the same tuned launch flags, so the rise to about `9930 MiB` in `nvtop` is not a silent config drift. The current PID `1971495` started with `CUDA0 model buffer = 5539.01 MiB`, `CUDA0 KV buffer = 1440.02 MiB`, `CUDA0 compute buffer = 208.01 MiB`, and prompt cache disabled, but later the same process logged a new `alloc_compute_meta: CUDA0 compute buffer size = 548.89 MiB` after multimodal requests that processed images. Impact: the extra roughly `341 MiB` of retained CUDA compute allocation explains most of the post-start VRAM growth, and on this build it appears to stay resident until the process is restarted rather than shrinking back to the earlier idle baseline.
[glm durable default] outcome — `scripts/serve-main.sh` and `systemd/localllm-main.service` were updated so the durable `localllm-main.service` default is now the tuned `GLM` stack, not the old Qwen3-VL profile. The active user service now serves `Huihui-GLM-4.6V-Flash-abliterated-Q4_K_M.gguf` with alias `glm-4.6v`, `131072` context, and the tested `-np 1 -tb 8 -b 512 -ub 256 -cram 0 -fa on --threads-http 4 -ctk q4_0 -ctv q4_0 -rea on --metrics --no-warmup` profile. Impact: restarts and reboots now preserve the chosen `GLM` default instead of requiring a manual screen-based override.
[glm active on 8091] outcome — the managed Qwen3-VL main service was stopped and `glm-4.6v` was loaded on `8091` in a detached `screen` session with the tuned `GLM` profile from the README. `/props` reports alias `glm-4.6v`, vision support, metrics enabled, and effective context `131072`. Impact: app and harness testing can now target the tuned `GLM` endpoint directly without editing request `model` names if they rely on `/props`.
[glm 128k relaunch] outcome — `glm-4.6v` was relaunched on `8091` with a direct `131072` launch context instead of the older `160768`-with-cap experiment. `/props` now matches the launch exactly at `131072`, and the GPU sits at about `862 MiB` free at idle on this profile. Impact: the live `GLM` screen session now matches the model-maker `128k or less` guidance exactly, and the README no longer needs to describe a capped launch mismatch as the active state.
[coding benchmark promotion] outcome — the corrected uncapped coding comparison was promoted from `/tmp` into `benchmarks/coding_compare.sh` as a reusable repo benchmark. Impact: future coding-quality comparisons no longer need to rebuild the same one-off shell harness by hand, and the relevant scratch script has a durable home in the repo.
[comparison harness correction] outcome — the user corrected two mistakes in the last coding comparison pass: the managed `8091` Qwen3-VL service was allowed to coexist with scratch candidate loads for part of the run, and the pass also used `max_tokens=4096` even though the repo guidance for thinking-first finalists is to avoid a hard completion cap during viability comparisons. Impact: future cross-model comparisons in this repo must first stop the managed main service cleanly, verify the GPU is empty before each candidate load, and avoid `max_tokens` caps unless the explicit goal is to measure truncation behavior rather than completion viability.
[uncapped coding comparison] outcome — rerunning the coding-focused pass the right way changed the result materially. With the managed main service stopped, one candidate on the GPU at a time, no `max_tokens`, and `thinking_budget_tokens=1000`, both `glm-4.6v` and plain `qwen-3.5` completed the bug-fix and larger implementation prompts successfully. `GLM` remained the cleanest coding model: it produced visible code on all three prompts with moderate reasoning and less visible scaffolding. Plain `qwen-3.5` also completed all three prompts and stayed fast, but still used more reasoning than `GLM`. `qwen-3.5-abl` also completed all three prompts, yet its visible answers were the least disciplined by far, with extreme scaffold leakage and very large visible outputs on the simple and bug-fix prompts. Impact: the corrected coding-quality ordering is now `glm-4.6v` first, `qwen-3.5` second, `qwen-3.5-abl` a distant third.
[temporary GLM screen] outcome — the detached `glm-test` session on `8091` was stopped after the interactive tuning pass. Impact: the temporary experimental endpoint is no longer serving until a model is started again, which avoids confusion between scratch candidate loads and the durable repo default stack.
[coding comparison pass] outcome — a final coding-focused run across `glm-4.6v`, `qwen-3.5`, and `qwen-3.5-abl` used four prompts spanning a tiny refactor, a retry-loop bug fix, an interval-merge implementation, and a task-runner implementation. `GLM` was the only finalist that consistently surfaced full visible code on the heavier implementation prompts at `max_tokens=4096`; both Qwen3.5 variants remained faster on raw decode but spent much more of that budget in reasoning and hit `finish_reason=\"length\"` on the harder prompts. Impact: for coding-heavy harness work, `GLM` currently looks like the cleanest model-side choice of the three even though the Qwen3.5 variants remain attractive for raw speed and long-context fit.
[jinja flag check] outcome — the local `llama.cpp` build already has `--jinja` enabled by default, and current finalist models are using their embedded templates successfully. Impact: `--jinja` is relevant only for template override or template-debug work here, not as a missing switch to fix the current request-shape behavior.
[request-shape correction] outcome — the current endpoint needs both `thinking_budget_tokens` and `chat_template_kwargs.enable_thinking` to be reasoned about separately. Local testing confirms that `thinking_budget_tokens` does budget reasoning, but it does not replace explicit thinking-mode control, and `tool_choice:"required"` is only correct after routing has already decided a tool must be called. Impact: future request-shape guidance should keep `enable_thinking` explicit, preserve tool-call history instead of flattening it, and avoid wasting reasoning budget on router turns.
[benchmark promotion] outcome — the useful local tuning sweeps were promoted into repo-owned helpers under `benchmarks/` instead of staying in `/tmp`. The repo now has one script for safe fixed-context flag comparisons and one for context climbs on a chosen profile. Impact: future model tuning can be repeated from the repo with the same logic instead of rebuilding throwaway shell scripts each time.
[main model aliases] outcome — the launcher now supports `MAIN_ALIAS`, and the README defines stable aliases for the three finalist models: `glm-4.6v`, `qwen-3.5-abl`, and `qwen-3.5`. Impact: future model swaps can use short stable names that also surface cleanly through `/props`.
[props endpoint documentation] outcome — the README now documents `GET /props` as the simplest introspection endpoint for discovering the currently served model alias, path, modality flags, and endpoint capabilities. Impact: future agents and local apps have a documented way to identify the live model without inspecting process arguments.
[qwen-3.5 tuning] outcome — the `unsloth` Qwen3.5 finalist followed the same safe workflow used for `GLM`: sweep flags at `-c 32768`, then climb context on the winning profile. The best tested profile stayed `-np 1 -tb 8 -b 256 -ub 128 -cram 0 -fa on --threads-http 4 -ctk q4_0 -ctv q4_0 -rea on --metrics --no-warmup`, and the closest tested `~500 MiB` headroom point was `-c 362496`, which left about `508 MiB` free while keeping the tiny thinking probe around `95.9 tok/s`. Impact: the plain Qwen3.5 finalist now has a reproducible high-context tuning target on this host.
[qwen-3.5-abl tuning] outcome — the `mradermacher` abliterated Qwen3.5 finalist followed the same safe workflow and landed on the same best-tested launch profile as `unsloth`: `-np 1 -tb 8 -b 256 -ub 128 -cram 0 -fa on --threads-http 4 -ctk q4_0 -ctv q4_0 -rea on --metrics --no-warmup`. Its closest tested `~500 MiB` headroom point was `-c 397312`, which left about `506 MiB` free while keeping the tiny thinking probe around `96.2 tok/s`. Impact: the abliterated Qwen3.5 finalist now has a matching reproducible tuning target, which makes direct model swaps less guessy.
[README GLM test profile] outcome — the README now includes the exact temporary `GLM` test profile, clearly labeled as experimental rather than the durable repo default. Impact: the active `GLM` test run on `8091` can be reproduced without confusing it with the default Qwen3-VL stack.
[GLM launch tuning] outcome — a safe `-c 32768` flag sweep showed the best tested `GLM` profile on this host was `-np 1 -tb 8 -b 512 -ub 256 -cram 0 -fa on --threads-http 4 -ctk q4_0 -ctv q4_0 -rea on --metrics --no-warmup`. The alternatives with `q8` KV cache were slower or consumed more VRAM without a throughput win, and the smaller batch profile was only a near-tie. Impact: the current `GLM` tuning work now has one clear launch profile to reuse for later context sweeps instead of relying on the older scratch defaults.
[GLM context tuning] outcome — after the safe flag sweep, the temporary `GLM` test load on `8091` was re-swept upward on the winning profile. The closest tested headroom match was a launch `-c 160768`, which left about `494 MiB` free VRAM and kept the tiny direct probe around `91.9 tok/s`; nearby points landed at `520 MiB` free for `158720`, `482 MiB` for `161792`, and `454 MiB` for `163840`. The `GLM` runtime then logged that any launch context above the model training context is capped, so the effective slot context on this host is `131072`. Impact: the current temporary `GLM` test load uses the tuned launch profile that lands near the requested headroom target, but the real effective context is `131072`, not the higher launch number.
[single-model request omission] outcome — the local `llama.cpp` server accepted a chat request without a `model` field when only one model was loaded on the endpoint, returning `200` and using the loaded model. Impact: server-side model omission is possible on single-model endpoints, but many OpenAI-compatible clients may still insist on sending `model`; `--alias` remains useful if a shorter stable model name is desired.
[README consistency pass] outcome — the README now distinguishes between the durable repo default stack and temporary experimental candidate loads, clarifies that the recent candidate findings are local comparison results rather than adopted defaults, and notes that a single-model endpoint can accept requests without an explicit `model` field even though many OpenAI-compatible clients still send one. Impact: the README no longer reads as if the Qwen3-VL default stack and the newer finalist findings are simultaneously the same active configuration.
[candidate labeling correction] outcome — the user corrected the model-family framing: `unsloth` is not an abliterated or uncensored variant, while `GLM` and `mradermacher` are the abliterated-style finalists in the current set. Impact: final recommendations should describe `unsloth` as the plain Qwen3.5 path, `GLM` as the cleaner abliterated option, and `mradermacher` as the weaker abliterated fallback.
[GLM tuning workflow] outcome — the user asked for a lower-context tuning pass before raising `GLM` back up, and the results validated that approach: the useful comparison came from sweeping flags at `-c 32768`, then climbing context only after the winning profile was chosen. Impact: future tuning work on large-context models in this repo should start from a reduced-context baseline so fit artifacts do not masquerade as flag wins.
[final decision pass for plain versus abliterated paths] outcome — the final real-use matrix across Telegram-style direct replies, no-tool turns with tools present, two-step tool flow, agent-looping, debugging, and post-failure revision sharpened the deployment recommendation. `GLM` remained the cleanest overall mixed-use model: it stayed concise on Telegram-style prompts, avoided gratuitous tool use, and produced structured agent/debugging answers without obvious scaffold leakage. `unsloth` remained the strongest abliterated Qwen choice: it handled tools correctly, gave strong long-form agent and debugging answers, and improved visibly at `budget=450`, but it still carried heavier reasoning and more verbose answers than `GLM`. `mradermacher` remained viable but weakest of the three for practical deployment, with the worst visible scaffold leakage, including one no-tool timeout when uncapped and very poor `budget=450` Telegram-style output. Impact: the repo now has two clear practical choices — `GLM` as the cleaner main/plain-style option and `unsloth` as the stronger abliterated Qwen option — with `mradermacher` retained only as a distant fallback.
[finalist head-to-head including `mradermacher`] outcome — the practical quality round with `GLM`, `unsloth`, and `mradermacher` confirmed that `mradermacher` is viable but still trails the other two on output discipline. `GLM` was the cleanest mixed-use model overall: direct Telegram-style replies stayed compact, debugging answers stayed structured, and the agent-looping answer was useful without excessive sprawl. `unsloth` was stronger on long-form agent and debugging depth but more prone to heavy reasoning and longer answers; `budget=450` improved compactness but sometimes surfaced planning-scaffold text. `mradermacher` completed everything but was consistently the most verbose and most likely to surface scaffold-like visible text, especially on Telegram-style and budgeted runs. Impact: the real finalist set is `GLM`, `unsloth`, and `mradermacher`, but the current practical ordering is still `GLM` first, `unsloth` second, `mradermacher` third.
[quality comparison after protocol fixes] outcome — once output caps and second-turn harness artifacts were removed, the remaining candidates separated more on answer style than on raw protocol correctness. `GLM` gave the most balanced and disciplined outputs overall: concise enough for Telegram-style direct replies, structured enough for debugging and agent-looping prompts, and less bloated than the Qwen3.5 variants. `unsloth` stayed strong and coherent on the harder planning prompts, but tended to produce heavier reasoning and longer answers; with `budget=450` it became shorter, though some prompts still showed planning-scaffold text in the visible answer. `mradermacher` was consistently the most verbose and least disciplined, often producing very long answers or visible scaffold text even with a budget. Impact: after the protocol issues were resolved, the live tradeoff looks more like `GLM` for cleaner mixed-use behavior versus `unsloth` for a stronger but more overthinking Qwen path, with `mradermacher` trailing mainly on output discipline.
[budget sweep and deeper tool flow] outcome — the sharper rerun showed that all remaining models can survive explicit thinking budgets and still complete the hard autonomous-runtime prompt, but each family has a different cost profile. `GLM` stayed relatively balanced across budgets `50` through `1000`, always returning visible content with modest to moderate reasoning. `unsloth` also stayed robust across that range, but its visible answer often began with planning-scaffold language at lower budgets and carried heavier reasoning at higher budgets. `mradermacher` and `Abhiray` behaved very similarly to each other: both completed every budgeted run, both produced especially long visible answers plus heavy reasoning, and both looked more verbose and less disciplined than `GLM` or `unsloth`. Impact: the useful budget question is no longer “does a budget work?” but “which model keeps the cleanest visible answer at a given budget?”
[deeper second-turn tool flow] outcome — replaying the model’s actual first-turn assistant message instead of a hand-built assistant stub fixed the second-turn tool path for all four remaining models. `GLM`, `mradermacher`, `Abhiray`, and `unsloth` all emitted structured `tool_calls` on the first turn and then returned a visible final answer after the tool result on the second turn. Impact: the earlier `400` errors were a harness-format artifact, not evidence that these models fundamentally fail second-turn tool integration.
[mradermacher versus Abhiray] outcome — the two abliterated Qwen3.5 variants now look effectively redundant. Across the uncapped runs, budget sweep, and corrected second-turn tool flow, `mradermacher` and `Abhiray` both completed successfully, both emitted first-turn `tool_calls`, both completed second-turn tool integration, and both showed the same broad tendency toward very long visible answers plus heavy reasoning. Impact: `Abhiray` can be dropped from further testing without losing much information because it does not appear meaningfully distinct from `mradermacher` on the current suite.
[thinking-budget threshold lesson] outcome — the user found that on the loaded `unsloth` Qwen3.5 server, `./scripts/chat.sh -m 32768` with a nonzero thinking budget shows a threshold effect rather than a simple on/off improvement: budgets below roughly `450` let truncated reasoning leak into visible output, while budgets around `450` and above truncate reasoning more cleanly before the final answer. Impact: future budget tests should look for the model-specific “clean handoff threshold” instead of only asking whether a budget exists at all. Prevention rule: when evaluating thinking budgets, score both visible-answer cleanliness and whether reasoning spills into the final answer, not just whether the model eventually responds.
[chat helper request knobs] outcome — `./scripts/chat.sh` now exposes request-level `--temp`, `--top-p`, `--top-k`, `--presence`, `--repeat`, and `-B` / `--thinking-budget` flags in addition to `-m` and `-T`. Impact: sampling and thinking-budget experiments can now be done interactively per turn against the loaded model without restarting or retuning the server process.
[uncapped candidate rerun] outcome — rerunning the remaining candidates without any `max_tokens` field changed the interpretation of the earlier Qwen3.5 failures. `GLM`, `mradermacher`, `Abhiray`, and `unsloth` all completed the retry-loop and autonomous-runtime prompts with `finish_reason="stop"` when uncapped, including substantial visible answers plus large `reasoning_content`. Impact: the earlier capped runs were measuring handoff efficiency under pressure, not absolute completion ability; the surviving issue is that some models overthink heavily before the final answer, not that they categorically fail to finish.
[uncapped tool behavior] outcome — without output caps, the remaining models did not show a fundamental “thinking plus tools is broken” pattern on the first tool turn. `GLM`, `mradermacher`, `Abhiray`, and `unsloth` all emitted structured `tool_calls` on the tool-needed prompt with `enable_thinking=true`, and all four answered directly without tool calls on the no-tool-needed prompt. `unsloth` also completed the synthetic second tool turn successfully with a visible final answer; the other three still returned `400` on that synthetic second-turn harness, which means the remaining second-turn uncertainty is currently a harness-format issue rather than clear evidence of bad first-turn tool selection. Impact: the main viability question is now reasoning efficiency and second-turn tool integration, not first-turn tool-call emission.
[lukey03 cleanup] outcome — the discarded `lukey03` text and vision artifacts were removed from cache. Impact: the staged Qwen3.5 comparison set now focuses on `mradermacher`, `Abhiray`, and `unsloth`, plus `GLM` as the non-Qwen control.
[thinking-history handling] outcome — the user’s uncapped `unsloth` tests suggest the model can complete reliably when given enough room, which shifts the likely app-level optimization target from “force shorter generations” to “avoid feeding old thought text back into new turns.” Impact: for thinking-first models, stripping prior `<think>` blocks or `reasoning_content` from the persisted conversation history is a promising default because it should reduce context bloat and may reduce repeated overthinking on later turns. Prevention rule: when evaluating app behavior for these models, distinguish between the model’s internal thinking on the current turn and whether prior turn reasoning is unnecessarily being replayed into the next prompt.
[Qwen3.5 testing lesson] outcome — the user confirmed that running `./scripts/chat.sh -m 32768` against the loaded `unsloth` Qwen3.5 server let the same planning prompts complete with full visible answers. Impact: earlier Qwen3.5 conclusions that relied on low `max_tokens` caps such as `512` or `768` may have overstated the plain-thinking failure mode by forcing `finish_reason="length"` before the model could transition from reasoning into a final answer. Prevention rule: for thinking-first models, do not treat low capped-token runs as decisive evidence about final-answer handoff until an uncapped or very high-cap follow-up is checked.
[loaded-model streaming chat helper] outcome — `./scripts/chat.sh` no longer launches a temporary CPU model. It now discovers currently loaded chat endpoints, offers an arrow-key picker for those live models, and streams SSE deltas from the selected endpoint. Streamed reasoning prints in gray, visible reply text in white, and tool-call deltas in cyan. Impact: manual testing now exercises the actual loaded runtime instead of a side CPU path, which is better aligned with debugging real model behavior.
[temporary unsloth test load] outcome — the current main Qwen3-VL service was stopped and `Qwen3.5-9B-Q4_K_M.unsloth.gguf` plus `mmproj-Qwen3.5-9B-F16.unsloth.gguf` was loaded manually on `8091` with the corrected scratch profile (`-c 32768`, `-fa on`, `-ctk q4_0`, `-ctv q4_0`). A direct `./scripts/chat.sh` smoke test against `8091` streamed gray reasoning text followed by a white visible `OK`. Impact: the host is temporarily set up for interactive `unsloth` testing through the live-streaming chat helper rather than the normal systemd-managed Qwen3-VL service.
[single-model deployment constraint] outcome — the real selection problem is not “best model per task” but “best single GPU-served model across Telegram plus coding-agent use.” Impact: future testing should compare `GLM` and the surviving Qwen3.5 variant as unified deployments and favor harness workarounds, turn policies, or constrained outputs over any design that assumes two concurrently served main models.
[research-backed next test framing] outcome — current provider docs and current `llama.cpp` docs point to a more precise next-round test split. Qwen’s docs say Qwen3 uses Hermes-style tool calling, allows additional assistant content alongside tool calls, treats tool results as user messages, supports `/think` and `/no_think` soft switches, and advises against stopword-based ReAct templates for reasoning models because thought text can interfere with tool stopping logic. GLM’s docs describe hybrid reasoning as part of the intended agent workflow, with thinking mode for complex reasoning and tool use and non-thinking mode for immediate responses. Current `llama.cpp` help also exposes `--chat-template-kwargs`, `--reasoning`, `--reasoning-budget`, `--reasoning-format`, and custom chat-template controls, while issue history shows that Qwen thinking toggles and template behavior have had real regressions. Impact: the next test strategy should stop treating all prompts the same and instead evaluate three distinct turn classes — direct chat turns, tool-routing turns, and long-horizon coding-agent turns — because the model vendors themselves imply different control policies for those classes.
[Qwen3.5 rerun runtime compatibility] outcome — the `-c 32768` comparison pass required two harness corrections before model behavior could be measured. The `Qwen3.5-9B-abliterated-vision-Q4_K_M.lukey03.gguf` file still fails to load on the current `llama.cpp` build with `qwen35.rope.dimension_sections has wrong array length; expected 4, got 3`, so it remains unusable here. Separately, all Qwen3.5 text variants rejected the speed-tuned `-ctk q4_0 -ctv q4_0` scratch profile unless `-fa on` was also enabled, reporting `V cache quantization requires flash_attn` when flash attention was off. Impact: the meaningful Qwen3.5 rerun set is `mradermacher`, `Abhiray`, `unsloth`, and `lukey03` text, all under a flash-attention-enabled scratch profile.
[Qwen3.5 rerun at `-c 32768`] outcome — the corrected rerun reproduced the original split between ordinary thinking turns and tool-capable turns instead of eliminating it. `mradermacher`, `Abhiray`, and `unsloth` all returned `content=\"\"` plus sizable `reasoning_content` on plain `enable_thinking=true` prompts like `Reply with exactly OK.` and the two longer quality prompts, while `enable_thinking=false` returned a clean visible `OK`. Yet the same models still emitted structured `tool_calls` on the add-tool prompt, especially with `enable_thinking=true`, showing that tool-enabled turns remain more reliable than ordinary thinking turns. Impact: the old Qwen3.5 “tools work better than plain thinking” weirdness is still present across most of the repackaged variants, so this is not just one bad GGUF.
[Qwen3.5 rerun best candidate] outcome — `Qwen3.5-9B-abliterated-Q4_K_M.lukey03.gguf` was the only Qwen3.5 variant in this rerun that consistently produced visible English answers on the plain `enable_thinking=true` quality prompts at `-c 32768`. It returned `OK.` with `reasoning_len` about `372` on the trivial probe, a useful retry-loop plan with `content_len` about `1453`, and a substantial autonomous-runtime answer with `content_len` about `1104`, though the longer architecture answer still hit `finish_reason=\"length\"`. It also emitted `tool_calls` on both thinking-enabled and thinking-disabled tool turns, but the non-thinking tool turn mixed visible answer text with the tool call. Impact: among the Qwen3.5 variants staged so far, `lukey03` text is the only one that looks meaningfully better than the original behavior rather than just repackaging the same failure mode.
[GLM rerun at `-c 32768`] outcome — the same corrected scratch profile showed that `Huihui-GLM-4.6V-Flash-abliterated-Q4_K_M.gguf` is more nuanced than the earlier failed pass suggested. At `-c 32768`, it returned visible `OK` with `enable_thinking=true`, returned a usable retry-loop answer with both visible content and reasoning, and emitted structured `tool_calls` on both thinking-enabled and thinking-disabled tool turns. However, it still failed the harder autonomous-runtime prompt by spending the full token cap in reasoning and returning no visible content. Impact: GLM remains better than most Qwen3.5 variants on mixed visible-answer plus tool behavior, but it still is not a fully reliable plain-thinking answer model on harder prompts.
[Qwen3.5 rerun baseline] outcome — the next Qwen3.5 comparison pass should start at `-c 32768` instead of the larger live Qwen3-VL context. Impact: the first rerun isolates model behavior around reasoning and tools from VRAM-fit and large-context slowdown noise.
[terminal chat helper] outcome — `./scripts/chat.sh` now provides a quick terminal path to the live main model with optional system prompt, optional reasoning display, prompt text from argv or stdin, and the current live model defaults baked in. Impact: light manual sanity checks no longer need ad hoc `curl` or bot traffic, while still exposing the same model-family caveat that `enable_thinking=false` may inline `<think>` text into the visible reply.
[llama.cpp metrics endpoint] outcome — the live main service now starts with `--metrics`, `GET /metrics` responds on `8091`, and `/props` reports `endpoint_metrics = true`. Impact: Grafana or Prometheus can scrape the live main model directly without changing the current model, context, or speed-tuned flags beyond enabling the endpoint.
[download staging for new model candidates] outcome — requested GGUF files were downloaded into `~/.cache/openwendy/gguf` without testing, loading, or changing the live `localllm` services. Main candidates staged are `gemma-3-12b-it-uncensored_Q4_k_m.gguf` with `gemma-3-12b-it-uncensored_mmproj-f16.gguf` and `Huihui-GLM-4.6V-Flash-abliterated-Q4_K_M.gguf` with `Huihui-GLM-4.6V-Flash-abliterated-mmproj-f16.gguf`; fun models staged are `ideal_girlfriend_Melina.IQ4_XS.gguf`, `hilarious-shitpost-llama.IQ4_XS.gguf`, `MoE-Girl-1BA-7BT.i1-IQ3_XS.gguf`, `ai-girlfriend-v2.i1-IQ4_XS.gguf`, and `gemma3-4b-tolkien.Q3_K_M.gguf`. Impact: the next comparison round can start from local cache with no runtime churn, while the current live Qwen3-VL service remains untouched.
[CPU chat helper] outcome — `./scripts/chat.sh` now launches a temporary CPU-only `llama-server` for a selected staged model instead of talking only to the live GPU service. In a TTY with no explicit `CHAT_MODEL`, it opens an arrow-key menu, then runs a multi-turn chat loop with `/exit` and `/reset`; in scripted mode, setting `CHAT_MODEL` keeps a non-interactive single-turn path. The CPU profile uses smaller context and conservative batching with `-fa off`, and the helper now enforces a request timeout so odd model templates cannot hang forever. Impact: ad hoc local chats with staged CPU models no longer require manual ports or service juggling, and the live Qwen3-VL stack stays untouched.
[CPU fun-model load check] outcome — all five staged fun models loaded successfully one at a time on CPU with the temporary helper profile and were unloaded afterward: `ideal_girlfriend_Melina.IQ4_XS.gguf`, `hilarious-shitpost-llama.IQ4_XS.gguf`, `MoE-Girl-1BA-7BT.i1-IQ3_XS.gguf`, `ai-girlfriend-v2.i1-IQ4_XS.gguf`, and `gemma3-4b-tolkien.Q3_K_M.gguf`. Impact: the staging set is confirmed loadable on host RAM without touching the live GPU-backed services.
[CPU helper model scope] outcome — the interactive CPU chat menu now lists only staged fun models, not the larger main-candidate checkpoints. If no fun models are staged, the helper requires `CHAT_MODEL` explicitly. Impact: the selector stays aligned with its lightweight exploratory purpose and avoids confusing the main candidate evaluation path.
[fun-model cache cleanup] outcome — the staged fun models were deleted from `~/.cache/openwendy/gguf` after user testing: `ideal_girlfriend_Melina.IQ4_XS.gguf`, `hilarious-shitpost-llama.IQ4_XS.gguf`, `MoE-Girl-1BA-7BT.i1-IQ3_XS.gguf`, `ai-girlfriend-v2.i1-IQ4_XS.gguf`, and `gemma3-4b-tolkien.Q3_K_M.gguf`. Impact: only the current live Qwen3-VL stack and the two main GPU candidates remain staged.
[Gemma 12B candidate] outcome — `gemma-3-12b-it-uncensored_Q4_k_m.gguf` plus `gemma-3-12b-it-uncensored_mmproj-f16.gguf` loaded on the RTX 3080 at `-c 8192` and avoided the earlier reasoning-only empty-response failure entirely in the tested path: both `enable_thinking=true` and `enable_thinking=false` returned visible `OK.` with no `reasoning_content`, and the longer quality prompts returned substantial English answers in `content`. The tool path was weaker: on an add-tool prompt with `tool_choice:auto`, it ignored the tool schema and answered directly with prose instead of emitting `tool_calls`. Impact: Gemma looks operationally cleaner than the current Qwen-style thinking models for visible-answer reliability, but weaker if strict tool-call obedience matters.
[GLM 4.6V candidate] outcome — `Huihui-GLM-4.6V-Flash-abliterated-Q4_K_M.gguf` plus `Huihui-GLM-4.6V-Flash-abliterated-mmproj-f16.gguf` loaded on the RTX 3080 at `-c 8192`, but it reproduced the same core annoyance as the rejected thinking-first models: with `enable_thinking=true`, even trivial prompts returned `content=\"\"` and only `reasoning_content`, and the longer quality probes exhausted the token cap entirely inside reasoning. The reasoning text also drifted into Chinese. With `enable_thinking=false`, the simple `OK` probe returned a clean visible answer. Tool handling was better than Gemma: on the add-tool prompt it emitted `tool_calls` on the first turn and returned `4` on the tool-response turn, but still included extra reasoning content. Impact: GLM preserves a usable tool flow, but it still fails the visible-answer reliability bar in thinking mode and reintroduces the language-drift risk.
[Gemma tool-guidance follow-up] outcome — stronger tool prompting clarified the limitation rather than fixing it. With `tool_choice:auto`, Gemma either answered in prose or asked for the tool to be provided; with a stronger system prompt it emitted plain text that looked like a tool invocation, but still not structured OpenAI `tool_calls`; and even `tool_choice:\"required\"` did not make it emit valid `tool_calls`. Impact: Gemma’s weak tool behavior is likely a format mismatch rather than just a soft prompt issue, so downstream code expecting OpenAI-style tool-call objects should treat it as incompatible or require a model-specific parser.
[GLM guided follow-up] outcome — GLM became materially more usable once turns were guided by mode. Adding an English-only system prompt made the trivial thinking probe return visible `OK` plus short English `reasoning_content`, which suggests the earlier Chinese reasoning was partly a token-efficiency choice rather than incoherent thought. With `enable_thinking=false`, a longer architecture prompt returned a large English answer in `content`, and the tool flow remained clean: it emitted structured `tool_calls` on the first turn and returned `4` after the tool response, all without hidden reasoning. Impact: GLM still has the risky empty-visible-answer behavior in unconstrained thinking mode, but it looks much stronger if the app disables thinking on tool turns and possibly on ordinary answer turns that need immediate visible output.
[translated GLM reasoning sample] outcome — the Chinese reasoning excerpts were valid and on-task when translated. For example, the empty-response `OK` probe reasoning translated to: “The user needs the reply ‘OK’; confirm the task requires this short response; output OK exactly with no extra explanation or formatting.” The longer Chinese reasoning samples for retry-loop refactoring and production debugging also tracked the prompt contents correctly, just in Mandarin. Impact: the language drift is a UX and integration problem, not obvious evidence of broken reasoning.
[model-doc alignment] outcome — official docs align with the observed differences. Google’s Gemma guidance describes function calling through prompt formatting and does not imply native OpenAI-style tool-call objects for base chat behavior, which matches Gemma’s prose or pseudo-tool outputs here. Z.AI’s GLM 4.6 materials describe hybrid thinking and interleaved tool use, which matches GLM’s strong tool path and also explains why suppressing or steering thinking per turn is likely the right harness control. Impact: the local results are not random quirks; they fit the intended model families, so model-specific orchestration is justified.
[live-service restore after candidate tests] outcome — the live `localllm-main.service` was temporarily runtime-masked so `Restart=always` would not respawn the current Qwen3-VL model during candidate testing, then it was unmasked and restarted cleanly after the Gemma and GLM runs. Impact: candidate testing stayed isolated on the GPU without leaving the host in a partial state, and the current Qwen3-VL service is back on `8091`.
[Gemma candidate cleanup] outcome — the discarded Gemma candidate files were removed from cache. Impact: the staged set now focuses on the current live Qwen3-VL model, GLM, and the new Qwen3.5 comparison candidates rather than keeping a candidate already ruled out for tool-fit reasons.
[Qwen3.5 restaging] outcome — four Qwen3.5 candidate families were staged for the next comparison round without loading or testing them: `Qwen3.5-9B-abliterated-Q4_K_M.lukey03.gguf`, `Qwen3.5-9B-abliterated-vision-Q4_K_M.lukey03.gguf`, `Huihui-Qwen3.5-9B-abliterated-Q4_K_M.Abhiray.gguf` with `mmproj-Huihui-Qwen3.5-9B-abliterated-f16.Abhiray.gguf`, `Qwen3.5-9B-Q4_K_M.unsloth.gguf` with `mmproj-Qwen3.5-9B-F16.unsloth.gguf`, and `Huihui-Qwen3.5-9B-abliterated-Q4_K_M.mradermacher.gguf` with `mmproj-Huihui-Qwen3.5-9B-abliterated-Q8_0.mradermacher.gguf`. Impact: the next test pass can compare Qwen3.5 variants side by side, including Lukey03’s separate text and vision-packaged artifacts plus a quantized projector path from `mradermacher`.
[Qwen3-VL-8B unbounded answer-first test] outcome — removing the numeric token cap did not make answer-first steering solve the handoff problem. In streamed observation windows capped at `20 s`, both baseline and answer-first variants produced reasoning quickly but never produced any visible answer tokens on either `coding_medium` or `agent_long`. The runs streamed about `8658` to `9243` reasoning chars in that window and did not complete. Impact: unbounded mode on this checkpoint is even more clearly reasoning-dominant; answer-first prompt wording does not make it surface visible answers promptly under realistic bot latency expectations.
[Qwen3-VL-8B answer-first steering] outcome — answer-first system prompts work slightly better than generic “think less” prompts at reducing reasoning length, but they still do not fix the visible-answer problem on harder prompts. On `coding_medium` at `max_tokens=512`, baseline reasoning was about `2529` chars and the best answer-first phrasing reduced it to about `2038`; on `agent_long`, baseline reasoning was about `2442` chars and the best answer-first phrasing reduced it to about `2239`. Every run still ended with `finish_reason="length"` and no visible final answer. Impact: answer-first phrasing is the strongest prompt-only steering tested so far, but it still is not enough on its own to make this thinking-first checkpoint reliably surface final answers under the same cap.
[Qwen3-VL-8B prompt-only thinking steering] outcome — non-numeric system-prompt instructions can nudge reasoning length downward a little on this checkpoint, but not enough to change the failure mode on harder prompts. On `coding_medium` at `max_tokens=512`, baseline reasoning was about `2176` chars; softer instructions reduced it to about `1975` to `2100`, and stronger phrasing landed around `2039` to `2060`, but every run still ended with `finish_reason="length"` and no visible final answer. On `agent_long`, baseline reasoning was about `2467` chars; the best stronger phrasing reduced it to about `2253`, but again every run still exhausted the budget before any visible answer. Impact: prompt-only steering can trim reasoning by roughly `5%` to `10%` here, but it does not solve the visible-answer latency problem on its own.
[Qwen3-VL-8B UX latency split] outcome — streaming measurements on the current live `-c 98304 -ctk q4_0 -ctv q4_0` profile showed that decode speed is no longer the main UX bottleneck. On the exact-OK probe, the first streamed event arrived in about `0.094 s`, but the first visible content token did not arrive until about `1.043 s` because the model streamed reasoning first. On medium and long technical prompts with `max_tokens=512`, the first streamed reasoning token still arrived quickly, about `0.034 s` to `0.060 s`, but no visible answer token arrived before the generation hit `finish_reason="length"` after about `4.7 s`; those runs emitted only reasoning. Impact: for bot UX on this thinking-first checkpoint, raw tok/s is now good, but visible-answer latency is still dominated by the reasoning-to-answer handoff and token budget rather than by first-token streaming delay.
[Qwen3-VL-8B context follow-up on `q4_0/q4_0`] outcome — after the KV-cache retune freed substantial VRAM, a second context sweep found the new ceiling. `98304` left about `352 MiB` free and held the longer reasoning prompt at about `110.0 tok/s`, essentially matching the `69120` result, while `102400` dropped to about `76.2 tok/s`, `106496` to about `59.5 tok/s`, `110592` to about `48.3 tok/s`, and `114688` to about `40.8 tok/s`. Impact: the live service now runs at `-c 98304`, because it is the highest tested context that preserved the new `q4_0/q4_0` speed regime instead of falling into the next slower fit tier.
[Qwen3-VL-8B KV-cache speed tuning] outcome — a focused flag sweep on the live `mradermacher` `Thinking` profile showed that batch-size tweaks were mostly noise on this workload, while KV-cache type was the meaningful lever. On the exact-OK reasoning probe at `-c 69120`, the baseline live `q8_0/q8_0` profile ran at about `37.4 tok/s`, `q8_0/q4_0` improved to about `81.4 tok/s`, and `q4_0/q4_0` reached about `111.2 tok/s` while still returning visible `OK` plus separate `reasoning_content`. On the longer agent-looping prompt, the same progression was about `35.4 tok/s` to `58.8 tok/s` to `110.4 tok/s`, with `q4_0/q4_0` also leaving about `1546 MiB` free on the GPU. Impact: the live service now uses `-ctk q4_0 -ctv q4_0`, which is materially faster than the previous `q8_0/q8_0` setting on this host without breaking the direct reasoning probes.
[Qwen3-VL-8B `mradermacher` live cutover] outcome — the main `localllm` defaults and live `localllm-main.service` were switched from the earlier `noctrex` package with F16 projector to `Huihui-Qwen3-VL-8B-Thinking-abliterated.Q4_K_M.gguf` plus `Huihui-Qwen3-VL-8B-Thinking-abliterated.mmproj-Q8_0.gguf`. A focused sweep around the new ceiling found `-c 69120` to be the highest tested context that stayed inside the requested `300` to `500 MiB` VRAM headroom band while keeping clean reasoning behavior: the live probe left about `314 MiB` free on the GPU and returned visible `OK` plus separate `reasoning_content` at about `36.6 tok/s`. Nearby results explain the choice: `67584` left about `420 MiB` free at about `37.0 tok/s`, `69632` fell below the headroom floor at about `278 MiB` free, and `73728` switched into a slower fit regime at about `29.5 tok/s`. Impact: the live host now keeps the reasoning-first Qwen3-VL-8B behavior while gaining materially more usable context than the earlier F16-projector package.
[GGUF cache cleanup] outcome — the obsolete Huihui Qwen3.5 `9B` files, the `mradermacher` `Instruct` package, and the old `noctrex` `Thinking` package were removed from `~/.cache/openwendy/gguf`, leaving only the active `mradermacher` `Thinking` pair plus the embedding model. Impact: the cache now matches the live runtime and avoids accidental fallback to rejected packages.
[Qwen3-VL-8B Thinking candidate with `mmproj-Q8_0`] outcome — `mradermacher/Huihui-Qwen3-VL-8B-Thinking-abliterated-GGUF` was downloaded and compared directly against the live `noctrex` `Thinking` stack, then the live service was restored. This package preserves the same reasoning-first surface while materially improving fit on this host: at `47104` it left about `760 MiB` free and answered the tiny `OK` probe with visible `content="OK"` plus separate `reasoning_content` at about `111.5 tok/s`; at `65536` it still loaded cleanly with about `324 MiB` free and answered the same probe with clean `reasoning_content` at about `41.9 tok/s`; and at `131072` it also loaded and answered the probe with clean `reasoning_content`, leaving about `606 MiB` free but slowing to about `14.9 tok/s`. Impact: unlike the current F16-projector live package, this is a real drop-in `Thinking` candidate for larger context on the 10 GB card, because it keeps the reasoning behavior while gaining most of the `mmproj-Q8_0` fit advantage already seen on the `Instruct` package.
[Qwen3-VL-8B Instruct candidate] outcome — `mradermacher/Huihui-Qwen3-VL-8B-Instruct-abliterated-GGUF` with `Q4_K_M` plus `mmproj-Q8_0` was downloaded and compared directly against the live `Thinking` stack, then the live service was restored. The quantized projector changes the fit story dramatically on this host: at `47104` it left about `760 MiB` free and answered a tiny `OK` probe at about `174.4 tok/s`; at `65536` it still left about `324 MiB` free and answered the same tiny probe cleanly at about `72.7 tok/s`; and even `131072` loaded with about `578 MiB` free and still answered the tiny probe, though only at about `28.4 tok/s`. Impact: if the goal is maximizing context fit on this 10 GB GPU, the `Instruct` plus `mmproj-Q8_0` package is materially better than the current `Thinking` package with its heavier projector, but it trades away the dedicated reasoning behavior and `reasoning_content` surface that motivated the current live choice.
[Qwen3-VL-8B context bump] outcome — before the `mradermacher` projector cutover, iterative sweeps on the earlier `noctrex` package with F16 projector found `-c 47104` to be the best live target. That older package left about `372 MiB` free at `47104`, but it failed outright at `65536` because the projector allocation hit CUDA OOM. Impact: this remains useful as the baseline that the new `mmproj-Q8_0` package surpassed.
[Qwen3-VL-8B cutover] outcome — the rejected GLM and Ministral candidates were removed, and the first Qwen3-VL-8B replacement cutover switched the main `localllm` defaults from Huihui Qwen3.5 `9B` to the earlier `noctrex` `Thinking` package with its F16 projector. Impact: this historical step explains why the later `mradermacher` `mmproj-Q8_0` cutover was worth doing; it is no longer the live package.
[Qwen3-VL-8B reasoning toggle] outcome — direct smoke tests against the new main service showed that `chat_template_kwargs.enable_thinking` behaves differently from the earlier Huihui Qwen3.5 `9B` stack. With `enable_thinking=true`, the server returns a clean visible answer plus separate `reasoning_content`. With `enable_thinking=false`, the model may still emit an inline `<think> ... </think>` block inside `content` before the final visible answer. Impact: downstream clients such as `openwendy` should not assume that `enable_thinking=false` yields a clean no-thinking surface on this candidate; they may need to strip inline think tags or normalize replies.
[OpenWendy boundary] outcome — `localllm` kept the real legacy fallback behavior for old OpenWendy binary/model roots in `llama_runtime.py` and `scripts/common.sh`, kept the tests that prove those fallbacks, renamed the code/test language to `legacy` rather than `openwendy`, and trimmed the README to one compatibility section plus one brief consumer note. Focused `ruff` and `unittest` verification passed, so the repo now reflects the shared-runtime boundary more cleanly without lying about the compatibility behavior it still owns.
[llama.cpp runtime refresh] outcome — the live CUDA build was updated from `f17b3be` to upstream `ebbf544` by building a fresh `llama-server`, backing up the previous install to `/home/j/.local/share/openwendy/llama.cpp/bin.cuda-backup-20260315-185703`, and restarting the main and embedding servers with the same launch arguments. Direct probes after the update still show the Huihui `9B` model returning empty `content` plus `reasoning_content` for no-tools `enable_thinking=true`, so the runtime refresh removed version doubt but did not eliminate the underlying reasoning-only output behavior.
[Qwen thinking diagnosis] outcome — the model family is not the blocker: Qwen 3 supports both thinking and non-thinking, and this repo’s own README already records that `chat_template_kwargs.enable_thinking` toggles the mode on the current server. `llama_runtime.py` only launches a generic `llama-server` command and does not inject `--jinja`, `--reasoning-format`, `-rea`, or request-body fields, so failures here are most likely from an older `llama.cpp` build or a client that omits/strips the Qwen-specific toggle rather than from this repo’s Python runtime itself.
[Tuning review] managed runtime now accepts global and per-service extra `llama-server` args — enables the same tuning flags as shell/systemd launches and preserves service-specific override order.
[Benchmark result] main service runs better with `--threads-http 6 -ctk q8_0 -ctv q8_0` added to the existing profile — reduced simple chat probe from ~0.57 s to ~0.13 s and increased free VRAM from ~262 MiB to ~594 MiB without breaking health checks.
[Slot scheduling] `-np 1` means one `llama-server` slot, and overlapping requests are deferred in the server task queue until the slot is free — `localllm` adds no extra rejection layer, so concurrency is serialized unless the client disconnects or an explicit no-slot probe is used.
[GPU upgrade guidance] outcome — for this `llama.cpp`-style local setup, the practical upgrade from a 10 GB RTX 3080 is primarily more VRAM with a stable CUDA path; 24 GB RTX 3090 is the strongest default value if priced near AMD Radeon AI PRO R9700, while 32 GB R9700 only wins when the operator accepts Linux-first ROCm/Vulkan tradeoffs to maximize model fit.
[3090 vs R9700 tradeoff] outcome — official specs make the decision concrete: RTX 3090 offers 24 GB GDDR6X, 350 W board power, and ~936 GB/s peak bandwidth, while Radeon AI PRO R9700 offers 32 GB GDDR6, 300 W TBP, 640 GB/s bandwidth, Linux-first ROCm support on specific distros, and Linux-only ECC memory support; choose 3090 for the least-risk LLM path and R9700 only when the extra 8 GB directly changes target model sizes enough to justify the software tradeoff.
[Reddit and model-fit scan] outcome — community reports still treat 3090 as the predictable single-card sweet spot for local LLMs, while R9700 feedback is mixed between strong llama.cpp/Vulkan or ROCm results and substantial setup, noise, or throttling complaints; current Hugging Face GGUF sizes show 24 GB already covers strong 24B and many 30B/32B Q4-Q5 models, while 32 GB mainly unlocks higher-bit 32B or extra context headroom rather than an entirely new carefree model tier.
[GPU fault] outcome — the current outage is not a project/runtime bug but an NVIDIA driver/hardware event: kernel logged `Xid 79` at `2026-03-15 06:59:44 UTC` with `GPU has fallen off the bus`, `nvidia-smi` reports unknown device-handle errors, and the stuck `llama-server` processes were terminated to leave the box ready for reboot-level recovery.
[GPU recovery] outcome — after reboot the RTX 3080 returned cleanly: this boot has no fresh `Xid` logs, `nvidia-smi` reports `P8`, about `13 W`, `35 C`, `0%` fan, and no active `llama-server` processes, which is a normal idle recovery state rather than evidence of an immediately persistent fault.
[Systemd autostart] outcome — the model services were installed, enabled, and started on boot, but both crashed in a restart loop because `llama-server` under systemd could not locate sibling `libmtmd.so.0`; exporting the binary directory into `LD_LIBRARY_PATH` from the shared launcher fixed the root cause, and both `localllm-main.service` and `localllm-embedding.service` now stay up and return `/health = 200 OK`.
[GPU power cap persistence] outcome — the host now has a repo-managed root systemd unit definition for reapplying an NVIDIA power cap at boot with an `/etc/default/localllm-gpu-power-limit` override; unit syntax verifies cleanly, but installing or enabling it still requires a privileged `systemctl` step outside this shell.
