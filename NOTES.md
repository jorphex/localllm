# Active
[Ornith 35B quick fit 2026-06-26] outcome — with all managed model services unloaded, both new `models/ornith/` GGUFs fit full `262144` context entirely in R9700 VRAM using q8 KV, Vulkan, `--gpu-layers 999`, `-fa on`, no mmproj, and prompt cache RAM/checkpoints disabled. Best quick Q5 shape was `Ornith-1.0-35B-Q5_K_M.gguf` at `c262144 / b4096 / ub2048`, using about `27856 MiB` VRAM at load and measuring `3070 pp/s, 113.6 tg/s` on the 7k prompt plus `2548 pp/s, 107.3 tg/s` on the 29.7k prompt; larger `b8192/ub4096` also fit but reduced PP, so it is rejected. Best quick Q6 shape was `Ornith-1.0-35B-Q6_K.gguf` at `c262144 / b4096 / ub2048`, using about `31402 MiB` VRAM at load and measuring `3043 pp/s, 107.8 tg/s` on the 7k prompt plus `2450 pp/s, 98.5 tg/s` on the 29.7k prompt; `b2048/ub1024` leaves more VRAM and slightly improves long TG to about `101.5` at lower PP. Impact: for a fast full-context Ornith trial on this 32 GiB card, start with Q5 `256k / b4096 / ub2048`; Q6 is viable only if the quality difference justifies the tighter headroom and slower TG.

[R9700 compute/bandwidth peak measurement 2026-06-20] outcome — ran `llama-bench` on the active `Qwen3.6-27B-abliterated-MTP-Q6_K-Huihui.gguf` (27.3B params / 22.4 GB) to compare actual hardware utilization to AMD's published R9700 specs. Vulkan measured `917.8 pp/s` → ~50.1 TFLOPS and `25.0 tg/s` → ~561 GB/s; HIP measured `676.5 pp/s` → ~37.0 TFLOPS and `23.6 tg/s` → ~530 GB/s. Prompt processing reaches roughly the FP32 ceiling (~47.8 TFLOPS published) but is far below the FP16 matrix peak (383 TFLOPS), likely because Q6_K uses integer/quantized compute paths rather than tensor cores. Raw token generation uses ~83-88% of the published 640 GB/s memory bandwidth, so TG *is* memory-bandwidth-bound at the raw level. The user's runtime uses draft-MTP speculative decoding, which raised effective TG to ~47-56 tok/s in the managed server probe, giving an effective bandwidth well beyond the raw memory limit; that is why memory bandwidth no longer looks like the practical bottleneck. After user feedback that the first combined chart was confusing, two redesigned options were produced: `social/x/5a-r9700-peak-split.png` (vertically stacked raw TFLOPS and GB/s charts) and `social/x/5b-r9700-utilization.png` (measured as % of published peak). Both removed the small footer text, narrowed plot boxes to keep value labels inside the panel border, increased bar/value font sizes, and added vertical breathing room for Twitter readability. Tweet copy and a two-tweet thread draft are in `social/x/5-r9700-peak.md`. The renderer was moved to `scripts/social/render_r9700_peak.py` and is called from `scripts/social/render_terminal_x_series.py`. The Vulkan build was rebuilt with `LLAMA_BUILD_EXAMPLES=ON` to produce `~/.local/src/llama.cpp/build-vulkan-r9700/bin/llama-bench`. Verification: `ruff check --fix`, `ruff check`, `uv run python -m py_compile`, and `pytest -q` pass; the original `qwen-3.6-27b-mtp-huihui-q6-fast-128k` preset was reloaded and all four stack services are healthy.

[Local LLM performance and cleanup pass 2026-06-19] outcome — the live 27B Huihui MTP stack was profiled and tuned. The largest real-world speed win came from raising the GPU power cap from 210 W to 300 W: at 300 W the 27B q8 128k preset measured about `790 pp/s` medium and `680 pp/s` long, versus about `704 pp/s` medium and `598 pp/s` long at 210 W, with long decode rising from about `42.4` to `47.4 tok/s`. A controlled test showed the AMD 7.2.4 driver update and the llama.cpp `b9496` → `b9716` upgrade contributed only marginal PP differences; the power cap was the dominant lever. The production llama.cpp source was consolidated to `~/.local/src/llama.cpp` at `b9716`, extra worktrees (`llama.cpp-b9716`, `llama.cpp-upstream-r9700`) were removed, and the runtime selector was simplified. Low-risk tuning changes were committed: `--threads-http 4` → `2`, `--image-max-tokens 12288` → `8192`, and `--ctx-checkpoints 8` → `0` for the 27B agent preset. Prompt cache was intentionally left disabled because q8 × 128k serialized states are ~20 GiB and OOMd the 31 GiB host at `MAIN_CACHE_RAM=8192`. The stale `localllm-amd-power-cap.service` and `/etc/default/localllm-amd-power-cap` were removed so the GPU boots to its 300 W default. Verification: all four stack services healthy, main running `version 1306 (db52540f7)` on Vulkan, VRAM ~32.7 / 34.2 GB, no CPU tensor/KV spill warnings, and commits `4f2d810`/`ba099ae` are clean. Impact: the daily 27B agent stack now runs faster at the cost of higher peak power draw, with a single clean source tree and no stale runtime paths.

[Commit organization 2026-06-17] outcome — the dirty localllm tree was split into scoped commits: `3ccd85b` for the Qwen 3.6 service stack/runtime cleanup, `445c7c6` for benchmark tooling and eval harness updates, and `1494afb` for social/X assets. During organization, `README.md` was corrected to match the live 27B Huihui + GPU reranker stack instead of stale 35B/CPU-reranker text. Verification before committing passed: `ruff check --fix .`, `ruff check .`, shell syntax checks, `uv run python -m py_compile ...`, `pytest -q`, and `systemd-analyze --user verify systemd/*.service systemd/*.timer`. Impact: history now separates runnable service changes from benchmark helpers and generated media, with no known unresolved lint or test failures.
[Qwen 3.6 optimization pass 2026-06-16] outcome — a parallel upstream llama.cpp worktree/build was created at `/home/j/.local/src/llama.cpp-upstream-r9700` with `llama-server version 1259 (a1824902b)`, while the production `build-vulkan-r9700` binary stayed on `version 1086 (94a220cd6)`. Multimodal q8 A/B results: upstream improved 27B Huihui long decode from about `40.9` to `43.1 tok/s` but did not improve PP; upstream improved 35B Huihui with `--direct-io` from about `115.1` to `123.6 tok/s` long decode and from `137.0` to `151.0 tok/s` medium decode; upstream did not materially improve 35B Unsloth (`~93 tok/s` long decode both ways). Safety finding: upstream 35B Huihui without `--direct-io` transiently drove system available RAM down to about `4.2 GiB` during load, and upstream 27B main plus GPU reranker co-load caused severe RAM/swap pressure with invalid VRAM accounting, even when the reranker used the old binary. Impact: do not promote upstream as the global runtime while the reranker is GPU-resident; keep the upstream build only as an isolated main-only test lane, and use `--direct-io` for future upstream 35B Huihui tests.
[Qwen 3.6 safe tuning promotion 2026-06-16] outcome — only low-risk production-build tunings were promoted. All retained Qwen 3.6 presets now cap context checkpoints at `--ctx-checkpoints 8` instead of default/`12`, reducing worst-case checkpoint RAM pressure without a measurable long-probe speed loss (`old 27B n3 cp8` measured about `592 pp/s`, `41.0 tok/s`). The live 27B Huihui preset kept draft-MTP `n=3` because production-build `n=4` was only a tiny decode change (`~41.2 tok/s`) with extra VRAM, while upstream `n=4` could not be safely co-loaded with the GPU reranker. The reranker now uses `-fa on`; the 5-document rerank probe improved slightly from about `0.110-0.163s` to `0.108-0.152s` across repeated calls. Verification: managed main, embedding, reranker, TTS, and idle-flush timer were restarted; live args show production `b9496`, 27B Huihui q8/mmproj, `draft n=3`, `--ctx-checkpoints 8`, reranker GPU with `-fa on`, embedding CPU; health checks pass; live VRAM is about `32.69GB / 34.21GB` decimal and system RAM available is about `23GiB`. Impact: the daily stack is restored with lower checkpoint RAM risk and a small reranker latency improvement, without adopting unsafe upstream co-load behavior.
[Qwen runtime benchmark helper 2026-06-16] outcome — `scripts/benchmark_qwen_runtime.py` was added as a repo-local scratch benchmark helper for llama-server presets. It parses preset env files safely, refuses dirty VRAM/process baselines, checks RAM/swap/VRAM gates, launches one scratch server at a time, runs fixed `/completion` probes, supports `--ignore-eos` and `--hold-seconds`, writes JSONL to `benchmarks/results/qwen-runtime-ab.jsonl`, and always attempts cleanup. `ruff check --fix`, `ruff check`, and `uv run python -m py_compile` pass. Impact: future runtime/build/preset comparisons can be repeated without hand-built launch commands and with fewer OOM footguns.
[Qwen 3.6 27B Huihui plus GPU reranker live load 2026-06-16] outcome — at user request, `localllm-main.service` and `localllm-reranker.service` were stopped to clear VRAM, the main service was loaded on `qwen-3.6-27b-mtp-huihui-q6-fast-128k`, and `config/localllm-reranker.env` was changed back to GPU mode (`RERANK_DEVICE=Vulkan0`, `RERANK_GPU_LAYERS=auto`, `RERANK_FIT=true`). Verification: live main args show the Huihui 27B MTP Q6 model with mmproj, `131072` context, q8 KV, `b2048/ub1024`, and draft-MTP `n=3`; live reranker args show `--device Vulkan0 --gpu-layers auto --fit on`; main, embedding, reranker, and TTS health checks pass; `/v1/rerank` returns a valid response; VRAM is about `32.69GB / 34.21GB` decimal; and both main/reranker have `NRestarts=0`. Impact: the current live stack prioritizes the proven 27B fast path with GPU-resident reranking while leaving embedding and TTS on CPU.
[Performance audit 2026-06-16] outcome — current live stack is `Qwen 3.6 27B Huihui MTP Q6` on Vulkan at `131072 / q8 KV / mmproj / b2048 / ub1024 / draft-MTP n=3` with the reranker fully GPU-resident and embedding/TTS on CPU. Upstream llama.cpp has advanced from local `b9496` to `b9664+`; relevant post-b9496 commits include MTP copy reduction (`e95dae18d`), checkpoint skip/restore improvements (`db94854ff`, `6f3a9f3de`), Vulkan transfer/barrier/op support (`fdc3db9b6`, `3e7bd4f39`, `d5fb10429`), and spec metrics (`635b65ad7`). Open issues also show fresh Qwen3.6/MTP/Vulkan long-context regressions, so a rebuild should be tested in a parallel build directory before replacing production. Driver stack is mostly current AMD 7.2.4, but `apt list --upgradable` still shows AMD graphics-package drift from `70201` to `07020400` for `amdgpu-*`, `libdrm-*`, and `libllvm20.1-amdgpu`; this is a consistency update more than a proven speed lever. Lucebox is conceptually useful for DFlash/KVFlash/PFlash/Spark experiments, but not a direct replacement for the current Huihui Q6 Vulkan/q8/mmproj daily service. Impact: safest next optimization work is isolated A/B: build latest llama.cpp alongside current, benchmark exact live 27B and retained 35B presets, then optionally test text-only/no-mmproj and Lucebox lanes separately.
[localllm networking surface cleanup 2026-06-16] outcome — after disabling the public 8095 Funnel route, the remaining stale localllm shared-service artifacts were removed from the repo: `config/localllm-share.env`, `systemd/localllm-share.service`, `scripts/serve-share.sh`, and `scripts/share_proxy.py`. `scripts/chat.sh` now discovers only `127.0.0.1:8091` by default, and `README.md` no longer advertises 8095/shared-chat or stale preset/service inventory. Verification: `rg` finds no live 8095/share references outside historical notes, shell syntax checks pass, `./scripts/status.sh` reports main/embedding/reranker/TTS healthy, and `tailscale serve status` shows only tailnet-only non-localllm routes on `7349` and `8443`. Impact: the old localllm public/shared networking path cannot be accidentally restarted from the repo or chat helper defaults.
[Tailscale Funnel disabled 2026-06-16] outcome — public Tailscale Funnel exposure was still enabled for `https://jhome.taila1332f.ts.net/` proxying to `http://localhost:8095`; it was disabled with `tailscale funnel --https=443 off`. Verification: `tailscale funnel status` now lists only tailnet-only routes on ports `7349` and `8443`, and `tailscale serve status --json` no longer has `AllowFunnel` or a `443` proxy to `8095`. Impact: the old shared 8095 path is no longer publicly exposed through Funnel.
[Lucebox relevance check 2026-06-15] outcome — `Luce-Org/lucebox-hub` is relevant research but not a drop-in replacement for the local Huihui Q6 Vulkan/q8/mmproj stack. The strongest ready path is Qwen 3.6 27B Q4_K_M with DFlash/DDTree/PFlash on CUDA or HIP; docs also include AMD HIP support and R9700/gfx1201 tuning data, but not the current Vulkan path or the exact Huihui Q6 MTP models used here. The most relevant ideas for future experiments are Spark for Qwen3.6 35B-A3B expert residency, KVFlash for bounded KV residency, and PFlash/FlowKV for long-context prefill compression; each changes behavior/quality assumptions enough to require isolated A/B testing before any adoption. Impact: keep Lucebox as a future isolated test candidate, especially for a fast 27B lane or a 35B Spark/KVFlash experiment, but do not switch the daily service to it without controlled validation.
[Kimi AGENTS loader investigation 2026-06-15] outcome — local Kimi Code `0.14.3` loads AGENTS context at agent bootstrap from `KIMI_CODE_HOME/AGENTS.md` or `~/.kimi-code/AGENTS.md`, then `~/.agents/{AGENTS.md,agents.md}`, then each directory from project root to cwd checking `.kimi-code/AGENTS.md` and `{AGENTS.md,agents.md}`. Logs and session metadata on 2026-06-14 through 2026-06-15 show this install was started with `workDir=/home/j` for normal shell sessions, so repo-local instructions under `/home/j/projects/localllm` would not have been injected unless Kimi was launched from that repo. Impact: to affect all sessions place instructions in `~/.kimi-code/AGENTS.md`; to affect only this repo launch Kimi from `/home/j/projects/localllm` and place instructions in that tree.
[Qwen 3.6 preset trim 2026-06-14] outcome — main presets were reduced to the four sane Qwen 3.6 choices: `27B Huihui MTP 128k`, `27B Unsloth MTP 128k`, `35B Huihui MTP 256k q8`, and `35B Unsloth fast 160k`. Deleted extra 35B shape presets: Huihui q8 offload, Unsloth full-256k q4-KV, Unsloth no-spec 212k, and older Unsloth 212k spec-default. Verification at trim time: `./scripts/list-presets.sh` reported exactly those four retained presets with `status=ok`, the then-live main remained `qwen-3.6-35b-a3b-huihui-mtp-q6-full-256k-q8`, and all stack health checks passed. Impact: preset surface now matches the maximum intended library: one normal and one abliterated/decensored preset each for 27B and 35B.
[Qwen 3.6 Hauhau 35B cleanup 2026-06-14] outcome — the Hauhau 35B model `Qwen3.6-35B-A3B-Uncensored-HauhauCS-Aggressive-Q6_K_P.gguf`, matching Hauhau mmproj, and `config/presets/main-qwen-3.6-35b-a3b-hauhau-q6.env` were deleted at user request. Verification: `find models config/presets -iname '*hauhau*' -o -iname '*Hauhau*'` returns no files; the only `Hauhau` hits left are historical notes. Runtime check found one main `llama-server`, one embedding server, one reranker server, CPU TTS, and no service restart loop (`NRestarts=0`). The brief nvtop blip is consistent with `localllm-main-idle-flush.timer`, which runs every five minutes and last exited successfully after logging `main activity changed; idle timer reset`. Impact: Hauhau is no longer an installable/servable 35B candidate on this host; current 35B choices should be Huihui MTP or Unsloth unless Hauhau is deliberately re-downloaded.
[Qwen 3.6 35B fit/speed comparison 2026-06-14] outcome — the retained 35B Qwen variants were compared from a clean unloaded stack in two scratch-only passes, all with q8 KV, mmproj, forced all-GPU loading, and no CPU tensor/KV/RS spill. Equalized common shape was `180224 / b2048 / ub1024`: Unsloth Q6 with `--spec-default` measured about `2481 pp/s`, `91.3 tok/s` exact-OK, `98.4 tok/s` direct, `1383 MiB` free; Hauhau Q6 with no spec measured about `2465 pp/s`, `94.4 tok/s` exact-OK, `106.1 tok/s` direct, `843 MiB` free; Huihui MTP Q6 with draft-MTP `n=2` measured about `2200 pp/s`, `102.3 tok/s` exact-OK, `102.4 tok/s` direct, `446 MiB` free. Best practical max-fit picks from this pass: Unsloth `163840 / b4096 / ub2048 / spec-default` at about `2540 pp/s`, `90.3` exact-OK, `98.5` direct, `1127 MiB` free; Hauhau `163840 / b4096 / ub2048 / no spec` at about `2504 pp/s`, `92.7` exact-OK, `106.3` direct, `586 MiB` free; Huihui MTP larger-context q8 pick `262144 / b1024 / ub512 / draft n=2` at about `1970 pp/s`, `118.2` exact-OK, `109.9` direct, `596 MiB` free, while the higher-PP/lower-context Huihui `196608 / b2048 / ub1024` measured `2069 pp/s`, `117.1` exact-OK, `108.5` direct, but left only `180 MiB` free and is too tight. Impact: for speed-only fit tests, Huihui MTP is the decode winner but not the PP winner; Hauhau is the strongest non-MTP decode result; Unsloth remains the best balanced PP/headroom speed shape.
[Qwen 3.6 35B daily-driver recommendation 2026-06-14] outcome — based on the 35B fit/speed pass, the practical remaining 35B daily candidates are Unsloth Q6 at `163840 / q8 KV / mmproj / b4096 / ub2048 / --spec-default` for the best PP/headroom balance, or Huihui MTP Q6 at `262144 / q8 KV / mmproj / b1024 / ub512 / draft n=2` when trusted abliteration/full context/decode feel matter more than PP. Hauhau Q6 measured well historically but was deleted on 2026-06-14 and is no longer a live candidate. Impact: future daily-driver decisions should choose between Unsloth stability/headroom and Huihui trust/full-context behavior unless Hauhau is intentionally re-downloaded.
[Qwen 3.6 35B daily plus full-GPU reranker check 2026-06-14] outcome — the recommended Unsloth daily candidate was checked with the reranker forced fully into VRAM while keeping q8 KV and mmproj. It technically co-loaded at several contexts, but none were safe enough to recommend: `131072 / b1024 / ub512` left about `133 MiB` free; `98304 / b1024 / ub512` left about `168 MiB`; `65536 / b1024 / ub512` left about `253 MiB`; and `65536 / b512 / ub256` left only about `10 MiB`. All runs kept main and reranker fully GPU-resident and avoided CPU spill, but the margin is too small for daily use. Impact: daily 35B should keep the main model fast and put reranker on CPU; if the reranker must be GPU-resident, accept a much smaller/non-mmproj/text-only shape or use a second GPU.
[Qwen 3.6 35B Huihui b4096 check 2026-06-14] outcome — Huihui MTP Q6 was checked with the larger `b4096 / ub2048` q8-KV mmproj shape at `180224`, `163840`, and `131072`, forced all-GPU with draft-MTP `n=2`. All three loaded cleanly with no CPU spill, but none beat the established Huihui max-fit picks: `180224` measured about `2096 pp/s`, `110.8 tok/s`, `375 MiB` free; `163840` measured about `2089 pp/s`, `112.9 tok/s`, `705 MiB` free; `131072` measured about `2089 pp/s`, `107.3 tok/s`, `510 MiB` free. Impact: do not use `b4096/ub2048` for Huihui MTP; the better daily/max-context shapes remain `262144 / b1024 / ub512` for context plus decode, or `196608 / b2048 / ub1024` only if accepting very tight VRAM.
[Qwen 3.6 35B Huihui all-GPU live load 2026-06-14] outcome — the managed main service was switched to a new preset `qwen-3.6-35b-a3b-huihui-mtp-q6-full-256k-q8` using `262144` context, q8 KV, mmproj, `b1024/ub512`, draft-MTP `n=2`, `MAIN_GPU_LAYERS=999`, `MAIN_FIT=false`, `MAIN_CACHE_PROMPT=true`, `MAIN_CACHE_RAM=0`, and `--ctx-checkpoints 12`. Reranker was moved back to strict CPU mode in `config/localllm-reranker.env` (`RERANK_DEVICE=none`, `RERANK_GPU_LAYERS=0`, `RERANK_FIT=false`), matching the existing CPU embedding and CPU OmniVoice TTS placement. Verification: main, embedding, reranker, and TTS health endpoints all returned ok; live process args show main on Vulkan and embedding/reranker on `--device none --gpu-layers 0`; VRAM was about `33.58GB / 34.21GB` decimal with all support services running; system RAM available was about `20GiB`; and startup logs showed no CPU tensor/KV/RS spill lines. Impact: the current live trial prioritizes Huihui trust, full context, q8 KV, and main-model speed while keeping all support services in CPU RAM.
[Qwen 3.6 35B Huihui MTP full-context q8 offload trial 2026-06-14] outcome — a managed preset `qwen-3.6-35b-a3b-huihui-mtp-q6-full-256k-q8-offload` was added and loaded for user testing. Shape: full `262144` context, q8 KV, mmproj, draft-MTP `n=2`, `b1024/ub512`, `--fit on`, `--fit-target 5120`, `--cache-ram 0`, and `--ctx-checkpoints 12`. Scratch comparison showed `--fit-target 4096` was faster on PP but tighter after the full-GPU reranker (`~673 pp/s`, `~53 tok/s`, about `1.39 GiB` free), `5120` was the best balance (`~599 pp/s`, `~53 tok/s`, about `2.43 GiB` free), and `6144` was safer but too slow (`~519 pp/s`, `~44 tok/s`). The live managed stack now has main, GPU reranker, and CPU embedding healthy, with TTS intentionally inactive for this trial. Live validation measured about `578 pp/s` on a 19.5k-token prompt, about `43 tok/s` on the capped exact-OK style probe, working rerank endpoint, and about `31.69GB / 34.21GB` decimal VRAM after CPU embedding was started. Impact: this is the practical “full context, q8 KV, reranker in VRAM” Huihui 35B MTP trial shape, but it is a CPU-offload compromise and should be judged in the real agent before replacing faster all-GPU profiles.
[Qwen 3.6 35B Huihui MTP q8-KV plus GPU reranker fit 2026-06-14] outcome — the safe combined fit boundary is much lower than the main-only q8-KV fit because the Qwen3 reranker consumes about `3.68GB` decimal VRAM by itself when forced fully onto GPU (`37/37` layers plus KV on Vulkan). With the Huihui 35B MTP main model's mmproj loaded, no q8-KV combined shape had comfortable headroom: `65536` context with reduced main/reranker buffers left only about `38 MiB` free, and `32768` left only about `381 MiB`, despite both services technically loading with full GPU placement. Text-only main is the practical path if the reranker must also be GPU-resident: `65536 / q8_0-q8_0 / b256 / ub128 / draft-MTP n=2 / no-mmproj` plus reranker `2048 / b128 / ub128 / forced all-GPU` loaded with `31804 MiB used / 819 MiB free`, about `1043 pp/s` on a 38.9k-token prompt, about `126 tok/s` on the simple decode probe, and a working rerank endpoint. The conservative fallback is the same text-only shape at `32768`, which left about `1238 MiB` free. Verification: scratch main and reranker were stopped after testing, all localllm services were inactive, no `llama-server` remained, and VRAM returned to idle baseline. Impact: for q8 KV plus GPU reranking on this 32GB card, use text-only `64k` as the largest practical shape or `32k` for safer headroom; do not expect the mmproj-enabled stack to be safe with the reranker fully in VRAM.
[Qwen 3.6 35B Huihui MTP Q6 fit/speed pass 2026-06-14] outcome — the downloaded `Huihui-Qwen3.6-35B-A3B-abliterated-MTP-Q6_K.gguf` plus `Huihui-Qwen3.6-35B-A3B-abliterated-MTP-mmproj-f16.gguf` were tested from a clean unloaded stack on the pinned Vulkan `llama-server version 1086 (94a220cd6)`. `--fit on` at full `262144` context did load but chose CPU tensor overrides and was slower; the best offload-path read was `q4_0/q4_0`, `b2048/ub1024`, draft-MTP `n=2`, about `1582 pp/s` on a 38.9k-token prompt and about `83 tok/s` decode. The better result was forcing all layers/KV/RS onto GPU with `--gpu-layers 999 --fit off`: full `262144` q8 KV fit cleanly at about `1689 pp/s`, `117 tok/s` exact-OK, and `114 tok/s` direct probe, while full `262144` q4 KV was slightly faster. Fastest all-GPU shape found was `q4_0/q4_0`, `b2048/ub1024`, draft-MTP `n=2`, about `1691 pp/s`, `118.6 tok/s` exact-OK, and `119.9 tok/s` direct probe, but with only about `396 MiB` free VRAM; the safer all-GPU speed shape is `q4_0/q4_0`, `b1024/ub512`, draft-MTP `n=2`, about `1667 pp/s`, `117.3 tok/s` exact-OK, and `120.5 tok/s` direct probe with about `1020 MiB` free. Draft `n=3` was rejected for this pass because it produced hidden-reasoning-only exact-OK responses even when speed was competitive. Verification: final service check showed main, embedding, reranker, and TTS inactive, no `llama-server` process, and VRAM back to baseline. Impact: for this Huihui 35B MTP model, the best full-context local path is not RAM offload; force all-GPU and use q4 KV for max speed, or q8 KV if KV precision matters more than the small speed gain.
[X/Twitter terminal series review 2026-06-13] outcome — reviewed the numbered `social/x` thread packages after the terminal-style visual reset. Checks passed: all markdown image references resolve to existing PNGs; all PNGs render at `1600x900`; `social/x` contains only numbered active thread files; `scripts/social` contains only `render_terminal_x_series.py`; rerendering from that script regenerates the full active image set; contact-sheet plus full-size review of dense charts found no current overlap/clipping; and `ruff check --fix` plus `ruff check` pass. Remaining caveats are intentional: thread 4's radar is qualitative/opinion-coded, and grouped metric charts use row-specific scaling for readability with numeric labels as the authoritative values. Impact: the current series is clean enough for manual editorial review/posting.
[X/Twitter terminal visual reset 2026-06-13] outcome — all `social/x` images were redone as chart-first terminal-style visuals with near-black backgrounds, monospace typography, gridlines, and saturated orange shades. Text-only/card-heavy images were replaced with charts or matrices: thread 2 image 1 is now a context-vs-decode scatter plot; thread 4 is a radar chart; thread 9 is a residency matrix; grouped bar charts now use row-specific scaling so mixed units such as PP/TG/context/VRAM remain visually meaningful. Old style-specific render scripts were removed, leaving `scripts/social/render_terminal_x_series.py` as the single source of truth for all numbered PNGs. Verification: regenerated all images, contact-sheet inspection caught and fixed mixed-scale readability, all files in `social/x` remain numbered, and `ruff check --fix` plus `ruff check` pass for the terminal renderer. Impact: the series now matches the requested terminal/orange visual direction and avoids images that merely duplicate tweet text.
[X/Twitter numbered thread package inventory 2026-06-13] outcome — `social/x` was cleaned into numbered active thread packages only, using terminal-style dark/orange graphics and removing the superseded 27B-only recipe draft. Active packages: `1-fits-in-vram.md/png`; `2-27b-vs-35b.md` plus `2-27b-vs-35b.png` and `2-27b-vs-35b-metrics.png`; `3-huihui-vs-unsloth-mtp.md/png`; `4-dense-hype-sparse-reality.md/png`; `5-llamacpp-upgrade-check.md/png`; `6-context-cliff-heatmap.md/png`; `7-vram-stack-budget.md/png`; `8-amd-vulkan-backend.md/png`; and `9-local-agent-stack-map.md/png`. Excluded by request: Qwen-vs-Nex because it was already posted, and a TTS/Higgs thread because Higgs Audio was researched but not locally tested. Verification: `scripts/social/render_terminal_x_series.py` renders all active PNGs without recreating stale filenames, contact-sheet visual inspection passed after fixing overlap/clipping/readability issues, and `ruff check --fix` plus `ruff check` pass. Impact: the folder is visually organized for posting and the remaining thread set is ready for review/use.
[X/Twitter Qwen 27B vs 35B best-shape post 2026-06-13] outcome — the 27B-only recipe card was superseded because it overlapped too much with the first context-speed thread. Active thread 2 is now `social/x/2-27b-vs-35b.md` with two terminal-style images generated by `scripts/social/render_terminal_x_series.py`: `social/x/2-27b-vs-35b.png` is a context-vs-decode scatter plot, and `social/x/2-27b-vs-35b-metrics.png` is a grouped metrics chart with row-specific scaling. The comparison frames the useful surprise: the best 27B MTP local shape was `128k / q8 KV / b2048 / ub1024 / draft-MTP n=3` at about `637 pp/s` and `45-50 tok/s`, while the faster 35B A3B Unsloth speed shape was `160k / q8 KV / b4096 / ub2048 / spec-default` at about `2570 pp/s` and `~89 tok/s` long decode. Fresh VRAM samples with support services still active measured `29.56 GB` used for the restored 27B stack and `33.03 GB` used for the 35B fast-160k stack via `rocm-smi --showmeminfo vram --json`. Verification: controlled 35B preset swap succeeded, original 27B preset was restored and stack health passed, both `1600x900` PNGs rendered, visual inspection caught/fixed chart overlap/readability issues, and `ruff check --fix` plus `ruff check` pass. Impact: thread 2 now adds a distinct parameter-count/sparse-vs-dense lesson with a second metrics chart instead of restating thread 1.
[X/Twitter local AI post plan 2026-06-13] outcome — package local tuning findings as short 2-3 post threads with one strong scrubbed visual each. Final scoped series excludes Qwen-vs-Nex because it was already posted and excludes TTS/Higgs because Higgs Audio was researched but not locally tested. Active topics are `fits in VRAM is not enough`, `27B dense vs 35B A3B`, `Huihui vs Unsloth MTP`, `dense hype vs sparse reality`, `llama.cpp upgrade reality check`, `context cliff heatmap`, `VRAM stack budget`, `AMD/Vulkan backend lesson`, and `local agent stack map`. First completed asset is now numbered as `social/x/1-fits-in-vram.md` plus `social/x/1-fits-in-vram.png`, generated by `scripts/social/render_terminal_x_series.py`; the chart is scrubbed and frames the result as local-stack evidence rather than a universal benchmark. Verification: chart rendered at `1600x900`; visual inspection passed; `ruff check --fix` and `ruff check` pass for the generator. Impact: the post series is organized around tested local-stack evidence only.
[Higgs Audio v3 TTS assessment 2026-06-12] outcome — `bosonai/higgs-audio-v3-tts-4b` is a materially more capable TTS candidate than OmniVoice for the user's Thai-learning use case because Thai is listed in the polished WER/CER-under-5 language tier, it supports no-reference multilingual speech, optional zero-shot voice cloning, streaming, and inline emotion/prosody/style controls. The blocker is stack fit, not model appeal: the official/self-host path is SGLang-Omni with H100/A100-class NVIDIA as the known-good baseline, the model card/repo guidance says full BF16 weights are about 9.3GB on disk / ~10GB weights and roughly 16GB practical VRAM minimum with 24GB comfortable for cloning, and the current R9700 is already around 29.6GB VRAM used with the active Qwen main model. Impact: Higgs should be tested only as an isolated GPU TTS experiment after unloading the main LLM, or via hosted API for convenience; do not replace the always-on CPU OmniVoice sidecar until an AMD/ROCm local path is proven and VRAM coexistence is resolved.
[Qwen 3.6 retained inventory cleanup 2026-06-12] outcome — the Qwen 3.6 model directory was initially reduced to four retained model families plus matching mmproj files: `27B normal` = `Qwen3.6-27B-MTP-Q6_K-unsloth.gguf`; `27B decensored` = active `Qwen3.6-27B-abliterated-MTP-Q6_K-Huihui.gguf`; `35B normal` = `Qwen3.6-35B-A3B-UD-Q6_K-unsloth.gguf`; and then-current `35B decensored` = `Qwen3.6-35B-A3B-Uncensored-HauhauCS-Aggressive-Q6_K_P.gguf`. Removed unpicked 27B files: non-MTP Unsloth Q6 plus mmproj and HauhauCS Balanced uncensored Q6 plus mmproj, freeing about `50.6GB` decimal (`47.2GiB`). Stale presets `main-qwen-3.6-27b-hauhau-q6.env` and `main-qwen-3.6-27b-unsloth-q6.env` were removed; a retained Unsloth MTP preset `main-qwen-3.6-27b-mtp-unsloth-q6-fast-128k.env` was added. This inventory was superseded on 2026-06-14 when Hauhau 35B was deleted and Huihui 35B MTP became the trusted decensored 35B path. Impact: do not use this older four-family note as current retained inventory; current 35B serving choices exclude Hauhau.
[Qwen 3.6 27B MTP Unsloth Q6 speed comparison 2026-06-12] outcome — the newly downloaded `models/qwen-3.6/Qwen3.6-27B-MTP-Q6_K-unsloth.gguf` plus `Qwen3.6-27B-MTP-mmproj-F16-unsloth.gguf` were tested from a clean unloaded stack using mmap-enabled one-server-at-a-time scratch launches. The model fit with mmproj and q8 KV through full `262144`, but q8 speed collapsed at large context: `212992 / b1024 / ub512 / n2` measured about `612 pp/s` and `29-30 tg/s`, while `262144` dropped to about `458 pp/s` and `11-15 tg/s`. At the practical `131072` q8 profile, Unsloth was competitive but slower overall than Huihui: `b2048 / ub1024 / n2` measured about `638 pp/s`, `43.45 tg/s` short, and `40.5 tg/s` long decode; `b512 / ub256 / n2` measured better long decode at `45.85 tg/s` but only `578 pp/s`; higher draft settings were mixed and `n5` again looked unreliable with bad short decode despite one high long-decode result. Full-context q4 KV worked at `262144 / b2048 / ub1024 / n3`, measuring about `633 pp/s`, `44.43 tg/s` short, and `43.77 tg/s` long decode, but it still trailed the Huihui 128k q8 live sanity read (`~637 pp/s`, `45.22 tg/s` short, `49.79 tg/s` long decode) and carries the q4-KV quality tradeoff. Impact: keep `qwen-3.6-27b-mtp-huihui-q6-fast-128k` loaded as the faster practical 27B MTP profile; do not add/promote an Unsloth preset unless behavior quality is unexpectedly better than Huihui.
[Qwen 3.6 27B MTP Huihui Q6 live tuning 2026-06-12] outcome — the downloaded `models/qwen-3.6/Qwen3.6-27B-abliterated-MTP-Q6_K-Huihui.gguf` was initially unusable because it was truncated (`22357767485` bytes local vs `22430998848` remote), and `llama.cpp` failed with `tensor 'blk.64.ffn_up.weight' data is not within the file bounds`. The missing tail was resumed from `huihui-ai/Huihui-Qwen3.6-27B-abliterated-MTP-GGUF`, after which the model loaded cleanly. All model services were stopped before scratch tuning to avoid contaminated VRAM and host-RAM pressure; probes used mmap-enabled launches rather than `--no-mmap`. Fit result: q8 KV with draft-MTP loads up to full `262144`, but full q8 is too VRAM-tight and slowed badly (`~22-25 tg/s` at `262144 / b1024 / ub512`, worse at `b2048 / ub1024`). Speed result: the best practical q8 profile is `131072 / np1 / q8_0-q8_0 / b=2048 / ub=1024 / draft-mtp n=3 / mmproj`, with live managed sanity check at `~45.22 tg/s` short, `~637.13 pp/s` on a 19.8k prompt, and `~49.79 tg/s` on the long-prompt decode. Full-context alternative: `262144 / q4_0-q4_0 / b=2048 / ub=1024 / n=3` recovered speed to about `625 pp/s` and `41-42 tg/s`, using about `29.97GB` VRAM, but carries the q4-KV quality tradeoff. A retained preset was added at `config/presets/main-qwen-3.6-27b-mtp-huihui-q6-fast-128k.env`, and the managed stack is now loaded on that preset with embedding, reranker, TTS, and the idle-flush timer healthy. Impact: the user can trial the faster 27B MTP Huihui agent path now; if 128k context is insufficient, test a separate q4-KV full-context preset rather than using full q8.
[Qwen 3.6 27B MTP/speed revisit 2026-06-11] outcome — current upstream/model review found one credible 27B speed path but no reason to promote it blindly. Official Qwen/Unsloth materials list Qwen 3.6 27B as MTP-trained and Unsloth's MTP GGUF card claims about `1.5-2x` faster inference; however, the same card still says `-np > 1` and `--mmproj` are not supported with MTP, so a clean 27B MTP trial should be single-slot and text-only unless the local runtime is deliberately tested beyond the supported path. Local numbers already show the core tradeoff: 27B non-MTP at `131072 / np1 / q8_0-q8_0 / b=1024 / ub=512` scored about `21.5 tg`, while 27B MTP with `n=2` scored about `43.3 tg` on the two-paragraph probe and `46.9 tg` on the long prompt, with lower PP than non-MTP. The current llama.cpp origin also contains an MTP-specific optimization after the pinned build (`Remove padding and multiple D2D copies for MTP`, `e95dae18d`), making a scratch MTP-only rebuild/test plausible even though the prior general newer-build test was rejected. Impact: if the user wants a faster 27B feel test, re-download only the 27B MTP non-XL GGUF plus mmproj if needed for inventory, test `--spec-draft-n-max 2` first, prefer no-mmproj supported mode, and compare behavior before keeping it on disk.
[Qwen/Gemma public-opinion research 2026-06-11] outcome — current qualitative public signal favors `Qwen 3.6 27B` for local agent/coding enthusiasm because it is dense, predictable, and widely described as a strong local coding model, but this host's actual 27B Hauhau/Unsloth trials were much slower in the real agent path than the 35B profiles. `Qwen 3.6 35B A3B` has less broad public hype because sparse active-parameter models are harder to trust heuristically and harder to compare by parameter count, but the local 35B Hauhau/Unsloth profiles remain better aligned with this box's throughput and context goals. `Gemma 4 31B` has credible qualitative interest for codebase understanding but the remaining local UD-Q4 build previously decoded only in the mid-20 tok/s range. `Gemma 4 26B A4B` has the strongest local speed signal among the Gemma candidates and is worth behavior testing if speed is the priority, but it needs agent/coding validation before replacing Qwen. Impact: do not let online praise for 27B dense override the host-specific real-agent result; use public opinion only to choose the next A/B candidate, not to pick the default.
[35B Hauhau live trial load] outcome — the managed main service was switched from `qwen-3.6-27b-hauhau-q6` to the retained `qwen-3.6-35b-a3b-hauhau-q6` preset for user agent testing. Live shape is `180224 / np1 / q8_0-q8_0 / b=2048 / ub=1024 / mmproj`, Vulkan, prompt cache enabled, and no `--spec-default` in the preset. Verification: `./scripts/current-main.sh` reports alias `qwen-3.6-35b-a3b-hauhau-q6`; all stack services are healthy; the running command points at `Qwen3.6-35B-A3B-Uncensored-HauhauCS-Aggressive-Q6_K_P.gguf`; and filtered startup logs show no CPU spill/tensor-override warning lines. Impact: the user can compare 35B Hauhau against the prior 35B Unsloth default in the real agent path, with Unsloth still retained as `qwen-3.6-35b-a3b-unsloth-q6`.
[27B Hauhau live trial load] outcome — a retained preset `config/presets/main-qwen-3.6-27b-hauhau-q6.env` was added for `Qwen3.6-27B-Uncensored-HauhauCS-Balanced-Q6_K_P.gguf` plus matching F16 mmproj at the documented clean ceiling: `180224 / np1 / q8_0-q8_0 / b=1024 / ub=512 / --spec-default`. The managed main service was switched from the 35B Unsloth default to this preset for user agent testing. Verification: `./scripts/list-presets.sh` reports the new preset `status=ok`; `./scripts/current-main.sh` reports alias `qwen-3.6-27b-hauhau-q6` with `n_ctx=180224`; all stack services are healthy; and filtered startup logs show no CPU spill/tensor-override warning lines. Impact: the user can now feel out 27B Hauhau in the real agent path, with the previous 35B Unsloth default still retained as `qwen-3.6-35b-a3b-unsloth-q6`.
[Local LLM artifact cleanup 2026-06-11] outcome — stale generated artifacts were removed without changing the active stack. Removed: old `llama.cpp` build dirs `build` and `build-hip`; broken legacy install `/home/j/.local/share/localllm`; stale `/tmp/localllm-*` probe files; repo Python/ruff/pytest caches; generated benchmark result directories and old repo logs; dead `.agent-browser/openwendy-*` pid/socket files; old OpenWendy local-LLM benchmark/service logs and caches; empty obsolete Hugging Face Qwen/Nemotron metadata stubs; and old NVIDIA shader cache. The `~/.local/bin/llama-server` shim now points at the active Vulkan build instead of the removed broken legacy install. Verification: `./scripts/status.sh` and `./scripts/list-presets.sh` pass, the shim reports `llama-server version 1086 (94a220cd6)`, live services still run from `/home/j/.local/src/llama.cpp/build-vulkan-r9700`, and the remaining large Hugging Face cache is active TTS/Whisper material. Impact: roughly 1 GB of stale local-LLM clutter was removed while preserving active models, presets, systemd services, and the HIP comparison build.
[Qwen 3.6 35B stack audit 2026-06-11] outcome — the live stack was audited against current local state and upstream runtime changes. The active daily driver remains `qwen-3.6-35b-a3b-unsloth-q6` on Vulkan build `b1086-94a220cd6` at `212992 / np1 / q8_0-q8_0 / b=2048 / ub=1024 / --spec-default / mmproj`; all support services are healthy and CPU-backed. The newer upstream Vulkan build `b1182-ac4cddeb0` was built separately for A/B testing, but probes did not show a prompt-processing gain (`~1256-1270 tok/s` vs old `~1259-1279 tok/s` on the same long prompt) and old b1086 had better warmed speculative decode on the repeated 256-token prompt (`118 tok/s` old vs `86 tok/s` new), so the live service and source checkout were restored to b1086. The rejected b1182 test build directory was later removed to avoid disk clutter. Impact: do not promote b1182 to default unless future upstream/RADV changes justify a fresh build-and-test pass.
[Qwen 3.6 35B full-context q4 KV preset] outcome — a targeted fit probe found that the Unsloth Q6 model plus mmproj can load at full `262144` context with `q4_0/q4_0` KV, `np1`, `b=2048`, `ub=1024`, `--spec-default`, and no observed CPU spill warnings; quick benchmark output showed about `95 tok/s` decode with roughly `1652 MiB` free VRAM. The more aggressive full-context `q4_0/q4_0 / b=4096 / ub=2048` variant also fit but was slower in the quick decode probe at about `58 tok/s`, so it was rejected. A retained experimental preset was added at `config/presets/main-qwen-3.6-35b-a3b-unsloth-q6-full-256k-q4kv.env`. Impact: use the default q8-KV profile for quality-conservative daily work; use the new q4-KV preset only when full 256k context matters more than KV precision risk.
[R9700 power/perf tuning check] outcome — during a 1024-token decode on the current daily profile, the R9700 stayed unthrottled with observed socket power roughly `75-112 W` and VRAM temperature in the mid-60s C, so the current 210 W PPT0 cap is not the decode bottleneck. Attempting a no-prompt sudo perf-level change failed because sudo requires a password. Impact: driver/power-cap changes are not an immediate speed lever from the available evidence; clock/perf-level experiments require an interactive privileged action before they can be tested.
[Context clearing preference] outcome — after long agent/chat sessions, the user's observed low-fan recovery path is clearing the main llama.cpp active slot state, not disabling prompt cache or restarting the model. Future work should preserve the idle slot-flush timer unless explicitly asked to remove it, and should prefer slot-state clearing after idle periods over global prompt-cache disablement. Impact: context clearing remains part of the intended local stack behavior across future chat context compaction.
[Main idle slot flush] outcome — an optional companion timer checks the main llama.cpp server every five minutes and clears long idle slot state after `15m` with a conservative `8192` token threshold. The script first tries native `POST /slots/0?action=erase`, but the current multimodal Qwen server still returns `501 not_supported`, so the verified live path falls back to a forced slot-0 one-token `/completion` request with `cache_prompt=false`, matching the user's manual `boop` behavior without touching app chat history. The script is now hardened with short HTTP timeouts and exits successfully without flushing if the main server is unavailable. Verification: forced low-threshold runs reduced the active slot to `n_prompt_tokens=2`; unavailable-server simulation exits `0`; normal no-op check measured about `0.05s` elapsed and `10.5 MB` max RSS; `localllm-main-idle-flush.timer` is enabled/active, user linger is `yes`, and the journal shows clean timer runs; `systemd-analyze --user verify`, `bash -n`, `ruff check`, `pytest -q`, and `./scripts/status.sh` pass. Impact: prompt cache stays enabled during active work, while long-session GPU memory heat can be cleared automatically after idle time with negligible steady overhead and reboot-persistent scheduling.
[TTS thread tuning] outcome — OmniVoice TTS now exposes `TTS_TORCH_THREADS` and `TTS_TORCH_INTEROP_THREADS`, reports the active PyTorch thread settings on `/health`, and the persistent user unit sets `TTS_TORCH_THREADS=6` / `TTS_TORCH_INTEROP_THREADS=2`. Benchmark on the same fixed 4-second French prompt: `4/2` took `111.023s` and `109.038s`; `6/2` took `78.265s` and `77.881s`. Verification: after clearing temporary systemd manager overrides and restarting from the unit file, `/health` reports `torch_threads=6` and `torch_interop_threads=2`; all four stack services are healthy. Impact: TTS remains always available for language learning with a lower thread setting than the default `12/12`, while avoiding the large latency penalty seen at `4/2`.
[Main preset A/B quick probe] outcome — the two experimental 35B Unsloth presets were tested through the managed service path with bounded timing probes, and the settled default was restored afterward. `no-spec` at `212992` was not attractive: `ok` decode `64.95 tok/s`, `direct` `78.46 tok/s`, `long_ack` PP `2370.50 tok/s` / decode `78.28 tok/s`. `fast-160k` loaded cleanly at `163840` and was faster: `ok` decode `97.61 tok/s`, `direct` `99.45 tok/s`, `long_ack` PP `2569.71 tok/s` / decode `88.87 tok/s`. The restored default at `212992` measured `ok` decode `69.22 tok/s`, `direct` `66.82 tok/s`, `long_ack` PP `2310.73 tok/s` / decode `73.39 tok/s`. Impact: keep the default loaded for full-context daily use; use `fast-160k` when speed matters and `160k` context is enough; do not prefer `no-spec` based on this quick read.
[Local stack optimization pass] outcome — helper `llama-server` services now use explicit `--threads-http 2`; embedding now uses `-b 128` to match `EMBED_UBATCH=128` instead of relying on llama.cpp to clamp `-b 256`; retained experimental main presets now include `qwen-3.6-35b-a3b-unsloth-q6-fast-160k` (`163840 / b=4096 / ub=2048 / --spec-default`) and `qwen-3.6-35b-a3b-unsloth-q6-no-spec` (`212992 / b=2048 / ub=1024 / no --spec-default`); and the HIP runtime selector now points at `build-hip-r9700-current`. Verification: helper services were restarted and all four live services are healthy; `list-presets` reports the new presets as `status=ok`; `bash -n`, `ruff check --fix`, `ruff check`, and `pytest -q` pass. Impact: the low-risk service cleanups are active, and main-model speed/spec experiments are available without changing the settled default.
[Current default stack] outcome — as of 2026-06-10 after the 27B trial revert, the live daily-driver stack is back on `qwen-3.6-35b-a3b-unsloth-q6` at `212992 / np1 / q8_0-q8_0 / b=2048 / ub=1024 / --spec-default / mmproj` on Vulkan, with reasoning on and prompt cache enabled. Verification: `8091` is healthy and `/v1/models` reports alias `qwen-3.6-35b-a3b-unsloth-q6` with `n_ctx=212992`; embedding `8092`, reranker `8093`, and OmniVoice `8094` are also healthy. Impact: this remains the settled default operating point for the box.
[27B Unsloth real-use verdict] outcome — on 2026-06-10, the user tested `qwen-3.6-27b-unsloth-q6` on the conservative safe shape `196608 / np1 / q8_0-q8_0 / b=1024 / ub=512 / --spec-default` and saw about `pp730 / tg16` in the real agent harness, versus about `pp2.4k / tg86` after returning to the 35B default. Impact: the 27B model remains usable for narrower focused tasks, but it is not competitive enough to replace the settled 35B default for the user’s mixed assistant-agent workload.
[27B documented safe fit ceilings] outcome — the current documented safe-fit guidance for the retained 27B Qwen 3.6 models is: `27B Unsloth` at `196608 / np1 / q8_0-q8_0` as the highest explicitly documented clean fit, and `27B Hauhau` at `180224 / np1 / q8_0-q8_0` as the highest documented clean fit from the normalized four-model sweep. Impact: future 27B reloads should start from those ceilings rather than improvising larger contexts.
[Second R9700 verdict for this box] outcome — on 2026-06-10, the second-GPU question was re-evaluated against the actual board topology and current service mix. The host’s second full-length slot is still the X570 chipset `PCIe 4.0 x4` slot, so a second `R9700` would be a headroom/concurrency purchase rather than a clean performance upgrade. With the current stack, embedding, reranker, and OmniVoice all run acceptably on CPU, so buying a second 32 GB GPU just to move those support services off host RAM is poor value. Impact: a second `R9700` is not recommended for the present stack unless the user later decides to keep another substantial GPU-side model or image-generation workload resident all the time.
[27B Unsloth recovery load after OOM] outcome — on 2026-06-10, fresh scratch tuning attempts for `Qwen3.6-27B-UD-Q6_K_XL-unsloth.gguf` destabilized the host badly enough to trigger system lockups/OOM, so the risky sweep was abandoned and the main service was restored on the previously proven safe shape instead of continuing to probe live-style variants. A new retained preset `config/presets/main-qwen-3.6-27b-unsloth-q6.env` was added and loaded with `196608 / np1 / q8_0-q8_0 / b=1024 / ub=512 / --spec-default / mmproj`. Verification: `8091` is healthy and `/v1/models` reports alias `qwen-3.6-27b-unsloth-q6` with `n_ctx=196608`; embedding, reranker, and TTS also remained healthy. Impact: the user can safely trial the 27B model now, and future 27B retuning should start from this conservative known-good shape rather than another aggressive scratch sweep on the live multimodal path.
[OOM avoidance for scratch model tuning] outcome — repeated host instability on 2026-06-10 showed that aggressive scratch tuning of large multimodal models with live-style `llama-server` settings is not safe on this box when the fit is uncertain. Impact: when a user asks to “try a model for a while,” prefer loading the strongest previously validated safe shape first; only retune later, one variable at a time, and avoid repeating broad live-style sweeps after an OOM incident unless the user explicitly wants the risk.
[35B MTP Q5 candidate evaluation] outcome — on 2026-06-09, `Qwen3.6-35B-A3B-MTP-UD-Q5_K_XL` plus `Qwen3.6-35B-A3B-MTP-mmproj-BF16` were downloaded into `models/qwen-3.6`, the managed stack was fully stopped, and the candidate was evaluated as a real default-replacement candidate against the current `Qwen 3.6 35B A3B Unsloth Q6` live default. This is not a pure quant-only result because the candidate also changes the runtime path to MTP. Fit result: the candidate came up clean on the real multimodal server path at `262144 / np1 / q8_0-q8_0 / b=2048 / ub=1024 / draft-mtp`, with no CPU tensor-override, CPU KV, or CPU RS spill lines in the fit log; `n=2` was the best draft count from the local probe sweep, beating `n=3` and `n=5` on decode while avoiding the sharp acceptance collapse seen at `n=5`. Speed probes on intended daily-use shapes showed the familiar MTP trade: candidate `256k` scored `88.34 pp / 94.17 tg` on the two-paragraph probe and `268.28 pp / 84.42 tg` on the longer direct-answer probe, while the current Q6 default at `212992` scored `170.76 pp / 69.25 tg` and `294.46 pp / 67.54 tg` respectively — much faster decode for the candidate, but materially worse PP. Reliability/capability comparison was mixed. `transcript_replay` tied on passed fixtures (`2/5` each), but the candidate timed out inside `soak_real` turn 2 after `300s`, so its replay summary collapsed to `14/18` matched turns versus Q6 `25/35`; impact: the candidate is much less trustworthy on long evolving replay flows. `sim_compare` favored the candidate strongly on discipline: both landed `7/8` pass, but Q6 was only `5/8` scope-clean and `6/8` tool-error-free, while the candidate landed `7/8` scope-clean and `8/8` tool-error-free. `opencode_compare` also favored the candidate: Q6 remained the old `1/3` pass-like shape because `revise_after_feedback` turn 2 timed out and `tool_followthrough` still drifted `read_file -> apply_patch`, while the candidate improved to `2/3` pass-like by completing both `repo_triage` and `revise_after_feedback`, though it still failed `tool_followthrough` the same way. `coding_compare` was a pleasant surprise for the candidate: unlike the earlier Nex run, it completed all four uncapped prompts cleanly and was much faster (`83.40 tg` average vs Q6 `65.73 tg`), with lower reasoning lengths on the smaller prompts and no hard runaway. The decisive negative was `agentic_barrage`: Q6 completed the full six-scenario uncapped barrage without tripping the external wall-clock guard, even though it still failed `tool_followthrough` by answering `2 + 2 = 4`; the candidate handled `plan_then_revise`, `review_then_retry`, `evidence_triage`, and `tool_restraint` acceptably and still failed `tool_followthrough` in the same trivial way, but `codex_workflow` turn 2 hit the `300s` guard and killed the first barrage pass. Impact: the candidate is a serious speed-and-discipline challenger with a real `256k` all-GPU shape, but it is still not a safe default replacement because the long-agent-flow stability regression in replay/barrage is too large for the user’s mixed assistant workload. The candidate artifacts were removed from disk after the decision, leaving the default Unsloth Q6 and Hauhau Q6 35B files as the only retained 35B main-model options.
[Nex N2 Q5 vs Q6 clean-shape blocker] outcome — on 2026-06-09, a fair equalized Q5-vs-Q6 comparison for Nex N2 mini was started with the intent to use the smaller clean shared context. The first assumption from the earlier Q6 fit pass was wrong under the actual comparison server shape: with `mmproj` loaded, `np1`, reasoning on, and real `llama-server` settings, Q6 still logged `tensor overrides to CPU` at every tested context using `q8_0/q8_0` KV, both at `4096/2048` and the safer `2048/1024` shape. A fallback check with lighter `q4_0/q4_0` KV also failed to produce a clean server shape: `262144` did not come up healthy, while `245760`, `229376`, and `212992` all served but still spilled. Impact: a truly clean apples-to-apples Q5-vs-Q6 chart with `mmproj` loaded is not possible on this single-card box under the requested fairness rule; any chart would need a relaxed rule such as “same usable shape” or “text-only/no-mmproj.”
[Nex-N2-mini Q6 fit check] outcome — on 2026-06-09, `Nex-N2-mini-UD-Q6_K_XL.gguf` was downloaded into `models/nex-n2-mini` and checked on a clean unloaded GPU with the same aggressive tuning used for the Q5 pass: `np1 / q8_0-q8_0 / b=4096 / ub=2048` plus `mmproj-BF16`. At the full trained context `262144`, the scratch server did become healthy, so the model is usable at `256k`, but the load log emitted `tensor overrides to CPU are used with mmap enabled`, which means the full-context shape is not a clean all-GPU fit. Stepping down to `245760` was the first tested context that both served successfully and avoided the CPU tensor-override warning. Impact: the highest clean tested context for Nex N2 mini Q6 on this host at the fixed aggressive tuning is `245760`, while `262144` is usable but spills.
[Nex-N2-mini Q5 tuned live load] outcome — on 2026-06-09, `Nex-N2-mini-UD-Q5_K_XL.gguf` and `mmproj-BF16.gguf` were downloaded into `models/nex-n2-mini`, then the live stack was unloaded and the model was fit-tested and tuned on Vulkan. The model is supported by the current `llama.cpp` build as a multimodal Qwen-VL-family checkpoint and reaches its full trained context of `262144` at `np1` with `q8_0/q8_0` KV. A real full-context scratch server launch with `mmproj` confirmed that even the aggressive `b=4096 / ub=2048` shape boots cleanly enough to serve at `256k`. Safe `llama-bench` sweep results at `q8` KV were: `1024/512` = `2386.65 pp / 108.46 tg`, `2048/1024` = `2899.30 pp / 108.55 tg`, `4096/2048` = `3029.51 pp / 108.16 tg`, `4096/4096` = `2536.19 pp / 108.60 tg`. Impact: the best practical Nex tuning on this host is `262144 / np1 / q8_0-q8_0 / b=4096 / ub=2048`, and the live main service is now loaded on `8091` as alias `nex-n2-mini-q5` with embedding/reranker/TTS restored.
[35B Unsloth Q8 trial] outcome — on 2026-06-09, the temporary `Qwen3.6-35B-A3B-UD-Q8_K_XL-unsloth.gguf` trial was reverted after real-agent testing showed the Q8 profile was materially less comfortable than the default Q6 profile (`~1.4k pp / 36 tg` observed by the user versus about `2.1k pp / 83 tg` on the settled Q6 setup). The live main service was restored to `qwen-3.6-35b-a3b-unsloth-q6` at `212992 / np1 / q8_0-q8_0 / b=2048 / ub=1024`, and the temporary Q8 GGUF was deleted from `models/qwen-3.6`. Verification: `/v1/models` on `8091` again reports alias `qwen-3.6-35b-a3b-unsloth-q6` with `n_ctx=212992`. Impact: the stack is back on the known-good default, and the Q8 test no longer consumes disk or create ambiguity about the active daily-driver profile.
[Qwen 3.6 35B Unsloth Q8 XL download] outcome — on 2026-06-09, `Qwen3.6-35B-A3B-UD-Q8_K_XL.gguf` was downloaded from `unsloth/Qwen3.6-35B-A3B-GGUF` into `models/qwen-3.6` as `Qwen3.6-35B-A3B-UD-Q8_K_XL-unsloth.gguf`. Verification: the file finished cleanly at `36G` on disk. Impact: the higher-quality 35B Unsloth quant is now available locally for future fit and quality tests without changing the current default runtime.
[Second R9700 viability] outcome — the host board is `MSI MAG X570 TOMAHAWK WIFI (MS-7C84)`. Local DMI and PCIe inspection plus the board manual confirm the primary GPU slot is the CPU-fed `PCI_E1`, while the second full-length slot is `PCI_E3 (PCH)` at `PCIe 4.0 x4`, not a second CPU x16 slot. The current R9700 sits on a full-width link in the primary slot. Impact: a second R9700 would mainly buy VRAM headroom and cleaner larger-model fits, but on this board it would hang off the chipset uplink and is therefore a poor basis for expecting large speed gains from multi-GPU splitting in `llama.cpp`; layer split might still be usable, but tensor-parallel style gains would be constrained by interconnect bandwidth and shared chipset traffic.
[35B Qwen 4096/2048 fit sweep] outcome — on 2026-06-09, both 35B Qwen finalists were rechecked one-model-at-a-time on Vulkan with `mmproj`, `np 1`, `q8_0/q8_0`, `-b 4096`, `-ub 2048`, prompt cache disabled, and speculative decoding disabled to find a practical context ceiling for the aggressive PP-max batch shape. Both models were *usable* all the way up to `262144`: every tested context booted and reached `/health`. The important difference was clean-fit behavior. `35B Unsloth` showed CPU tensor overrides at `262144`, `245760`, `229376`, `212992`, `196608`, `180224`, `176128`, `172032`, and `167936`, but the warning disappeared at `163840`; impact: `163840 / np1 / q8_0-q8_0 / 4096/2048` is the highest clean Unsloth context found so far for the aggressive batch shape, while anything above that is usable but spills. `35B Hauhau` was worse: CPU tensor overrides persisted at every tested context from `262144` down through `81920`; impact: Hauhau is usable at `4096/2048`, including `256k`, but no clean all-GPU threshold was found in the tested range, so `4096/2048` is not a sensible “fit-maximized” daily shape for Hauhau on this box.
[HIP runtime refresh] outcome — on 2026-06-08, the stale HIP comparison path was corrected by building a fresh separate ROCm binary tree at `/home/j/.local/src/llama.cpp/build-hip-r9700-current` from the current `llama.cpp` source (`94a220cd6`, version `1086`). The old `build-hip` tree was left untouched because the environment blocked cache deletion. The fresh HIP configure used AMD-relevant options that the old build lacked: `GGML_HIP_GRAPHS=ON`, `GGML_HIP_ROCWMMA_FATTN=ON`, `GGML_HIP_NO_VMM=ON`, `CMAKE_HIP_ARCHITECTURES=gfx1201`, plus the current ROCm 7.2.4 toolchain and `hip-lang` config path. Verification: the build completed successfully, including `libggml-hip.so` and `llama-server`, so the ROCm retest is now based on a current HIP binary instead of the stale older build. Impact: future Vulkan-vs-ROCm comparisons on this host are now fairer because the HIP path finally includes the newer AMD/rocWMMA/graphs improvements rather than lagging several release generations behind.
[Main switched to ROCm test backend] outcome — on 2026-06-08, the runtime selector in `config/localllm-runtime.env` was moved from Vulkan to HIP and pinned explicitly to `/home/j/.local/src/llama.cpp/build-hip-r9700-current/bin/llama-server`, then the live main service was restarted. Verification: `systemctl --user status localllm-main.service` shows the main process running the fresh HIP binary with `--device ROCm0`, and `/v1/models` plus `/health` are live on `8091` with the usual Unsloth profile (`212992 / np1 / q8_0-q8_0 / b=2048 / ub=1024`). Impact: the current private main model is now running on ROCm for real agent testing, while embeddings/reranker/TTS remain unchanged.
[ROCm rollback to Vulkan] outcome — after real agent-side testing on 2026-06-08, the fresh HIP/ROCm path was judged slightly slower on both prompt processing and decode than the Vulkan baseline, with no practical upside felt by the user. The runtime selector was therefore returned to `LOCALLLM_RUNTIME_BACKEND=vulkan` / `LOCALLLM_RUNTIME_DEVICE=Vulkan0`, and the live main service was restarted back onto `/home/j/.local/src/llama.cpp/build-vulkan-r9700/bin/llama-server`. Verification: `systemctl --user status localllm-main.service` now shows the Vulkan binary with `--device Vulkan0`, and `/v1/models` is live again on the normal Unsloth profile (`212992 / np1 / q8_0-q8_0 / b=2048 / ub=1024`). Impact: Vulkan remains the default runtime for this host, while the fresh HIP build is kept only as a future comparison path rather than the active serving backend.
[35B preset surface cleanup] outcome — on 2026-06-08, the retained main-model preset inventory was pruned to only the two surviving 35B Qwen 3.6 candidates: `main-qwen-3.6-35b-a3b-unsloth-q6.env` and `main-qwen-3.6-35b-a3b-hauhau-q6.env`. The older Gemma and 27B Qwen preset files were removed because they are no longer part of the intended main-model decision set. Impact: future preset reloads and comparisons now stay focused on the only two plausible defaults instead of carrying stale model choices.
[35B Unsloth final live shape] outcome — on 2026-06-08, the main service was explicitly restarted on the finalized Unsloth tuning after the head-to-head tests. The live `8091` model is `qwen-3.6-35b-a3b-unsloth-q6` with `212992 / np1 / q8_0-q8_0 / b=2048 / ub=1024`, `mmproj` loaded, prompt cache enabled, and `--spec-default` restored. Verification: `/v1/models` reports `n_ctx=212992`, and `config/localllm-main.env` plus the Unsloth preset carry the same tuned shape. Impact: the default private model is back on the highest trusted all-GPU context shape we settled on for daily use, not the equalized lower test shape.
[Share service retired] outcome — on 2026-06-08, the authenticated share proxy on `8095` was stopped and disabled because it is no longer used. `localllm-share.service` is now inactive and `systemctl --user is-enabled localllm-share.service` returns `not-found`; `8095` no longer listens. `scripts/start-stack.sh`, `scripts/stop-stack.sh`, `scripts/status.sh`, and `README.md` were updated so the normal stack no longer expects a share service. Impact: future stack restarts will not resurrect the share port, and health checks now only cover the active private chat, embedding, reranker, and TTS surfaces.
[35B Unsloth tuned live shape] outcome — on 2026-06-08, the main service was switched back from the temporary Hauhau test profile to `qwen-3.6-35b-a3b-unsloth-q6` on the safer tuned single-slot `q8` shape: `MAIN_CONTEXT=212992`, `np 1`, `b=2048`, `ub=1024`, `q8_0/q8_0`, reasoning on, prompt cache on, and `--spec-default` restored. Verification: the live `llama-server` command on `8091` shows `-c 212992 -np 1 -b 2048 -ub 1024 -ctk q8_0 -ctv q8_0`, and `/v1/models` now advertises `qwen-3.6-35b-a3b-unsloth-q6`. Impact: the default main model is back up on the safer higher-batch tuning without returning to the older `1024/512` or full `256k` live shape.
[35B Hauhau live test shape] outcome — on 2026-06-08, the main service was temporarily switched from the default 35B Unsloth profile to `qwen-3.6-35b-a3b-hauhau-q6` for real-agent testing on the recommended safer tuning point: `MAIN_CONTEXT=180224`, `np 1`, `q8_0/q8_0`, `b=2048`, `ub=1024`, reasoning on, prompt cache on, and no speculative decoding. Verification: the live `llama-server` command on `8091` now shows the Hauhau GGUF with `-c 180224 -np 1 -b 2048 -ub 1024 -ctk q8_0 -ctv q8_0`, and `/v1/models` advertises `qwen-3.6-35b-a3b-hauhau-q6`. Impact: the user can now judge Hauhau directly under the tuned `q8` single-slot shape without modifying the shared stack layout.
[35B Qwen tuning safety rule] outcome — after repeated OOM/regression incidents during 2026-06-08 scratch tuning, future batch/ubatch sweeps on large multimodal Qwen models should not use temporary `llama-server` or `llama-cli` chat-style runs with `mmproj`, uncapped reasoning, or `--no-mmap` just to estimate throughput. The safe method on this host is: reuse prior context-fit findings, unload the live stack, then use `llama-bench` with mmap enabled and no projector to compare `-b / -ub` on the GPU math path. Impact: this avoids host-RAM blowups and prevents tuning work from destabilizing unrelated services.
[35B Qwen batch tuning] outcome — on 2026-06-08, the two remaining 35B Qwen 3.6 candidates were retuned safely after a reboot using `llama-bench` only, with the live stack unloaded and the sweep restricted to the text path (`q8_0/q8_0` KV, `-p 4096`, `-n 512`, `-fa on`, mmap enabled, one repetition per point). Earlier fit work was reused instead of rerunning the risky full-runtime `256k` load path: `35B Unsloth` still has the cleanest no-CPU-RS point at `212992 / np1 / q8_0-q8_0`, while `35B Hauhau` keeps some CPU RS spill even as context drops and the normalized all-model comparison shape remains `180224 / np1 / q8_0-q8_0 / no-spec`. Bench sweep results: Unsloth PP improved sharply once `ub` reached `1024+`, peaking at `b=4096, ub=2048` with `3003.76 tok/s` PP while TG stayed effectively flat at `105.82 tok/s`; a safer near-top point is `b=2048, ub=1024` with `2851.63 tok/s` PP and `106.65 tok/s` TG. Hauhau showed the same pattern, peaking at `b=4096, ub=2048` with `2910.29 tok/s` PP and `111.99 tok/s` TG, while `b=2048, ub=1024` gave the best TG `112.24 tok/s` with `2823.37 tok/s` PP. Impact: for both 35B models on this box, `ub=1024+` is the real win and decode barely changes; `4096/2048` is the aggressive PP-max choice, while `2048/1024` is the safer recommendation if we later convert these sweep findings into live service flags.
[Qwen 3.6 four-model common eval shape] outcome — on 2026-06-08, the earlier `196608` normalized comparison target was invalidated by a stricter fit sweep because `qwen36-27b-unsloth` still landed at `64/65` layers with about `1632 MiB` of `Vulkan_Host` model spill and `3.12 MiB` of CPU recurrent-state spill there. With all managed services stopped, both 27B non-MTP models were then swept downward under the normalized comparison policy (`mmproj` loaded, `np 1`, `q8_0/q8_0` KV, no speculative decoding, `-tb 8 -b 1024 -ub 512 -fa on --threads-http 4 --metrics --no-warmup --no-mmap --image-max-tokens 12288`, shared sampling defaults). Highest common clean 27B result: `180224` context. At `180224`, both `qwen36-27b-unsloth` and `qwen36-27b-hauhau` reached `65/65` layers with no CPU KV or CPU RS spill; Unsloth still uses about `1288 MiB` of `Vulkan_Host` model staging and Hauhau about `995 MiB`, but both are otherwise fully GPU-resident by the practical test criterion. The 35B models were then checked at the same `180224 / np1 / q8_0-q8_0 / no-spec` shape: `qwen36-35b-unsloth` loads acceptably with `41/41` layers, no CPU KV/RS spill, and about `515 MiB` of `Vulkan_Host` model staging; `qwen36-35b-hauhau` also loads acceptably with `41/41` layers, no CPU KV spill, and about `62.81 MiB` of CPU RS spill plus `1638 MiB` of `Vulkan_Host` model staging. Impact: the staged four-model quality comparison should use `180224 / np1 / q8_0-q8_0 / no-spec` as the new common denominator because it is the highest context that keeps both 27B variants clean while still allowing both 35B sparse variants to load on the same normalized profile.
[Qwen 3.6 four-model staged comparison] outcome — on 2026-06-08, the four remaining non-MTP Qwen 3.6 models were compared on the normalized fairness shape `180224 / np1 / q8_0-q8_0 / no-spec` with `mmproj` loaded, shared sampling defaults, prompt cache off, and the common runtime profile `-tb 8 -b 1024 -ub 512 -fa on --threads-http 4 --metrics --no-warmup --no-mmap --image-max-tokens 12288`. Stage 1 (`transcript_replay` + `sim_compare`) eliminated the 27B models as main-default candidates: `qwen36-27b-unsloth` only managed replay `1/5` with `13/18` matched turns and timed out badly, while `qwen36-27b-hauhau` improved to `25/35` matched replay turns and perfect `8/8` sim pass count but still only replayed `1/5` fixtures correctly. The real finalists were the 35Bs: `qwen36-35b-unsloth` scored replay `2/5`, `26/35`, sim `7/8`, `scope_clean=6`, `tool_error_free=5`; `qwen36-35b-hauhau` scored replay `3/5`, `21/26`, sim `7/8`, `scope_clean=7`, `tool_error_free=8`, and was the only model to pass the transcript `retry_tool_followthrough` fixture. Impact: the remaining decision is 35B-vs-35B, not 27B-vs-35B.
[Qwen 3.6 35B uncapped head-to-head] outcome — on 2026-06-08, the two 35B finalists were rerun from a clean unloaded state on the equalized no-spec shape `180224 / np1 / q8_0-q8_0` with `mmproj` loaded and no output caps, one model at a time to avoid co-load OOM. The first uncapped replay attempt exposed a real pathology in `soak_real`: `qwen36-35b-unsloth` ran away past `66k` decoded tokens, so the clean replay rerun excluded `soak_real` and used only `retry_fix_real`, `retry_tool_followthrough`, `queue_fix_real`, and `retry_triage_real`; both models then tied at `2/4` fixtures and `14/16` matched turns. The tie was shape-sensitive rather than identical: Unsloth passed `queue_fix_real` and `retry_triage_real` but still failed `retry_tool_followthrough`, while Hauhau passed `retry_tool_followthrough` and `retry_triage_real` but regressed `queue_fix_real`. The uncapped sim rerun also tied exactly: both `qwen36-35b-unsloth` and `qwen36-35b-hauhau` finished at `4/4` pass, `3/4` scope-clean, and `4/4` tool-error-free, with both only losing scope cleanliness on `session_store_exploration`. Uncapped barrage narrowed the practical difference further: both handled plan/review/evidence prompts well, both still collapsed `tool_followthrough` turn 2 into the trivial `2 + 2 = 4` answer, and Unsloth was actually better on `tool_restraint` because Hauhau unnecessarily called a tool there. Impact: the earlier broad conclusion that Hauhau was the clear capability winner is too strong after the uncapped equal-shape rerun; the gap is now narrow enough that the choice is mostly speed-and-familiarity (`35B Unsloth`) versus slightly better historical followthrough on one replay fixture (`35B Hauhau`), not a decisive model-quality blowout.
[Qwen benchmark timeout override] outcome — on 2026-06-08, the replay and sim harness drivers were patched to honor `LOCALLLM_BENCH_REQUEST_TIMEOUT` instead of hardcoding `300s`, using a default of `1800s` for long uncapped Qwen runs. Verification: both `benchmarks/transcript_replay/run_replay.py` and `benchmarks/sim_compare/run_agentic_sim.py` now derive request timeout from the environment, and `ruff check` passes on both files. Impact: future uncapped Qwen evals can run long without artificial request aborts, while timeout policy remains adjustable per run instead of requiring more source edits.
[Qwen no-cap rule] outcome — repeated Qwen harness behavior confirmed that capped-token comparisons distort the model's behavior enough to hide the real ranking. Future Qwen evals should not use `max_tokens`, `thinking_budget_tokens`, or similar output caps unless truncation behavior itself is under test. Impact: capability comparisons now rely on uncapped runs even when they take longer, because capped runs produced false negatives and misleading tool-followthrough failures.
[Qwen 3.6 four-model staged-eval readiness] outcome — on 2026-06-08, the future staged comparison set was verified against the actual on-disk Qwen 3.6 inventory. All four remaining non-MTP targets and their `mmproj` files exist: `27B Unsloth`, `27B Hauhau`, `35B Unsloth`, and `35B Hauhau`. Candidate specs for all four parse cleanly through `benchmarks.model_eval.parse_candidate_specs`, and the main standalone harness wrappers now default to live Qwen 3.6 paths instead of deleted Qwen 3.5 models. Impact: the local harness layer is ready for a staged four-model run; the remaining decision is methodological, not infrastructural — whether to compare each model on its own best-known serving shape or force a stricter apples-to-apples normalization by size.
[Qwen 3.6 harness default repair] outcome — on 2026-06-08, the benchmark/eval wrappers were audited for a future four-model Qwen 3.6 comparison and several standalone defaults were found to still point at deleted Qwen 3.5 assets. `benchmarks/run_model_eval.sh`, `benchmarks/transcript_replay/run_compare.sh`, `benchmarks/sim_compare/run_compare.sh`, `benchmarks/opencode_compare/run_compare.sh`, and `benchmarks/agentic_barrage_compare.sh` now default back to the live `qwen-3.6-35b-a3b-unsloth-q6` restore preset and use current Qwen 3.6 35B Unsloth candidate specs instead of dead Qwen 3.5 paths. Verification: `bash -n` passes on the touched wrappers, and grep over `benchmarks/` no longer shows deleted Qwen 3.5 default candidate specs in the runnable harness entrypoints; only historical score-sheet references remain in `benchmarks/BENCHMARK_RESULTS.md`. Impact: the harness layer is now safe to run standalone again without silently targeting missing models or restoring to a dead preset.
[Qwen 3.6 27B MTP temporary live swap] outcome — on 2026-06-08, the temporary 27B MTP trial was completed and cleaned up. The live main service was first switched to `Qwen3.6-27B-MTP-Q6_K-unsloth.gguf` on the previously best all-around shape (`131072 / np1`, `q8_0/q8_0`, `mmproj`, `--spec-type draft-mtp --spec-draft-n-max 2`) so the user could test it in their real agent workflow. After the test, `config/localllm-main.env` was restored from `/tmp/localllm-main.env.pre-27b-mtp`, `localllm-main.service` was restarted back onto the default `qwen-3.6-35b-a3b-unsloth-q6`, authenticated share `8095` recovered onto the same default alias, and both temporary 27B MTP files were removed from disk. Verification: `8091` and authenticated `8095` now both advertise `qwen-3.6-35b-a3b-unsloth-q6`, while `models/qwen-3.6` no longer contains `Qwen3.6-27B-MTP-Q6_K-unsloth.gguf` or `Qwen3.6-27B-MTP-mmproj-F16-unsloth.gguf`. Impact: the system is fully back on the normal 35B default with the discarded 27B MTP artifacts removed.
[Qwen 3.6 35B MTP temporary live swap] outcome — on 2026-06-08, a temporary swap from the live `qwen-3.6-35b-a3b-unsloth-q6` main model to the previously tested `Qwen3.6-35B-A3B-MTP-UD-Q6_K-unsloth.gguf` candidate was attempted using a direct edit of `config/localllm-main.env` with the known-good scratch shape (`131072 / np1 / q8_0-q8_0`, `mmproj` loaded, `--spec-type draft-mtp --spec-draft-n-max 3`). The swap failed immediately because the 35B MTP GGUF is no longer present on disk: `scripts/serve-main.sh` exited with `Missing required file: /home/j/projects/localllm/models/qwen-3.6/Qwen3.6-35B-A3B-MTP-UD-Q6_K-unsloth.gguf`. The backup config was restored from `/tmp/localllm-main.env.pre-mtp-35b`, and `localllm-main.service` was restarted back onto the normal non-MTP 35B Unsloth model. Impact: there is no live 35B MTP test to perform until that exact model file is downloaded again; the failed swap did not leave the repo config or service state altered.
[Qwen 3.6 27B MTP Q6_K n=1..9 sweep] outcome — on 2026-06-05, `Qwen3.6-27B-MTP-Q6_K-unsloth.gguf` was rerun alone across `--spec-draft-n-max` values `1` through `9` on the same scratch shape used in the earlier comparisons: `131072 / np1 / q8_0-q8_0 KV`, `mmproj` loaded, `-b 1024 -ub 512`, cache off, and the two simple uncapped probes (2-paragraph and long prompt). The sweep showed that the earlier `n=3` result was not the best all-around setting once the full curve was measured. `n=2` is the strongest overall choice: it won both PP and TG on the 2-paragraph probe (`184.16 pp / 43.27 tg`) and also won PP on the long prompt (`554.63 pp / 46.86 tg`). The only place `n=2` did not win was long-prompt decode, where `n=3` edged it out slightly (`47.55 tg` versus `46.86 tg`) at lower PP. Higher values degrade steadily: `n=5+` are clear losers on the 2-paragraph probe, and `n=8/9` are the worst on both shapes. Impact: for any future deeper 27B MTP testing, the default `draft-mtp` setting should be `n=2`, with `n=3` only worth considering if a longer-prompt decode microbenchmark is the primary target.
[Qwen 3.6 non-MTP 27B/35B baseline against MTP] outcome — on 2026-06-05, the current non-MTP Unsloth Qwen 3.6 27B and 35B models were benchmarked on the exact same simple scratch shape used for the MTP elimination pass: `131072 / np1 / q8_0-q8_0 KV`, `mmproj` loaded, `-b 1024 -ub 512`, cache off, no speculation, and the same uncapped 2-paragraph plus long-prompt probes. The `27B` non-MTP baseline (`Qwen3.6-27B-UD-Q6_K_XL-unsloth.gguf`) landed at `211.02 pp / 21.54 tg` on the 2-paragraph probe and `663.40 pp / 21.47 tg` on the long prompt. The `35B` non-MTP baseline (`Qwen3.6-35B-A3B-UD-Q6_K-unsloth.gguf`) landed at `411.79 pp / 102.12 tg` on the 2-paragraph probe and `1833.59 pp / 101.76 tg` on the long prompt. Compared directly to the best MTP candidates from the earlier sweep, this cleanly splits the recommendation by size: for `27B`, MTP is meaningfully faster on decode (`42.03/44.95 tg` best MTP versus `21.54/21.47 tg` non-MTP) despite lower PP; for `35B`, the current non-MTP model is faster on both PP and TG than the best MTP option (`102.12/101.76 tg` non-MTP versus `72.20/86.67 tg` best MTP). Impact: if the user wants one `27B` and one `35B` option to keep, the speed-only answer is `27B MTP` (plain `Q6_K`, `n=3`) and `35B non-MTP` (current main model).
[Qwen 3.6 Unsloth MTP first-pass speed elimination] outcome — on 2026-06-05, the live main/share services were stopped so the GPU was clean, then the four downloaded Unsloth MTP Q6 variants were benchmarked one at a time on a shared scratch shape: `131072 / np1 / q8_0-q8_0 KV`, `-b 1024 -ub 512`, `mmproj` loaded, `--spec-type draft-mtp`, `--no-cache-prompt --cache-ram 0`, and `--spec-draft-n-max` swept across `3`, `5`, and `7`. Each run used two simple text prompts: the standard uncapped 2-paragraph probe and a longer direct-answer prompt. Results cleanly separated the field. The two 27B models are speed losers for this first-pass use case: plain `27b-mtp-q6_k` was best at `n=3` with `42.03 tg` on the 2-paragraph probe and `44.95 tg` on the long prompt, while `27b-mtp-ud-q6_k_xl` was worse and collapsed badly at higher `n` (`19.18/15.08 tg` on the 2-paragraph probe for `n=5/7`). The two 35B A3B models are the only plausible survivors. `35b-a3b-mtp-ud-q6_k` was the overall speed winner: `n=3` gave the best 2-paragraph decode (`72.20 tg`, `175.46 pp`), while `n=5` gave the best long-prompt decode (`86.67 tg`, `1176.45 pp`), with `n=7` clearly worse. `35b-a3b-mtp-ud-q6_k_xl` trailed the non-XL 35B at every tested `n`, topping out at `64.67 tg` on the 2-paragraph probe and `72.39 tg` on the long prompt at `n=3`. Impact: for any deeper agent/harness retest, the 27B MTP variants should be eliminated on speed alone, the 35B XL variant is a secondary candidate at best, and the only strong next-step MTP candidate is `Qwen3.6-35B-A3B-MTP-UD-Q6_K` with focus on `n=3` and maybe `n=5`.
[Qwen 3.6 Unsloth MTP model redownload] outcome — on 2026-06-05, the missing Unsloth MTP assets were redownloaded into `models/qwen-3.6` without overwriting the existing non-MTP Qwen files by saving them under explicit `-MTP-...-unsloth` names. Downloaded files: `Qwen3.6-27B-MTP-Q6_K-unsloth.gguf` (`22G`), `Qwen3.6-27B-MTP-UD-Q6_K_XL-unsloth.gguf` (`25G`), `Qwen3.6-27B-MTP-mmproj-F16-unsloth.gguf` (`885M`), `Qwen3.6-35B-A3B-MTP-UD-Q6_K-unsloth.gguf` (`28G`), `Qwen3.6-35B-A3B-MTP-UD-Q6_K_XL-unsloth.gguf` (`31G`), and `Qwen3.6-35B-A3B-MTP-mmproj-F16-unsloth.gguf` (`858M`). Impact: the full Unsloth MTP Q6 set needed for fresh 27B/35B testing is back on disk, while the existing non-MTP model files remain untouched.
[Qwen 35B post-AMD-upgrade probe pair] outcome — on 2026-06-05, after the host moved to the AMD `7.2.4` graphics/ROCm userspace stack, the live main model `qwen-3.6-35b-a3b-unsloth-q6` was probed twice again with the same uncapped two-paragraph request used for prior build checks. Both runs hit prompt-cache reuse rather than a cold prefill (`4` prompt tokens in each run), so the useful comparison is mostly decode-side. Run 1 produced `28.74 tok/s` prompt processing on the reused prompt fragment and `78.83 tok/s` decode over `2063` generated tokens with `0.81027` draft acceptance. Run 2 produced `117.87 tok/s` prompt processing on the reused fragment and `74.83 tok/s` decode over `2651` generated tokens with `0.44761` draft acceptance. Impact: the post-driver-upgrade live service remains healthy and decode still sits in the mid-to-high `70 tok/s` range on this main Qwen profile; these two specific runs are warm/cache-reuse reads rather than cold-PP benchmarks.
[llama.cpp b9496 rebuild and live probe] outcome — on 2026-06-04, after a fresh tag fetch showed newer stable releases beyond the existing `b9442` checkout, the local `~/.local/src/llama.cpp` tree was moved to exact tag `b9496` (`94a220cd6`) and the Vulkan build was regenerated in the same production profile: `CMAKE_BUILD_TYPE=Release`, `GGML_VULKAN=ON`, `GGML_NATIVE=ON`, `LLAMA_BUILD_SERVER=ON`, `LLAMA_BUILD_COMMON=ON`, and `LLAMA_BUILD_TESTS=OFF`. The rebuilt binary reports `llama-server version 1086 (94a220cd6)`. `localllm-main.service`, `localllm-share.service`, `localllm-embedding.service`, `localllm-reranker.service`, and `localllm-tts.service` were all restarted successfully on the new runtime without a reboot; the main/share services took their usual model-load time but all `8091`–`8095` health endpoints and model listings recovered. A repeated uncapped two-paragraph probe on the live main model (`qwen-3.6-35b-a3b-unsloth-q6`) produced: cold run `88` prompt tokens at `429.81 tok/s` and `2496` decoded tokens at `81.65 tok/s`; warm run `4` prompt tokens at `92.46 tok/s` with prompt-cache reuse and `2267` decoded tokens at `85.17 tok/s`. Impact: the live stack is now on the newer `b9496` release line and decode speed on the current Qwen service improved versus the earlier `b9442` probe, while no launch-flag change was needed.
[AMD 7.2.4 rollout] outcome — on 2026-06-05, after the repo pins were updated externally from `7.2.1` to `7.2.4`, the live host was verified to have upgraded cleanly without a reboot. Installed versions are now `mesa-amdgpu-vulkan-drivers`, `libegl1-amdgpu-mesa`, `libgl1-amdgpu-mesa-dri`, and `libgbm1-amdgpu` all at `1:26.0.0.07020400-2341098.24.04`, plus `rocm 7.2.4.70204-93~24.04` and `hip-runtime-amd 7.2.53211.70204-93~24.04`. `vulkaninfo --summary` still reports the expected active runtime path on the R9700 (`driverName = radv`, `driverInfo = Mesa 26.0.0-devel`), and all managed user services recovered healthy after restart: main `8091`, embedding `8092`, reranker `8093`, OmniVoice `8094`, and share `8095`. Impact: the box is now on the newer AMD 7.2.4 graphics/ROCm userspace stack with no immediate health regression.
[AMD driver stack June 2026 check] outcome — on 2026-06-04, the host was confirmed to still be running the previously installed AMD userspace stack from Radeon `7.2.1`: `Mesa 26.0.0-devel` via `mesa-amdgpu-vulkan-drivers 1:26.0.0.70201-2303469.24.04`, plus `rocm 7.2.1.70201-81~24.04` / `hip-runtime-amd 7.2.53211.70201-81~24.04`; local apt candidates remain identical because `/etc/apt/sources.list.d/rocm.list` is pinned to `graphics/7.2.1` and `rocm/apt/7.2.1`. Upstream AMD now has newer repos available for this stack: `graphics/7.2.4` / `graphics/latest` carry Mesa `26.0.0.07020400-2341098` packages dated 2026-05-26, and ROCm release history shows `7.2.4` released on 2026-05-29 with matching `rocm/apt/7.2.4` plus `amdgpu-install_7.2.4.70204-1_all.deb` published. A clean apt simulation against temporary `7.2.4` sources showed the expected upgrade set without touching the live sources: `mesa-amdgpu-vulkan-drivers`, `libegl1-amdgpu-mesa`, `libgl1-amdgpu-mesa-dri`, and `libgbm1-amdgpu` would all move from `1:26.0.0.70201-2303469.24.04` to `1:26.0.0.07020400-2341098.24.04`; `rocm` would move from `7.2.1.70201-81~24.04` to `7.2.4.70204-93~24.04`; and `hip-runtime-amd` would move from `7.2.53211.70201-81~24.04` to `7.2.53211.70204-93~24.04`. Impact: there is a real newer AMD driver/runtime line available and it looks straightforward to stage, but this box will not see it through apt until the Radeon source entries are repointed off `7.2.1`; no upgrade was applied during the check.
[llama.cpp release line June 2026 check] outcome — on 2026-06-04, the local `~/.local/src/llama.cpp` checkout remained at exact tag `b9442` (`d4c8e2c29`), while `git fetch --tags origin` showed newer stable release tags through `b9496`. Impact: there is a newer upstream stable `llama.cpp` release line available than the current local build, but no rebuild or rollout was done during the check.
[llama.cpp b9442 Vulkan rebuild] outcome — on 2026-05-31, the local `~/.local/src/llama.cpp` checkout was moved from `d14ce3dab4de` to the latest release tag `b9442` (`d4c8e2c29`) and the Vulkan build was regenerated as a production profile with `CMAKE_BUILD_TYPE=Release`, `GGML_VULKAN=ON`, `GGML_NATIVE=ON`, `LLAMA_BUILD_SERVER=ON`, `LLAMA_BUILD_COMMON=ON`, and `LLAMA_BUILD_TESTS=OFF`. The rebuilt binary reports `llama-server version 1032 (d4c8e2c29)`. Managed `llama.cpp` services were restarted on the new binary: main `8091`, embedding `8092`, reranker `8093`, and the share proxy `8095`; all health endpoints recovered. The updated main-service startup log now explicitly reports multimodal fit accounting (`[mtmd] estimated worst-case memory usage of mmproj is 857.60 MiB`), confirming one of the useful upstream late-May changes is active. No new runtime flags were adopted during the reload because none of the newly available options looked like a clearer win than the existing Qwen launch profile on this host. A repeated uncapped two-paragraph probe on the live main model (`qwen-3.6-35b-a3b-unsloth-q6`) produced: cold run `prompt_n=157`, `cache_n=0`, `458.57 tok/s` prompt processing, `67.44 tok/s` decode; warm run `prompt_n=4`, `cache_n=153`, `85.81 tok/s` prompt processing, `77.63 tok/s` decode. Impact: the runtime update is live and healthy, multimodal fit accounting improved, and the current main model remains fast on this build with no immediate evidence that a new launch flag change is needed.
[llama.cpp late-May 2026 update scan] outcome — on 2026-05-31, the local `~/.local/src/llama.cpp` checkout was confirmed at `d14ce3dab4de`, while `git fetch` showed upstream `origin/master` at `399739d5c` and release tags through `b9442`. The useful changes for this host since `d14ce3dab` are real but mixed with fresh risk. Likely beneficial changes for this AMD/Vulkan + Qwen + multimodal stack: `mtmd: fit_params now take into account mmproj` (`#21489`) makes `--fit` include projector memory; `llama: use f16 mask for FA to save VRAM` (`#23764`) should reduce FA VRAM pressure; several Vulkan kernel improvements landed, including `Switch MUL_MAT_VEC to 4 K per iteration for F16/32` (`#22887`) and `fast path for walsh-hadamard transform` (`#23687`); and recent MTP/server fixes include `server: MTP layer kv-cache should respect draft type ctk` (`#23646`) plus `free draft/MTP resources on sleep to fix VRAM leak` (`#23461`). The caution side matters too: fresh upstream issues in late May report Vulkan/AMD regressions across some builds (`#23779` clean-exit/no-listener regression between `b9272` and `b9357`), and MTP+Qwen+vision/long-context problems are still active upstream (`#23371`, `#23233`). Impact: there are enough potentially helpful Vulkan and multimodal-fit changes that a staged upgrade is worth considering, but not a blind production jump on the live main service. The safest recommendation is to benchmark a fresh scratch build against the current pinned `d14ce3dab` on this host before replacing the working runtime.
[Qwen 3.6 MTP real-agent sweep] outcome — on 2026-05-19, all four downloaded Qwen 3.6 MTP models were run through the real harness stack after updating `llama.cpp` to `version 825 (d14ce3dab)` with the MTP prompt-decode fix. Replay and sim used the intended live multimodal shape (`27B` at `98304 / np1 / q8_0-q8_0`, `35B` at `131072 / np1 / q8_0-q8_0`, mmproj loaded, prompt cache off). `transcript_replay` results separated the field clearly: `35b-unsloth-mtp` was best at `3/5` passed fixtures and `28/35` matched turns; `35b-heretic-mtp` landed `2/5` and `26/35`; both `27B` models landed `1/5` and `26/35`. `sim_compare` was stronger overall but still differentiated fit-for-agent use: `27b-unsloth-mtp` passed `8/8` with `6/8` scope-clean and `7/8` tool-error-free; `27b-heretic-mtp` passed `8/8` with `6/8` scope-clean and `8/8` tool-error-free; `35b-unsloth-mtp` passed `8/8` with `7/8` scope-clean but only `6/8` tool-error-free; `35b-heretic-mtp` passed `7/8` with `7/8` scope-clean and `7/8` tool-error-free, failing `session_store_exploration`. Two uncapped suites exposed a separate MTP pathology: the default `coding_compare` and `agentic_barrage` shapes can run away into very long hidden reasoning or huge generations on some MTP variants. Concrete failures seen locally: `27b-heretic-mtp` produced a very long uncapped `retry_bug` coding answer instead of terminating promptly, and `35b-unsloth-mtp` ran past `70k` decoded tokens on uncapped barrage `codex_workflow`. To finish the full comparison fairly, both suites were rerun under explicit caps (`coding` with `max_tokens=2048`, `barrage` with `max_tokens=1024`) for all four models. Under the bounded coding pass, all four models only completed `simple_edit`; `retry_bug` and `merge_intervals` hit the cap on every model, with the 35B variants much faster (`~123–131 tok/s`) than the 27B variants (`~37–43 tok/s`). Under the bounded barrage pass, all four models still showed the same bad `tool_followthrough` failure mode on turn 2 (`2 + 2 is 4` style collapse instead of workflow continuation), and all four were prone to exhausting the cap in hidden reasoning on plan/review/evidence prompts; `27b-heretic-mtp` was the least bad bounded barrage variant with `6/10` turns producing non-empty content versus `4/10` for `27b-unsloth-mtp`, `4/10` for `35b-heretic-mtp`, and `3/10` for `35b-unsloth-mtp`. Impact: MTP is now genuinely working and fast on this host, but for the user’s actual assistant-agent workload none of the four MTP variants is clean enough to replace the stable non-MTP default yet. The strongest real-workload candidate is `35b-unsloth-mtp` because it wins the replay suite and stays fully passing on sim despite some tool errors, while `35b-heretic-mtp` is the raw-speed leader and `27b-heretic-mtp` is the best-behaved 27B. The common blocker is not throughput anymore; it is hidden-reasoning bloat plus persistent followthrough drift on tool-heavy turns.
[Qwen 3.6 MTP post-fix comparison table] outcome — on 2026-05-19, after rebuilding `llama.cpp` to `version 825 (d14ce3dab)` with the MTP prompt-decode fix, the four downloaded MTP models were rebenchmarked on a long text-only prompt using `--no-mmproj` so projector overhead would not pollute text-speed comparisons. The refreshed runs used the same single-slot Q8-KV family as before: `27B` at `98304 / np1 / q8_0-q8_0`, `35B` at `131072 / np1 / q8_0-q8_0`, `-b 1024 -ub 512`, and prompt cache disabled by virtue of direct CLI runs. Reference baselines: `baseline-27b-nonmtp` scored `737.2 pp / 21.8 tg`, and `baseline-35b-nonmtp` scored `2314.7 pp / 101.8 tg`. MTP results: `27b-unsloth-n2` scored `656.7 pp / 41.7 tg`; `27b-unsloth-n3` scored `653.7 pp / 42.2 tg`; `27b-heretic-n2` scored `623.7 pp / 44.1 tg`; `35b-unsloth-n2` scored `1976.6 pp / 127.6 tg`; `35b-heretic-n2` scored `1911.1 pp / 134.0 tg`. Impact: on the fixed upstream build the MTP tradeoff is now sane on this host — roughly `11–15%` PP loss in exchange for large TG gains — and the best observed text-only decode winners are `27b-heretic-n2` and `35b-heretic-n2`, while `27b-unsloth-n3` remains a slight improvement over Unsloth `n2`.
[Qwen 3.6 MTP PP regression investigation] outcome — on 2026-05-19, the huge prompt-processing collapse from the first MTP sweep was traced to two separate issues rather than a single real-world `90%` regression. First, the original MTP benchmark used a tiny 24-token prompt, while the user’s non-MTP baselines came from real long prompts; on a long text-only prompt, the same local non-MTP models are much faster in PP than those first MTP sweep numbers implied. Second, the local `llama.cpp` build used for the first MTP tests (`4f13cb742`, version 781) predated upstream PR `#23198` (`avoid copying logits during prompt decode in MTP`), which was merged on 2026-05-17 and explicitly targets MTP PP speed. Long-prompt snapshots before updating showed: 35B non-MTP baseline `2308.4 t/s`, 35B MTP model with speculation disabled `2220.3 t/s`, 35B MTP with `draft-mtp` + `n=2` + mmproj `1161.9 t/s`, and 35B MTP with `draft-mtp` + `n=2` and `--no-mmproj` `1297.8 t/s`; 27B baseline `735.1 t/s`, 27B MTP model with speculation disabled `741.2 t/s`, and 27B MTP with `draft-mtp` + `n=3` + mmproj `513.2 t/s`. This proves the MTP GGUFs themselves are not the PP problem; the large drop only appears when the MTP speculative path is active, and `--mmproj` makes the hit somewhat worse. The local checkout was then fast-forwarded from `4f13cb742` to `d14ce3dab` and rebuilt successfully to `llama-server version 825`, which includes PR `#23198`. After the update, the same long-prompt snapshots improved to: 35B non-MTP `2254.3 t/s` versus 35B MTP `1942.9 t/s` with `draft-mtp n=2`, and 27B non-MTP `736.1 t/s` versus 27B MTP `645.4 t/s` with `draft-mtp n=3`. Impact: the earlier “almost 90% PP loss” was mostly a bad comparison between a tiny-prompt MTP micro-benchmark and real long-prompt non-MTP runs; on current upstream, the real PP penalty on this host is now roughly `14%` for 35B and `12%` for 27B, while TG still improves substantially (`102.6 -> 130.6 t/s` on 35B and `21.8 -> 40.3 t/s` on 27B in the long-prompt snapshots). The main and share services were restored healthy after the investigation.
[MTP benchmark comparison rule] outcome — after the user challenged the first MTP PP results, the investigation confirmed that comparing short-prompt MTP micro-benchmarks against long-prompt live baselines is misleading. Impact: future MTP PP comparisons must use prompt lengths comparable to the real baseline workload, or explicitly state that tiny-prompt overhead dominates the reported PP figure.
[Qwen 3.6 Unsloth MTP n-max sweep] outcome — on 2026-05-17, the initial poor MTP result was followed up with a fixed-shape `--spec-draft-n-max` sweep on the two Unsloth MTP models at `128k / np 1 / q8_0-q8_0 KV`, prompt cache disabled, and the same short two-sentence generation probe. For `qwen-3.6-27b` Unsloth MTP, the best decode point was `n-max 3`: `92.51` tok/s PP, `42.44` tok/s TG, and `0.85000` acceptance; `n-max 2` landed at `38.63` TG, while `n-max 4/5/6` regressed to `37.15`, `39.66`, and `35.91` TG as acceptance fell to `0.67327`, `0.71154`, and `0.57937`. For `qwen-3.6-35b-a3b` Unsloth MTP, the best decode point stayed `n-max 2`: `184.18` tok/s PP, `112.30` tok/s TG, and `0.84286` acceptance; raising `n-max` to `3/4/5/6` reduced TG to `100.85`, `103.25`, `101.30`, and `88.38` with lower acceptance (`0.73563`, `0.70000`, `0.68224`, `0.66964`). After the sweep, `localllm-main.service` and `localllm-share.service` were restarted and verified healthy again, with the normal live main model restored to `qwen-3.6-35b-a3b-unsloth-q6`. Impact: higher `n-max` does not rescue the Unsloth MTP models on this host; the 27B has a modest local optimum at `3`, but both Unsloth MTP builds remain disappointing relative to the established non-MTP baselines.
[Qwen 3.6 MTP benchmark sweep] outcome — on 2026-05-17, the local `llama.cpp` checkout was fast-forwarded from `8bccdbbff` to `4f13cb742` and the active Vulkan binary was rebuilt successfully to `llama-server version 781 (4f13cb742)`, which includes upstream `draft-mtp` support. For a fair common benchmark shape, the main and share services were temporarily stopped, a 27B Unsloth MTP verbose fit probe was run, and `128k / np 1 / q8_0-q8_0 KV` was confirmed as a clean all-GPU fit for that 27B MTP model: `offloaded 66/66 layers to GPU`, `Vulkan0 model buffer size = 23511.48 MiB`, `Vulkan0 KV buffer size = 4352.00 MiB`, and `Vulkan0 RS buffer size = 448.88 MiB`, with no CPU KV or CPU RS spill lines. All four downloaded MTP models were then benchmarked at that same `128k` context using `--spec-type draft-mtp --spec-draft-n-max 2`, prompt cache disabled, and the same short two-sentence request. Results: `27b-unsloth-mtp-q6` scored `77.50` tok/s PP and `39.37` tok/s TG with `0.81944` draft acceptance; `27b-heretic-mtp-q6` scored `100.72` PP and `48.06` TG with `0.91045` acceptance; `35b-unsloth-mtp-q6` scored `120.69` PP and `100.41` TG with `0.81690` acceptance; `35b-heretic-mtp-q6` scored `175.73` PP and `128.22` TG with `0.91045` acceptance. After the sweep, `localllm-main.service` and `localllm-share.service` were restarted and returned healthy. Impact: upstream MTP now works locally, `128k` is enough for a clean 27B MTP Q8-KV fit, and the Heretic MTP builds were materially faster than the Unsloth MTP builds on this host for this short-generation test.
[Main preset cleanup plus Unsloth restore] outcome — on 2026-05-08, the stale `config/presets/main-qwen-3.6-35b-a3b-hauhau-q5.env` preset and the no-longer-backed `config/presets/main-qwen-3.5-27b-huihui-qwopus-v3-q6.env` preset were deleted so the retained main-preset surface matches the current model inventory. The main service was then switched from Hauhau back to `config/presets/main-qwen-3.6-35b-a3b-unsloth-q6.env`. Verification: `./scripts/load-main-preset.sh qwen-3.6-35b-a3b-unsloth-q6` completed; `8091` health returned `ok`; `/props` now reports `model_alias = qwen-3.6-35b-a3b-unsloth-q6` with `n_ctx = 262144`; and startup logs again show `offloaded 41/41 layers to GPU`, `Vulkan0 model buffer size = 27151.74 MiB`, `Vulkan0 KV buffer size = 2720.00 MiB`, and `Vulkan0 RS buffer size = 62.81 MiB`. Impact: the dead preset entries are gone, and the live main model is back on the single-slot 256k Unsloth 35B Q6 shape with Q8 KV.
[Qwen 3.6 35B A3B Hauhau Q6 live swap] outcome — on 2026-05-07, the main service was switched from the Unsloth 35B Q6 build to the retained Hauhau preset `config/presets/main-qwen-3.6-35b-a3b-hauhau-q6.env`. Only the Hauhau Q6 model is actually present on disk; the older Hauhau Q5 preset remains stale inventory. The old Unsloth process needed a forced kill after systemd stop timed out, but the managed preset loader still recovered and brought the new Hauhau service up. Verification: `/props` now reports `model_alias = qwen-3.6-35b-a3b-hauhau-q6` with model path `Qwen3.6-35B-A3B-Uncensored-HauhauCS-Aggressive-Q6_K_P.gguf`; startup logs show `offloaded 41/41 layers to GPU`, `Vulkan0 model buffer size = 28335.00 MiB`, and `Vulkan_Host model buffer size = 883.98 MiB`; and the retained preset’s older shape is what came up live (`MAIN_CONTEXT=262144`, `-np 2`, `q4_0/q4_0` KV), which means `/props` reports `n_ctx = 131072` per slot rather than a single-slot 256k shape. Impact: Hauhau Q6 is now the live main model, but its retained preset still reflects the older two-slot q4-KV serving shape rather than the newer single-slot q8-KV Unsloth conventions.
[Qwen 3.6 35B A3B Unsloth Q6 live swap] outcome — on 2026-05-04, the main service was switched from the 27B Unsloth Q6 preset to a new retained preset `config/presets/main-qwen-3.6-35b-a3b-unsloth-q6.env` using the exact requested shape: `-np 1`, `MAIN_CONTEXT=262144`, and `-ctk q8_0 -ctv q8_0`. The managed load succeeded cleanly. Verification: `8091` health returned `ok`; `/props` reports alias `qwen-3.6-35b-a3b-unsloth-q6` with `n_ctx = 262144`; startup logs show `offloaded 41/41 layers to GPU`, `Vulkan0 model buffer size = 27151.74 MiB`, `Vulkan0 KV buffer size = 2720.00 MiB`, `Vulkan0 RS buffer size = 62.81 MiB`, and no CPU KV or CPU RS spill lines. Impact: the requested 35B Unsloth Q6 build fits on the single R9700 at the full single-slot `256k` shape with Q8 KV, while still keeping some expected host-side Vulkan buffers (`Vulkan_Host model/compute`) rather than being a literal zero-host-memory path.
[Qwen 3.6 27B UD Q6 default preset] outcome — on 2026-04-25, the repo’s default main-model surface was aligned to the current Qwen 3.6 27B UD Q6 fit instead of the stale Gemma fallback. `scripts/serve-main.sh` now defaults to `qwen-3.6/Qwen3.6-27B-UD-Q6_K_XL-unsloth.gguf` with alias `qwen-3.6-27b-unsloth-q6`, `MAIN_CONTEXT=196608`, Q8 KV, `--no-mmap`, the requested sampling defaults, and `--spec-default`; `README.md` now also points its main preset examples at `config/presets/main-qwen-3.6-27b-unsloth-q6.env`. Verification: `bash -n scripts/serve-main.sh` passed, the active env already matched the same preset, and repo grep no longer shows the old Gemma default in those default-facing surfaces. Impact: a bare launcher/default-oriented read of the repo now matches the actual intended main model instead of silently falling back to the old Gemma 31B Q5 setup.
[Qwen 3.6 27B UD Q6 Q8-KV fit threshold] outcome — on 2026-04-25, the live Qwen preset was switched from `q4_0/q4_0` KV to `q8_0/q8_0`, which immediately caused spill at the full `262144 / np 1` shape (`CPU KV buffer size = 544.00 MiB`, `CPU RS buffer size = 15.59 MiB`). A reduction to `229376` removed the CPU KV spill but still left the model short of a full-GPU fit (`offloaded 62/65 layers to GPU`, `CPU RS buffer size = 9.35 MiB`). Sweeping downward showed that `196608 / np 1` is the highest clean fit for the current Qwen 3.6 27B UD Q6 service with Q8 KV: startup logs show `offloaded 65/65 layers to GPU`, `Vulkan0 KV buffer size = 6528.00 MiB`, and `Vulkan0 RS buffer size = 149.62 MiB` with no `CPU KV buffer size` or `CPU RS buffer size` lines. Pushing back up to `212992` broke the fit again (`offloaded 64/65 layers to GPU`, `CPU RS buffer size = 3.12 MiB`). Impact: `196608` is the practical sweet spot for keeping all layers, KV, and recurrent state on the GPU side in this Q8-KV configuration, though `llama.cpp` still uses a `Vulkan_Host model buffer size = 1288.28 MiB`, so this is not a literal zero-host-memory path.
[Mesa state audit] outcome — on 2026-04-24, the local Mesa/RADV stack was audited rather than changed because the active Vulkan path is already on AMD’s newer userspace (`Mesa 26.0.0-devel`, `mesa-amdgpu-vulkan-drivers 1:26.0.0.70201-2303469.24.04`) and `apt-get update` plus `apt-cache policy` showed installed = candidate for the AMD Mesa packages. AMD’s `graphics/latest` Noble repo was also checked directly and it currently exposes the same package versions, so there is no newer Mesa build available to install from the configured AMD channel today. The remaining Ubuntu Mesa packages (`libegl-mesa0`, `libgl1-mesa-dri`, `libglx-mesa0`, `mesa-va-drivers`, `mesa-vdpau-drivers`, `mesa-libgallium`) were not removed because they are still tied to generic `libegl1`, `libglx0`, `xserver-xorg-core`, `va-driver-all`, and `vdpau-driver-all` dependencies/recommends rather than being clean dead duplicates. Impact: there was no Mesa upgrade to apply, and the mixed Mesa package set should be treated as a cautious cleanup problem rather than a straightforward package removal task.
[Sarvam 30B GGUF load failure] outcome — on 2026-04-24, the newly downloaded `models/sarvamai.sarvam-30b.f16.gguf.Q6_K.gguf` was identified as the intended “new Indian model”, but current `llama.cpp` could not load it. Startup failed repeatedly with `error loading model architecture: unknown model architecture: 'sarvam_moe'`, even on the latest local Vulkan build (`b480-8bccdbbff`). The attempted temporary preset was then removed and the active main service was restored to the existing `qwen-3.6-27b-unsloth-q6` preset. Verification: `8091` health returned after the restore; the failing logs explicitly show `general.architecture = sarvam_moe`; and the broken Sarvam preset file was deleted so it does not linger as stale config. Impact: this Sarvam GGUF is not usable on the current local `llama.cpp`; testing it would require upstream support for the `sarvam_moe` architecture or a different runtime that already supports it.
[Qwen 3.6 27B UD Q6 restored to 256k] outcome — on 2026-04-22, after a brief 128k containment test, the live `qwen-3.6-27b-unsloth-q6` preset was restored to the preferred single-slot `256k` shape at the user’s request. Verification: the retained preset again sets `MAIN_CONTEXT=262144`, the reloaded service health came back on `8091`, and the user-facing intent is back to “accept the extra host-memory cost and keep the larger context”. Impact: the live service is once again optimized for maximum single-conversation context rather than host-RAM containment.
[Qwen 3.6 27B UD Q6 context reduction] outcome — on 2026-04-22, the live `qwen-3.6-27b-unsloth-q6` preset was reloaded from single-slot `256k` down to single-slot `128k` at the user’s request to reduce host RAM and CPU activity. The retained preset `config/presets/main-qwen-3.6-27b-unsloth-q6.env` now sets `MAIN_CONTEXT=131072` while keeping `-np 1`, the requested sampling defaults, and `--spec-default`. Verification: the live command line now includes `-c 131072 -np 1`; `/props` now reports `n_ctx = 131072`; and the freshly restarted service shows markedly lower early resident memory (`5.8 GiB` in `systemctl --user status` while the new 128k instance was coming up) than the bloated `256k` run that had climbed to roughly `9.5 GiB+` host RAM during use. Impact: the main service now uses the smaller single-slot `128k` context as the first containment step; if host-memory growth still proves annoying under real use, the next lever to test is prompt-cache reduction or disabling rather than further context reshaping alone.
[llama.cpp b480 plus --spec-default on Qwen 3.6 27B UD Q6] outcome — on 2026-04-22, the local `~/.local/src/llama.cpp` checkout was fast-forwarded from `52f1096f2` to `8bccdbbff` and the active Vulkan binary was rebuilt successfully to `llama-server version 480 (8bccdbbff)`. The rebuilt binary now exposes `--spec-default`, so the live `qwen-3.6-27b-unsloth-q6` preset was reloaded first without the flag for a two-run baseline and then with `--spec-default` added to `config/presets/main-qwen-3.6-27b-unsloth-q6.env`. Baseline summarization decode landed at `21.85` and `21.84 tok/s` with no speculative statistics line. With `--spec-default`, startup logs show `speculative decoding will use checkpoints` plus `initialized ngram_mod with n=24`; run 1 stayed neutral at `21.83 tok/s` with zero drafts, but run 2 improved to `23.31 tok/s` with real speculation (`draft acceptance rate = 1.00000`, `24 accepted / 24 generated`, `#gen drafts = 1`, `#acc drafts = 1`, `#gen tokens = 64`, `#acc tokens = 24`). Impact: `--spec-default` is now kept live on the 27B UD Q6 preset because it produced an actual warm-run decode win on a practical summarization task rather than just synthetic benchmark noise.
[Qwen 3.6 27B preset cleanup] outcome — on 2026-04-22, after confirming the UD Q6 build fits fully on GPU at the full single-slot `256k` shape, the no-longer-needed retained preset `config/presets/main-qwen-3.6-27b-unsloth-q5.env` was deleted at the user’s request. Verification: `config/presets` now retains only `main-qwen-3.6-27b-unsloth-q6.env` for the 27B Qwen 3.6 line, and the active `8091` service remains healthy. Impact: the preset surface now matches the intended steady-state choice instead of carrying an unused fallback entry.
[Qwen 3.6 27B UD Q6 main swap] outcome — on 2026-04-22, the live main backend was upgraded from the Unsloth UD Q5 build to `Qwen3.6-27B-UD-Q6_K_XL-unsloth.gguf` while preserving the same requested sampling defaults and the single-slot `256k` context shape. A clean new retained preset `config/presets/main-qwen-3.6-27b-unsloth-q6.env` was added and the original Q5 preset was restored under its correct name so both variants remain explicitly reloadable. Verification: `./scripts/load-main-preset.sh qwen-3.6-27b-unsloth-q6` completed successfully; `/props` now reports alias `qwen-3.6-27b-unsloth-q6` with `n_ctx = 262144`; startup logs show `offloaded 65/65 layers to GPU`, `Vulkan0 model buffer size = 23150.09 MiB`, `Vulkan0 KV buffer size = 4608.00 MiB`, `Vulkan0 RS buffer size = 149.62 MiB`, and `n_ctx_seq = 262144`; and the share proxy on `8095` is still healthy. Impact: the denser UD Q6 build fit fully on GPU at the full single-slot `256k` context, so no context reduction was needed.
[Qwen 3.6 27B context reshape] outcome — on 2026-04-22, after the initial 27B swap came up at `262144 / np 2` (effective `131072` per slot), the retained preset `config/presets/main-qwen-3.6-27b-unsloth-q5.env` was updated from `-np 2` to `-np 1` and reloaded so one conversation can use the full `256k` context. Verification: the live `localllm-main.service` command line now includes `-c 262144 -np 1`, `/props` now reports `n_ctx = 262144`, and the model still fits fully on GPU with `offloaded 65/65 layers to GPU`. Impact: the main Qwen 3.6 27B service now trades the second slot for a full single-slot `256k` context while keeping the same model and sampling defaults.
[Qwen 3.6 27B main swap] outcome — on 2026-04-22, the live main backend on `8091` was switched from `qwen-3.6-35b-a3b-hauhau-q6` to the newly downloaded Unsloth `Qwen3.6-27B-UD-Q5_K_XL-unsloth.gguf` with its matching `Qwen3.6-27B-mmproj-F16-unsloth.gguf`. A retained preset was added at `config/presets/main-qwen-3.6-27b-unsloth-q5.env`, then `./scripts/load-main-preset.sh qwen-3.6-27b-unsloth-q5` reloaded the managed main service successfully. The requested generation defaults are live on `/props`: `temperature=0.6`, `top_p=0.95`, `top_k=20`, `min_p=0.0`, `presence_penalty=0.0`, and `repeat_penalty=1.0`; the existing `262144 / np 2` shape also fit cleanly without adjustment. Verification: `/props` now reports alias `qwen-3.6-27b-unsloth-q5` and the new model path; startup logs show `offloaded 65/65 layers to GPU` with `Vulkan0 model buffer size = 18265.89 MiB`, `Vulkan0 KV buffer size = 4608.00 MiB`, and `Vulkan0 RS buffer size = 299.25 MiB`; `localllm-main.service` is healthy on `8091`; and the share proxy on `8095` is still healthy. Impact: the system is now serving the denser Qwen 3.6 27B checkpoint at the full prior context/slot shape while keeping the same local and shared client surfaces unchanged.
[Qwen 3.6 speculative checkpointing two-run repeat] outcome — on 2026-04-21, the checkpoint-backed speculative test was repeated on the live Qwen 3.6 Hauhau Q6 service using a deliberately repetitive prompt (`alpha beta gamma delta` repeated) and two runs per setting to separate cold-start effects from real speculation. Baseline decode landed at `71.59` then `74.49 tok/s`. `ngram-mod` (`n=24`, `draft-min=48`, `draft-max=64`) landed at `79.81 tok/s` on run 1 with zero drafts, then `66.77 tok/s` on run 2 while finally generating and accepting drafts (`draft_n=21`, `draft_n_accepted=21`, `#gen drafts = 1`, `#acc drafts = 1`, `#gen tokens = 64`, `#acc tokens = 21`). `ngram-map-k` (`n=12`, `m=48`, `min-hits=1`, same draft bounds) landed at `75.40` then `75.79 tok/s`, but both runs still reported `#gen drafts = 0` and `#acc drafts = 0`. Impact: the latest checkpoint-capable `llama.cpp` build can now speculate on this Qwen path in at least some repetitive cases, but the only setting that actually drafted (`ngram-mod`) was slower when it did so; no speculative flags were kept live because there is still no clear production win.
[llama.cpp speculative checkpointing A/B on Qwen 3.6 Q6] outcome — on 2026-04-21, the local `~/.local/src/llama.cpp` checkout was fast-forwarded from `aa00911d1` to `52f1096f2` and the active Vulkan binary was rebuilt to `llama-server version 463 (52f1096f2)` after installing the newly required `spirv-headers` package. The live `localllm-main.service` was restarted successfully on the rebuilt binary and the Qwen 3.6 Hauhau Q6 main model now logs `speculative decoding will use checkpoints` automatically because its context does not support partial sequence removal. A small A/B was then run on the live `262144 / np 2` setup using the same fixed chat prompt with baseline vs `ngram-mod` (`MAIN_SPEC_NGRAM_SIZE_N=24`, `MAIN_DRAFT_MIN=48`, `MAIN_DRAFT_MAX=64`) vs `ngram-map-k` (`MAIN_SPEC_NGRAM_SIZE_N=12`, `MAIN_SPEC_NGRAM_SIZE_M=48`, `MAIN_SPEC_NGRAM_MIN_HITS=1`, same draft bounds). Results: baseline `79.21` prompt tok/s and `71.85` decode tok/s; `ngram-mod` `272.39` prompt tok/s and `72.85` decode tok/s; `ngram-map-k` `281.53` prompt tok/s and `74.32` decode tok/s. However, both speculative modes reported `#gen drafts = 0` and `#acc drafts = 0`, so there was no real speculative work happening on the test request; the decode deltas are effectively noise while prompt-speed changes mostly reflect post-load warm state rather than a real win. One additional constraint surfaced during cleanup attempts: `POST /slots/{id}?action=erase` currently returns `501` on this multimodal Qwen service with `This feature is not supported by multimodal`, so slot erase is not available here. Impact: the host is now on the latest rebuilt `llama.cpp` with checkpoint-capable speculative infrastructure, but no speculative flags were kept live because the tested self-spec modes produced zero drafts on the actual Qwen 3.6 workload.
[Qwen 3.6 Hauhau Q6 main swap] outcome — on 2026-04-18, the live Qwen 3.6 main backend was switched from the Hauhau Q5 build to the newly downloaded Hauhau Q6 build while keeping the same unauthenticated `8091` main path and authenticated share proxy on `8095`. A new retained preset `config/presets/main-qwen-3.6-35b-a3b-hauhau-q6.env` was added for `qwen-3.6/Qwen3.6-35B-A3B-Uncensored-HauhauCS-Aggressive-Q6_K_P.gguf` plus the existing `mmproj`, using the same `262144` total-context, `-np 2`, and `--no-mmap` profile as the Q5 preset, then `./scripts/load-main-preset.sh qwen-3.6-35b-a3b-hauhau-q6` was run successfully. Verification: `/props` now reports alias `qwen-3.6-35b-a3b-hauhau-q6`; `http://127.0.0.1:8091/slots` still shows two idle slots at `131072` each; load logs show `offloaded 41/41 layers to GPU` with `Vulkan0 model buffer size = 28335.00 MiB` and `Vulkan_Host model buffer size = 883.98 MiB`; `./scripts/status.sh` shows all services healthy; and authenticated chat through `http://127.0.0.1:8095/v1/chat/completions` still returns `200`. Impact: the system is now serving the heavier Hauhau Q6 checkpoint through the same dual-front-door arrangement, so local apps and the friend harness can test it without any client-side changes.
[Reranker physical batch size increase] outcome — on 2026-04-17, the reranker 500s were traced to the server-side physical batch limit rather than random instability. The live logs showed `error: input (143 tokens) is too large to process. increase the physical batch size (current batch size: 128)` before `POST /v1/rerank` returned `500`, so the managed reranker config was updated from `RERANK_UBATCH=128` and `-b 256` to `RERANK_UBATCH=512` and `-b 512` in both `config/localllm-reranker.env` and `scripts/serve-reranker.sh`. Verification: `bash -n scripts/serve-reranker.sh` passed, `localllm-reranker.service` restarted healthy, and the live command line now shows `-ub 512 ... -b 512`. Impact: the reranker has materially more token headroom for large candidate inputs, so Wendy should stop hitting the known `current batch size: 128` failure mode on moderate over-limit requests.
[Qwen 3.6 main plus authenticated share proxy] outcome — on 2026-04-17, the Gemma main and share-model services were replaced with a single-backend Qwen 3.6 arrangement so local apps could stay on unauthenticated `8091` while the friend-facing share URL kept the same bearer-key contract. A new retained preset `config/presets/main-qwen-3.6-35b-a3b-hauhau-q5.env` was added for `qwen-3.6/Qwen3.6-35B-A3B-Uncensored-HauhauCS-Aggressive-Q5_K_P.gguf` plus its `mmproj`, using total context `262144` and `-np 2` with `--no-mmap`. The share service was generalized by adding `scripts/serve-share.sh` and `scripts/share_proxy.py`; `systemd/localllm-share.service` now runs the wrapper, and `config/localllm-share.env` is currently in `SHARE_MODE=proxy` forwarding mode with `PROXY_UPSTREAM_URL=http://127.0.0.1:8091` and the existing bearer key. Verification: `./scripts/load-main-preset.sh qwen-3.6-35b-a3b-hauhau-q5` succeeded; `http://127.0.0.1:8091/slots` now shows two idle slots at `131072` each; `http://127.0.0.1:8095/health` returns `ok`; unauthenticated local chat on `8091` works; unauthenticated chat on `8095` returns `401 {"error":"Invalid API Key"}`; authenticated chat on both `http://127.0.0.1:8095/v1/chat/completions` and `https://jhome.taila1332f.ts.net/v1/chat/completions` returns `200`; and `tailscale serve status` still shows the Funnel URL proxying to `127.0.0.1:8095`. Impact: there is now only one loaded Qwen 3.6 model instance, but both the local app path and the authenticated share path remain unchanged for clients.
[Model inventory cleanup after local model pruning] outcome — on 2026-04-17, before the later Qwen 3.6 re-download, the repo launcher surface was reconciled against the then-cleaned `models/` tree. The absent Qwen 3.6 retained presets were deleted at that point, while the surviving Qwopus Q6 preset was corrected rather than removed because its model still exists on disk under `models/qwen-3.5/`; its preset path now points at `qwen-3.5/Huihui-Qwopus3.5-27B-v3-abliterated-Q6_K-mradermacher.gguf` plus the matching `mmproj`. `scripts/serve-main.sh` fallback defaults and `README.md` were also updated to the actual main stack at that time (`gemma-4-31b-q5` live, Gemma Q5/Q6 plus Qwopus Q6 retained). Verification: `bash -n` passes on the touched launcher scripts and `./scripts/list-presets.sh` reported exactly three retained presets, all with `status=ok`, before the later Qwen 3.6 preset was reintroduced. Impact: that cleanup removed stale launcher references left behind by the temporary absence of the Qwen 3.6 files, while preserving the still-valid Qwopus preset.
[Gemma 31B Q5 main with partial-GPU share restore] outcome — on 2026-04-17, the `gemma-4-31b-q6` main model was replaced with `gemma-4-31b-q5` and the shared Gemma service was restored from CPU-only back to the mixed Vulkan split. `config/localllm-share.env` was returned to `MAIN_DEVICE=` with `MAIN_GPU_LAYERS=auto`, `localllm-share.service` was stopped, the main model was switched with `./scripts/load-main-preset.sh gemma-4-31b-q5`, and then the share service was started again. Verification: `/props` now reports main alias `gemma-4-31b-q5`, `./scripts/status.sh` shows all five services healthy, the main Gemma Q5 logs show `offloaded 61/61 layers to GPU` with `Vulkan0 model buffer size = 21112.25 MiB`, `CPU_Mapped model buffer size = 1102.50 MiB`, and the restored share service logs show mixed placement with `CPU_Mapped model buffer size = 15999.57 MiB`, `Vulkan0 model buffer size = 4028.35 MiB`, plus `Vulkan0` KV buffers of `340.00 MiB` and `159.38 MiB`. Impact: the box is now back on the lighter Gemma 31B Q5 main model and can keep the shared Gemma service partially on GPU without sacrificing full GPU residency for the main model.
[Gemma 31B Q6 full-GPU restore with CPU-only share] outcome — on 2026-04-17, the shared Gemma 26B service had to be moved fully onto CPU to let the `gemma-4-31b-q6` main model reclaim full GPU residency. First, reducing the share service to `MAIN_GPU_LAYERS=0` while leaving it on `Vulkan0` improved the main model only from `55/61` to `60/61` layers offloaded, which was still not enough. The decisive fix was updating `config/localllm-share.env` to `MAIN_GPU_LAYERS=0` and `MAIN_DEVICE=none`, then restarting `localllm-share.service` and `localllm-main.service` in that order. Verification: the share service is healthy on `8095` with live args `--device none --gpu-layers 0`, and the reloaded main Gemma 31B Q6 logs now show `offloaded 61/61 layers to GPU` with `Vulkan0 model buffer size = 26231.29 MiB` and only `CPU_Mapped model buffer size = 1428.00 MiB`. Impact: the main `8091` Gemma 31B Q6 service is fully back on GPU, and the shared Gemma endpoint stays online, but it is now intentionally CPU-only to preserve the main model fit.
[Gemma 31B Q6 main restore after Qwen 3.6 test] outcome — on 2026-04-17, after the user finished evaluating the fast Qwen 3.6 Hauhau Q5 main model, the managed main service on `8091` was switched back to the retained `gemma-4-31b-q6` preset with `./scripts/load-main-preset.sh gemma-4-31b-q6`. Verification: `/props` now reports `gemma-4-31b-q6` with the Gemma 31B Q6 model path, `./scripts/status.sh` shows all five services healthy, and current load logs show `offloaded 55/61 layers to GPU` with `Vulkan0 model buffer size = 23708.24 MiB`, `CPU_Mapped model buffer size = 3951.05 MiB`, plus `Vulkan0` KV buffers of `2592.00 MiB` and `303.75 MiB` at `131072 / np 1`. Impact: the box is back on the denser Gemma 31B Q6 main model while keeping the shared Gemma, embedding, reranker, and OmniVoice services up.
[Qwen 3.6 Hauhau no-mmap A/B] outcome — on 2026-04-17, the live `qwen-3.6-35b-a3b-hauhau-q5` main service was re-tested carefully with and without `--no-mmap` after an earlier premature status sample led to a bad first conclusion. Using the same cold-restart timeline and the same fixed local `Reply with exactly: OK` probe on both branches: `--no-mmap` reached health in about `25.7s`, cgroup memory settled around `7.76 GiB`, process `VmRSS` stayed around `826400 kB`, and the probe returned about `143.60 tok/s` prompt processing and `77.84 tok/s` decode; mmap reached health in about `26.7s`, cgroup memory settled around `8.63 GiB`, process `VmRSS` rose to about `26066060 kB`, and the same probe returned about `149.07 tok/s` prompt processing and `72.24 tok/s` decode. The mmap branch also restored the large `CPU_Mapped model buffer size = 26320.66 MiB` behavior, while the `--no-mmap` branch instead reported `Vulkan_Host model buffer size = 1092.63 MiB`. The active config and retained preset were updated back to include `--no-mmap` after the corrected A/B. Impact: for this Hauhau Q5 build on this host, `--no-mmap` is worth keeping because it materially lowers host-memory footprint and slightly improves decode throughput, even though prompt processing is a bit lower and the earlier one-shot status sample briefly suggested otherwise.
[Qwen 3.6 Hauhau Q5 main live switch] outcome — on 2026-04-17, the main service on `8091` was switched from the Unsloth `qwen-3.6/Qwen3.6-35B-A3B-UD-Q5_K_XL-unsloth.gguf` build to the newly downloaded `qwen-3.6/Qwen3.6-35B-A3B-Uncensored-HauhauCS-Aggressive-Q5_K_P.gguf` build using a new retained preset `config/presets/main-qwen-3.6-35b-a3b-hauhau-q5.env` with the same `131072` / `np 1` serving shape, matching `mmproj`, and unchanged generation defaults. Verification: `./scripts/load-main-preset.sh qwen-3.6-35b-a3b-hauhau-q5` succeeded, `/props` now reports alias `qwen-3.6-35b-a3b-hauhau-q5` and the Hauhau model path, and the service is healthy on `8091` while the shared Gemma service remained untouched. Load logs show `offloaded 41/41 layers to GPU`, `Vulkan0 model buffer size = 25625.89 MiB`, `Vulkan0 KV buffer size = 720.00 MiB`, `Vulkan0 RS buffer size = 62.81 MiB`, and `Vulkan0 compute buffer size = 753.28 MiB`, but also a much larger `CPU_Mapped model buffer size = 26320.66 MiB` than the prior Unsloth Q5 build. Impact: the Hauhau Q5 is now the live main model for testing, but its substantially larger CPU-mapped weight footprint may affect latency or host-memory behavior versus the Unsloth build despite the same nominal context and GPU execution shape.
[Qwen 3.6 35B A3B Q5 main plus shared Gemma fit] outcome — on 2026-04-16, the current Gemma 31B Q6 main model and the shared Gemma 26B service were both stopped, a new retained preset `config/presets/main-qwen-3.6-35b-a3b-q5.env` was added for `qwen-3.6/Qwen3.6-35B-A3B-UD-Q5_K_XL-unsloth.gguf` plus its `mmproj`, and the main service was switched to that preset successfully at `131072` / `np 1`. Verification: `./scripts/load-main-preset.sh qwen-3.6-35b-a3b-q5` succeeded, the live main service on `8091` now runs alias `qwen-3.6-35b-a3b-q5`, `/props` reports the Qwen 3.6 model path and `131072` context, and load logs show `offloaded 41/41 layers to GPU` with `Vulkan0 model buffer size = 24834.80 MiB`, `Vulkan0 KV buffer size = 720.00 MiB`, `Vulkan0 RS buffer size = 62.81 MiB`, `Vulkan0 compute buffer size = 753.28 MiB`, and only `CPU_Mapped model buffer size = 515.31 MiB`. The shared Gemma 26B service was then restarted unchanged and came back healthy on `8095`; its coexistence placement shifted to `CPU_Mapped model buffer size = 15999.57 MiB`, `Vulkan0 model buffer size = 2621.72 MiB`, plus `Vulkan0` KV buffers of `340.00 MiB` and `159.38 MiB`. Impact: the host can now run `qwen-3.6-35b-a3b-q5` as the `128k / np1` main service while still keeping the authenticated shared Gemma 26B service online, though the share model now gets less GPU residency because the Qwen main load consumes more VRAM.
[Gemma 31B Q6 main plus full shared Gemma fit] outcome — on 2026-04-16, the current Gemma 31B Q5 main model and the shared Gemma 26B service were both stopped, a new retained preset `config/presets/main-gemma-4-31b-q6.env` was added for `gemma-4/Huihui-gemma-4-31B-it-abliterated-v2-UD-Q6_K_XL.gguf` at `131072` / `np 1`, and the main service was switched to the new Q6 preset successfully. Verification: `/slots` on `8091` reports one idle `131072` slot for alias `gemma-4-31b-q6`, load logs show `offloaded 61/61 layers to GPU` with `Vulkan0 model buffer size = 26231.29 MiB`, `Vulkan0 KV buffer size = 2880.00 MiB` plus `337.50 MiB` SWA KV, and only `CPU_Mapped model buffer size = 1428.00 MiB`. The shared Gemma 26B service was then restarted unchanged and the original `32768` / `np 1` shape came back healthy on the first try, so no share downtuning was required. Impact: the host can run the heavier `gemma-4-31b-q6` main service at `128k / np1` while still keeping the full existing shared Gemma 26B service on `8095`.
[Gemma 31B Q5 prompt cache re-enabled] outcome — on 2026-04-16, during the temporary Gemma 31B Q5 main phase, the Q5 config and retained preset were restored to `MAIN_CACHE_PROMPT=true` and `MAIN_CACHE_RAM=8192`, then `localllm-main.service` was restarted successfully. Verification: the live main process on `8091` at that point ran with `--cache-prompt --cache-ram 8192` on the `gemma-4-31b-q5` alias. Impact: this established the cache-on Q5 baseline before the later Q6 switch replaced it.
[Gemma 31B Q5 prompt cache disabled] outcome — on 2026-04-16, during the temporary Gemma 31B Q5 main phase, the Q5 config and retained preset were updated to disable prompt caching by setting `MAIN_CACHE_PROMPT=false` and `MAIN_CACHE_RAM=0`, then `localllm-main.service` was restarted successfully. Verification: the live main process on `8091` at that point ran with `--no-cache-prompt --cache-ram 0` on the `gemma-4-31b-q5` alias. Impact: this captured the no-cache Q5 behavior before the later cache-on restore and Q6 switch superseded it.
[Gemma 31B Q5 plus shared Gemma coexistence] outcome — on 2026-04-16, after the Gemma 31B Q5 main model was loaded on `8091`, the shared Gemma 26B service on `8095` was started again unchanged and both services stayed healthy. Verification: `http://127.0.0.1:8091/health` and `http://127.0.0.1:8095/health` both returned `ok`, `localllm-main.service` remained active on the Gemma 31B Q5 launch, and `localllm-share.service` resumed successfully on the mixed GPU/CPU Gemma 26B share launch. Impact: this demonstrated that the host could keep the Q5 main load and the shared Gemma 26B service up together before the later Q6 switch.
[Gemma 31B Q5 main live switch] outcome — on 2026-04-16, a new retained preset `config/presets/main-gemma-4-31b-q5.env` was added for `gemma-4/Huihui-gemma-4-31B-it-abliterated-v2-UD-Q5_K_XL.gguf` plus its `mmproj`, using `131072` context, `-np 1`, and fit-oriented `q4_0/q4_0` KV cache types. To free GPU headroom for the first load, the shared Gemma 26B service on `8095` was stopped before switching the main service away from Qwopus Q6. Verification: `./scripts/load-main-preset.sh gemma-4-31b-q5` succeeded, `/slots` now reports one idle slot at `131072`, and load logs show a near-full GPU fit with `offloaded 61/61 layers to GPU`, `Vulkan0 model buffer size = 21112.25 MiB`, `Vulkan0 KV buffer size = 2880.00 MiB` plus `337.50 MiB` SWA KV, and only `CPU_Mapped model buffer size = 1102.50 MiB`. Impact: the main service on `8091` is now `gemma-4-31b-q5` at `128k / np1` and effectively GPU-resident; shared-service coexistence was validated in a follow-up step rather than assumed during the initial fit load.
[Qwen reranker CPU service] outcome — on 2026-04-16, a dedicated CPU-only reranker service was added for `models/embedding/Qwen3-Reranker-4B-Q4_K_M.gguf` using a new `config/localllm-reranker.env`, `scripts/serve-reranker.sh`, and `systemd/localllm-reranker.service`, then enabled as `localllm-reranker.service` on port `8093`. The launcher uses `--embedding --pooling rank --reranking` with `--device none --gpu-layers 0`, so it does not disturb the GPU-backed chat stack. Verification: `systemctl --user status localllm-reranker.service` shows the expected CPU-only command line, `./scripts/status.sh` now reports `ok` on `http://127.0.0.1:8093/health`, and a live `POST /v1/rerank` request returns `200`. Impact: the embedding stack now has a colocated local reranker endpoint at `http://127.0.0.1:8093/v1/rerank` that can be used alongside the existing embedding service on `8092`.
[Repo surface cleanup for current stack] outcome — on 2026-04-14, the repo was trimmed to the current live service layout: stale main presets for missing Gemma/Qwen variants were removed, broken one-off model loader shortcuts were deleted, the obsolete `GPU_ARRIVAL_CHECKLIST.md` handoff was dropped, and top-level helper/docs surfaces were realigned to the active services (`8091` main Qwopus Q6 on GPU, `8092` Qwen embedding on CPU, `8094` OmniVoice on CPU, `8095` shared Gemma 26B mixed GPU/CPU behind Tailscale). `scripts/serve-main.sh` fallback defaults now match the current Qwopus Q6 stack, `scripts/chat.sh` now discovers `8091` and `8095` by default, `scripts/start-stack.sh` / `stop-stack.sh` / `status.sh` now operate on the real `systemd --user` services instead of dead `screen` sessions, benchmark restore/default candidate surfaces now stop pointing at removed 9B presets, and `README.md` now documents the live stack instead of old OmniCoder/9B inventory. Verification: `bash -n` passes on the touched shell/benchmark scripts, `./scripts/list-presets.sh` shows only the retained Qwopus Q6 preset with `status=ok`, `./scripts/status.sh` reports all four services healthy, and `tailscale funnel status` plus public `GET /v1/models` still resolve to the shared Gemma 26B endpoint. Impact: the repo operator surface is now materially less confusing and less likely to reload or benchmark against models that are no longer present.
[Gemma 26B no-mmap A/B] outcome — on 2026-04-14, the shared Gemma 26B A4B service was A/B tested with and without `--no-mmap` using authenticated local probes after clean restarts. Baseline restart-to-health was about `12.6s`, while `--no-mmap` took about `29.9s`; baseline medium-prompt decode held about `24.35–24.48 tok/s`, while the `--no-mmap` branch was worse overall at about `23.50–23.53 tok/s` with one major outlier at `15.33 tok/s`. The share config was restored to the baseline `mmap` setup and the service restarted healthy. Impact: for this mixed CPU/Vulkan 26B share model on this host, `--no-mmap` is not a win and should stay off unless future upstream/runtime changes alter the tradeoff.
[Gemma 26B shared public load] outcome — on 2026-04-14, the shared service on `8095` was switched from Gemma E2B to the actual on-disk `gemma-4/Huihui-gemma-4-26B-A4B-it-abliterated-i1-Q4_K_M.gguf` file, keeping the existing bearer key and using a conservative `32768` / `np 1` launch shape with `--gpu-layers auto --fit on`. A stale preset path to a non-existent `MXFP4_MOE` file was discovered and bypassed, and there is no matching 26B `mmproj` on disk so the shared 26B service is text-only. Verification: `localllm-share.service` is healthy on `8095` with alias `gemma-4-26b-a4b-i1-q4-share`, load logs show mixed placement (`CPU_Mapped model buffer size = 15999.57 MiB`, `Vulkan0 model buffer size = 4305.18 MiB`), `localllm-main.service` remains healthy on `8091`, and Tailscale Funnel now proxies `https://jhome.taila1332f.ts.net/` to `127.0.0.1:8095`. Public `GET /v1/models` returns the Gemma 26B alias, while unauthenticated `POST /v1/chat/completions` still returns `401`, so the existing share API key remains required for chat. Impact: the friend harness can now hit the larger Gemma 26B share model through the existing public URL without displacing the private Qwopus main service.
[Tailscale Funnel moved to main Qwopus] outcome — on 2026-04-14, the existing public Tailscale URL `https://jhome.taila1332f.ts.net/` was moved from the shared Gemma backend on `127.0.0.1:8095` to the main Qwopus backend on `127.0.0.1:8091`. Verification: `tailscale funnel status` and `tailscale serve status` both now show `/ proxy http://127.0.0.1:8091`, direct `curl` to `https://jhome.taila1332f.ts.net/v1/models` returns the Qwopus Q6 model list, and the shared Gemma service on `8095` remains locally healthy. Impact: the friend harness will now hit the main Qwopus model through the existing Tailscale URL while the Gemma share service remains available only on its local port unless the Funnel mapping is changed again.
[Qwen3.5 self-spec rollback] outcome — after the no-gain A/B check, the active Qwen Q6 preset and `config/localllm-main.env` were reverted to the plain launch path by removing `MAIN_SPEC_TYPE`, `MAIN_SPEC_NGRAM_SIZE_N`, `MAIN_DRAFT_MIN`, and `MAIN_DRAFT_MAX`. Verification: `bash -n` still passes on the launcher scripts, `localllm-main.service` restarted healthy on `8091`, and the live `llama-server` process args no longer include speculative-decoding flags. Impact: bot traffic is back on the simpler baseline, while the launcher still supports speculative env keys for future upstream retests.
[Qwen3.5 self-spec A/B probe] outcome — on 2026-04-13, a controlled restart-and-restore comparison on the live Qwen Q6 main service showed effectively identical short-request decode throughput with and without `ngram-mod` self-speculative flags. Measured `predicted_per_second` over five `Reply with exactly OK.` probes was about `27.75 tok/s` with `--spec-type ngram-mod --spec-ngram-size-n 24 --draft-min 48 --draft-max 64` versus about `27.71 tok/s` without them, and `/metrics` exposed no speculative-specific counters to justify leaving the mode enabled. Impact: the current self-spec configuration is not buying a visible speedup on this workload, so the simplest default is to revert unless future testing targets a different prompt shape or a newer upstream implementation.
[Qwen3.5 self-spec main-service rollout] outcome — the managed Qwen Q6 main bot now launches `llama.cpp` self-speculative decoding via `--spec-type ngram-mod --spec-ngram-size-n 24 --draft-min 48 --draft-max 64`, wired through new generic launcher env keys instead of preset-specific hardcoding. Verification: `bash -n` passes on the launcher scripts, `localllm-main.service` restarted healthy on `8091`, and the live process args show the speculative flags on the Qwen Q6 service. Impact: bot traffic can now exercise self-speculative decoding immediately, while other presets remain unchanged until they opt in with the same env keys.
[Qwen3.5 speculative decoding on llama.cpp] outcome — as of 2026-04-13, upstream `llama.cpp` exposes generic speculative decoding via a draft model (`-md`) and self-speculative n-gram modes, but the official Qwen3.5 docs recommend native MTP only through SGLang or vLLM and no upstream llama.cpp source found during this check documents Qwen3.5 MTP/NEXTN support. Impact: for Qwen3.5 on llama.cpp, treat speculative decoding as draft-model/ngram-only today; do not assume the model’s built-in MTP head is usable there.
[llama.cpp server cache_n=0 conditions] outcome — on current local `llama.cpp` server code, repeated identical `/v1/chat/completions` can still report `timings.cache_n=0` when slot reuse is bypassed (most commonly via slot routing) even with `--cache-prompt` and `--cache-ram` enabled. In `tools/server/server-context.cpp`, `cache_n` is assigned from `n_past`, and `n_past` stays zero whenever the selected slot has no matching prefix, request-level `cache_prompt` is false, cache is cleared on LoRA change, or slot selection never returns to the previously primed slot. Impact: the strongest likely root cause for repeated identical requests is slot-selection behavior (`id_slot`/LRU/similarity settings) rather than cache flags alone.
[Retained Qwen 3.5 9B set] outcome — the active local 9B comparison set is now intentionally narrow: `qwen-3.5-abl` (Huihui Q4), `qwen-3.5-g` (Gemini-distilled Q4), and `qwen-3.5-unsloth`. Impact: repo notes and benchmark summaries should treat removed 9B variants as archived history, not live contenders.
[Benchmark execution sequence] outcome — the canonical evaluation flow for candidate models on this host is: optimize tunings first, then run speed tests on the chosen launch profile, then run the full tests/benches suite, and only then run barrage/diagnostic prompts. Impact: future model passes should not jump straight into replay/sim/barrage before a candidate has a known-good launch profile.
[Benchmark isolation constraint] outcome — before loading any model for a scratch benchmark pass, unload whatever GPU-backed chat model is already loaded. The managed preset loader already has an unload-if-loaded behavior because `scripts/load-main-preset.sh` always stops `localllm-main.service` before starting the requested preset, and the scratch benchmark entrypoints now enforce the same rule by unloading the managed main service before starting temp candidate servers. Impact: future tests should not rely on memory or manual discipline alone to avoid multi-model GPU overlap.
[Launcher and benchmark surface hardening] outcome — the preset launcher path now validates preset model/mmproj existence before touching `config/localllm-main.env`, and `scripts/load-main-preset.sh` will restore the prior active preset if a restart or health check fails after a valid switch attempt. The active preset inventory was trimmed to the retained 9B set by removing the stale OmniCoder and Q6 preset files, `scripts/list-presets.sh` now reports preset status instead of blindly advertising every file as loadable, and the ambiguous `scripts/load-qwen-3.5.sh` shortcut now warns that it targets the Unsloth preset. The benchmark compare defaults were also retargeted to the retained 9B models (`qwen-3.5-abl`, `qwen-3.5-g`, `qwen-3.5`), and the compare barrage now defaults to uncapped prompts instead of reasoning-budget caps. Finally, the launcher/benchmark script layer no longer hardcodes `CUDA0` when no explicit device is set; current behavior is unchanged on this host because the live presets still set `MAIN_DEVICE=CUDA0`, but future presets can switch GPU backends without fighting a script-level default. Verification: `bash -n` passes on the touched scripts, `./scripts/list-presets.sh` now shows only the retained presets with `status=ok`, and `./scripts/load-main-preset.sh qwen-3.5-abl` still reloads the live service cleanly. Impact: the launcher path is now safer against stale presets, and the default benchmark entrypoints are aligned with the current retained model set and less brittle ahead of the GPU swap.
[Benchmark runner hardening] outcome — the benchmark runners now support truly serial candidate passes without reloading a managed chat model between suites unless explicitly requested, and the top-level model-eval wrapper now calls the repo-local suite paths while exporting `PYTHONPATH` so the `benchmarks.*` imports resolve consistently. The wrapper’s remaining live-eval gaps were also closed during the baseline-vs-Gemini rerun: barrage now dispatches through `benchmarks/agentic_barrage.sh` instead of a non-existent `scripts/` path, and replay/sim/opencode suite selection now follows the injected candidate alias so non-baseline candidates write correct summary rollups. The prompt-only diagnostics also stop defaulting to `thinking_budget_tokens` caps: `agentic_barrage.sh` and `final_decision_round.sh` now default to `uncapped`, and `coding_compare.sh` no longer injects a default thinking budget unless one is requested explicitly. Verification: `bash -n` passes on the updated benchmark shells, and the corrected wrapper completed clean barrage/model-eval passes on `qwen-3.5-abl` and `qwen-3.5-g`. Impact: future candidate evaluations should no longer accidentally co-load a restored baseline between suites, and the default diagnostic stack now aligns with the standing repo rule that reasoning/token caps distort model comparisons unless the explicit goal is truncation testing.
[Baseline vs Gemini Q4 rerun] outcome — the hardened one-model-at-a-time sequence was rerun on `qwen-3.5-abl` and `qwen-3.5-g`, including tuning/speed checks plus replay, sim, and uncapped barrage. Tuning: both fit the standard `131072` no-warmup multimodal profile cleanly on this host, and both now spend capped micro-probes mostly on hidden reasoning, so those probes should be treated as throughput-only. The baseline still ran slightly faster on the speed sweeps: about `77.7 tok/s` exact-`OK` and `75.2 tok/s` short-answer on the chosen profile, versus Gemini at about `73.8 tok/s` and `73.1 tok/s`. Replay: they tied on total matched turns at `29/35`, but Gemini was materially faster overall (`83.99s` vs `107.03s`) and cleaner on `soak_real` (`15/19` vs `14/19`), while the baseline kept the edge on `retry_tool_followthrough` (`2/2` vs `1/2`). Sim: both landed at `7/8` pass and `6/8` scope-clean, failing only `session_store_exploration` and over-editing `flush_report_two_file_fix`, but Gemini was much faster (`159.64s` vs `280.89s`) and tool-clean on all `8/8` scenarios versus the baseline’s `7/8` because `retry_bugfix` incurred one tool-format error. Barrage: both completed the uncapped scenario set at roughly the same token-speed band, but Gemini spent far less hidden reasoning on the plain planning/revision prompts while keeping comparable outward quality; across `plan_then_revise`, `review_then_retry`, and `evidence_triage`, Gemini emitted about `39k` hidden-reasoning characters versus the baseline’s roughly `94k`. Impact: the current default baseline still has the cleaner general-agent `retry_tool_followthrough` behavior and remains a defensible default, but the Gemini-distilled Q4 rerun now looks like the stronger coding-side alternate on this host because it matches replay totals, ties sandbox score, and gets there much faster with cleaner tool execution.
[Benchmark score sheet] outcome — `benchmarks/BENCHMARK_RESULTS.md` is the canonical score sheet, and it has been trimmed to the retained Qwen 3.5 9B set instead of carrying stale 9B candidate noise. Impact: future comparisons can read one short file for the active baseline, the active Gemini-distilled alternate, and the current Unsloth status.
[Benchmark runner invocation rule] outcome — the top-level candidate wrapper is usable again for live evaluations. `benchmarks/run_model_eval.sh` now dispatches to the repo-local suite scripts, carries `PYTHONPATH` into the suite runners, routes barrage through the benchmark script path, and passes the active candidate alias into replay/sim/opencode summaries so non-baseline reruns are not silently mis-labeled. Impact: future candidate passes can use `run_model_eval.sh` as the canonical serial entrypoint again instead of working around it manually.
[Baseline] outcome — `qwen-3.5-abl` is the current default baseline on this host and should be treated as the standard every future retained 9B candidate must beat in real client behavior, not just fit or token speed. Impact: candidate work should start from direct comparisons against the Huihui Q4 baseline rather than open-ended exploration.
[Qwen baseline launch] outcome — the tested durable `qwen-3.5-abl` launch profile is `-np 1 -tb 8 -b 512 -ub 256 -cram 0 -fa on --threads-http 4 -ctk q4_0 -ctv q4_0 -rea on --metrics --no-warmup --image-max-tokens 12288` at `131072` context with `MAIN_FIT=true` and `MAIN_GPU_LAYERS=auto`. Impact: this is the reference server profile to preserve unless a later tuning pass clearly beats it.
[Current models] outcome — the durable repo-local Qwen 3.5 9B inventory now consists of `qwen-3.5-abl`, `qwen-3.5-g`, and `qwen-3.5-unsloth`, plus the embedding model. Impact: benchmark docs and future benchmark passes should stay focused on those retained 9B checkpoints.
[Unsloth benchmark status] outcome — `qwen-3.5-unsloth` remains staged but does not yet have current replay/sim/barrage evidence in the trimmed score sheet. Impact: it is still an active retained model, but it should be treated as unevaluated against the hardened April benchmark stack until a fresh pass is run.
[27B R9700 prep] outcome — for the AMD Radeon AI PRO R9700 32GB phase, the same benchmark sequence and isolation rules should carry forward to larger models: unload first, tune launch/KV/build choices first, then speed-test, then run the full replay/sim suite, and only then run barrage/secondary diagnostics. The script layer is now less GPU-vendor-specific because launcher/benchmark defaults no longer force `CUDA0` when unset, but any live preset used on the new card will still need an explicit backend/device review when the hardware is installed. Impact: the repo is better prepared for 27B benchmarking on the new GPU, but the actual preset/device fields should still be rechecked against the final AMD runtime.
[GPU arrival checklist] outcome — the persistent cold-start handoff for the R9700 swap now lives in `GPU_ARRIVAL_CHECKLIST.md`, including the canonical unload/tune/speed/suite/barrage sequence, the retained 9B fallback path, the 27B bring-up order, and the reminder that `run_model_eval.sh` covers the suite stage only while tuning/speed remain separate helpers. Impact: future sessions do not need to recover this workflow from memory or from scattered notes.
[Prompt cache policy] outcome — prompt caching is relevant on this stack, but the current client and harness request builders were already mostly cache-friendly: static tool ordering in sim/opencode/barrage, replay fixtures with fixed payloads, and chat clients that preserve message history instead of rewriting prior turns. The main gap was policy ambiguity, so the launcher path now exposes explicit llama-server cache controls for the managed main service (`MAIN_CACHE_PROMPT`, `MAIN_CACHE_REUSE`, `MAIN_SLOT_PROMPT_SIMILARITY`, `MAIN_SLOT_SAVE_PATH`, `MAIN_SLOTS`), while scratch benchmark servers default to `BENCH_CACHE_PROMPT=false` so latency comparisons do not silently inherit cache reuse unless a run opts in deliberately. Verification: `bash -n` passes on `scripts/common.sh`, `scripts/serve-main.sh`, and `benchmarks/common.sh`. Impact: app/bot traffic can benefit from server-side prompt caching, while benchmark runs now have an explicit cache policy instead of relying on implicit server defaults.
[R9700 host check] outcome — the new AMD GPU is physically present on PCIe as device `1002:7551`, `amdgpu` is loaded, and `/dev/kfd` exists, so the kernel sees the card. But this user is not in the `render` group, `/dev/dri` render nodes were not visible in the current shell, and AMD user-space tools like `rocm-smi`, `amd-smi`, `rocminfo`, and `radeontop` were not installed. Impact: the card is detected but the host is not yet ready for normal ROCm/llama.cpp AMD bring-up until group membership and user-space/runtime visibility are fixed.
[R9700 post-reboot state] outcome — the user is now in `video` and `render`, and `/dev/kfd` is visible, so the permissions fix landed. But AMD user-space tools are still absent, `/dev/dri` render nodes were still not visible in this shell, and the currently installed local `llama-server` is a CUDA build linked against `build-cuda` and `libggml-cuda.so`; `--list-devices` reports no available devices now that the NVIDIA card is gone. Impact: the next required work is not model loading or benchmarking yet; it is AMD runtime/tool installation plus a ROCm or Vulkan `llama.cpp` build before the R9700 can be used by this stack.
[R9700 runtime bring-up] outcome — after installing AMD’s `workstation,rocm` stack and rebooting, the host now initializes the R9700 cleanly: `/dev/dri/renderD128` exists, `rocminfo` reports `AMD Radeon AI PRO R9700` as `gfx1201`, `rocm-smi` sees the card, and `vulkaninfo` sees the real RADV GPU instead of `llvmpipe`. Fresh `llama.cpp` backend builds were also configured and built successfully in `~/.local/src/llama.cpp/build-hip` and `~/.local/src/llama.cpp/build-vulkan-r9700`; `llama-server --list-devices` on those builds reports `ROCm0` and `Vulkan0` respectively. Impact: the R9700 is now ready for backend-specific loading and direct HIP-vs-Vulkan tuning/benchmark comparisons, but the repo’s default runtime path still points at the old CUDA build and should not be trusted until an AMD backend is selected deliberately.
[R9700 runtime selector] outcome — the repo launcher path now has a persistent backend selector in `config/localllm-runtime.env`, with `scripts/set-runtime-backend.sh` writing `hip`, `vulkan`, or `cuda` plus the matching default device (`ROCm0`, `Vulkan0`, `CUDA0`). The retained main presets and active env were also cleared of stale `MAIN_DEVICE=CUDA0` pinning so the selected runtime backend can drive device choice. Verification: `load-main-preset.sh` successfully reloaded the live `qwen-3.5-g` service on both `build-hip` and `build-vulkan-r9700`, and the host now ends with the main service live on HIP. Impact: future AMD backend comparisons can switch runtimes cleanly without editing binary paths or preset files by hand.
[llama.cpp prompt-cache caveat] outcome — on the new AMD `llama.cpp` builds, prompt caching is not actually active with the current launch args because the presets still carry `-cram 0`; server logs now say `prompt cache is disabled - use --cache-ram N to enable it`. Impact: the earlier prompt-cache launcher work still matters, but the main service will need a nonzero cache-ram policy before prompt caching is truly enabled on the new runtime.
[R9700 backend control pass] outcome — the first AMD backend control pass was limited to fit and speed only on the retained `qwen-3.5-abl` 9B baseline. On HIP, the standard no-warmup profile fit cleanly through `262144` context and ran at about `74.2 tok/s` at `131072`, `74.7 tok/s` at `196608`, and `75.6 tok/s` at `262144` on the short exact-`OK` probe; the `131072` profile sweep landed at about `74.5 tok/s` exact-`OK` and `73.5 tok/s` on the short direct-answer probe, with no meaningful warmup benefit. On Vulkan, the same profile also fit cleanly through `262144` and was materially faster: about `79.3 tok/s` at `131072`, `84.5 tok/s` at `196608`, and `84.6 tok/s` at `262144`; the `131072` profile sweep landed at about `85.5 tok/s` exact-`OK` and `86.4 tok/s` on the short direct-answer probe, again with no meaningful warmup benefit. Impact: for this first 9B control on this host, Vulkan is the faster backend and should be treated as the likely first candidate for broader tuning, while HIP remains available for follow-up comparisons. Caveat: these probes were fit-and-speed only, not replay/sim behavior tests.
[Benchmark GPU probe fix] outcome — `benchmarks/common.sh` now treats `nvidia-smi` as a valid backend probe only when it can actually enumerate a live NVIDIA GPU; otherwise it falls back to ROCm probing. Impact: future speed sweeps on this AMD-only host will no longer print bogus `NVIDIA-SMI has failed` parse errors in the `MEM` field.
[R9700 27B fit-and-speed pass] outcome — the first 27B backend comparison was run on `Huihui-Qwen3.5-27B-abliterated-i1-Q4_K_M-mradermacher.gguf` with its matching Q8 multimodal projector, limited to tuning-plus-speed only and no suite/barrage. Both backends fit cleanly through `131072` context on the standard `q4_0/q4_0` KV no-warmup profile. Vulkan landed at about `31.9 tok/s` at `32768`, `32.0 tok/s` at `65536`, `32.0 tok/s` at `98304`, and `31.8 tok/s` at `131072`, with the `131072` profile sweep at about `31.4 tok/s` exact-`OK` and `31.2 tok/s` on the direct-answer probe; warmup again had no meaningful benefit. ROCm was clearly slower on the same model and settings: about `26.8 tok/s` at `32768`, `26.8 tok/s` at `65536`, `26.7 tok/s` at `98304`, and `26.8 tok/s` at `131072`, with the `131072` profile sweep at about `26.2 tok/s` exact-`OK` and `25.8 tok/s` on the direct-answer probe. Impact: Vulkan is the faster backend on this host for both the retained 9B control and the first Huihui 27B control, so the live service was restored on Vulkan and Vulkan should be treated as the default first backend for subsequent 27B tuning unless a candidate shows a Vulkan-specific stability or fit problem.
[R9700 27B Vulkan sweep] outcome — the remaining Qwen 3.5 27B Q4/Q6 candidates were run on Vulkan only with the canonical fit-and-speed flow and no suite/barrage. Every tested model fit cleanly through `131072` on the standard no-warmup profile. The Q4 group stayed tightly clustered around `31-32 tok/s`, led by `jackrong-claude-q4` at roughly `32.0 tok/s` on the context sweep and `31.4 tok/s` on the `131072` exact-`OK` probe; `gemini-q4` and `huihui-claude-q4` were essentially tied just behind it, while `unsloth-q4` trailed slightly at about `31.5 tok/s` at `131072`. The Q6 group stayed tightly clustered around `25 tok/s`, with only marginal spread between Huihui, Jackrong, Gemini, and Unsloth variants. Warmup again gave no meaningful speed benefit. Impact: on this R9700 Vulkan stack, Q4 is the practical default quant tier for Qwen 3.5 27B unless a later quality pass proves a Q6 variant materially better.
[Gemma runtime unblock] outcome — the first Gemma 4 31B Vulkan load failed immediately with `unknown model architecture: 'gemma4'`. Root cause was not the GPU stack or the models; the local `~/.local/src/llama.cpp` checkout was stale at commit `568aec82d` even though its cached remote refs looked current. After a real `git pull`, the checkout moved to `b8702` / `c5ce4bc22`, which adds native `gemma4` support and Gemma 4 chat templates. Rebuilding `build-vulkan-r9700` on that refreshed source unblocked Gemma loading. Impact: Gemma 4 benchmarking on this host requires the refreshed Vulkan build; if Gemma loads start failing again, check the local `llama.cpp` revision first instead of assuming a quant or GPU-fit problem.
[Gemma 31B Vulkan sweep] outcome — Gemma 4 31B `Q4_K_M`, `IQ4_NL`, and `UD-Q4_K_XL` were run on Vulkan only with the canonical fit-and-speed flow and no suite/barrage. All three fit cleanly through `131072`. `IQ4_NL` was the fastest at roughly `30.1 tok/s` on the context sweep and `30.2 tok/s` on the no-warmup exact-`OK` probe; `Q4_K_M` was the middle option at roughly `28.4 tok/s` at `131072`; `UD-Q4_K_XL` was slowest overall at roughly `28.0 tok/s` at `131072` and about `27.6 tok/s` on the no-warmup exact-`OK` probe. Warmup again gave no meaningful speed gain. Impact: on this R9700 Vulkan stack, `IQ4_NL` is the strongest Gemma 4 31B speed pick of the downloaded set, and `UD-Q4_K_XL` does not justify itself on speed alone.
[Prompt cache default] outcome — the managed launcher path now enables real prompt caching by default with `MAIN_CACHE_PROMPT=true` and `MAIN_CACHE_RAM=8192`, and the live Gemma preset confirms `prompt cache is enabled` in the server logs. Impact: repeated long-prefix app turns can now benefit from cache reuse without relying on implicit backend defaults.
[Gemma 31B elimination] outcome — the full Vulkan evaluation pass completed for `gemma-4-31b-q4km`, `gemma-4-31b-iq4nl`, and `gemma-4-31b-udq4kxl` across replay, sim, opencode, coding-compare, and barrage. `IQ4_NL` won overall: replay `23/35` with `2/5` fixtures, sim `7/8` pass with `7/8` scope-clean and the fastest elapsed (`1061.47s`), coding-compare about `27.04 tok/s`, and barrage about `27.47 tok/s` with the lightest reasoning load of the set. `Q4_K_M` trailed on replay (`22/35`) and was much slower in sim (`1535.61s`); `UD-Q4_K_XL` tied `IQ4_NL` on replay turns (`23/35`) but only passed `1/5` fixtures, stayed slowest on prompt-only diagnostics, and carried the heaviest barrage reasoning load. Impact: keep `gemma-4-31b-iq4nl` as the default Gemma preset, keep `q4_k_m` only as a control fallback, and eliminate `ud-q4_k_xl` first.
[Gemma loser cleanup and live switch] outcome — the eliminated Gemma loser files (`Q4_K_M` and `UD-Q4_K_XL`) were removed from `models/gemma-4-31b`, their presets were deleted, and a new `qwen-3.5-27b-huihui-q6` preset was added and loaded successfully on Vulkan. Impact: the live service now runs `Huihui-Qwen3.5-27B-abliterated-i1-Q6_K-mradermacher.gguf`, and the active preset surface is trimmed to `gemma-4-31b-iq4nl` plus `qwen-3.5-27b-huihui-q6`.
[Qwen 27B Huihui Q4 live preset] outcome — the Q6 live model was unloaded cleanly, a matching `qwen-3.5-27b-huihui-q4` preset was added, and the service was reloaded onto `Huihui-Qwen3.5-27B-abliterated-i1-Q4_K_M-mradermacher.gguf` on Vulkan with the standard `131072` cached no-warmup profile. Impact: the active surface now includes both Huihui 27B control quants, and the live model is back on the faster Q4 variant.
[Huihui Claude 27B quant A/B] outcome — the full Vulkan quant comparison completed on the Huihui Claude-distill 27B pair. `Q4_K_M` won overall: replay `29/35` with `2/5` fixtures versus `Q6_K` at `28/35` and `1/5`; both quants went `8/8` pass and `8/8` tool-clean on the coding harness, but `Q6_K` only gained a narrow scope edge (`7/8` scope-clean versus `6/8`) by keeping `flush_report_two_file_fix` to the expected files. Prompt-only diagnostics stayed decisively in favor of `Q4_K_M` at about `28.78 tok/s` coding-compare and `28.85 tok/s` barrage, versus `Q6_K` at about `23.20` and `23.32`. Impact: do not carry the full Q6 tier forward by default; keep `Q4_K_M` as the main quant and treat `Q6_K` as, at most, a specialist fallback for scope-sensitive coding tasks.
[Suite manifest cleanup] outcome — the replay, sim, and opencode compare wrappers now build run manifests from the actually injected candidate specs instead of hardcoding stale Qwen defaults into every Gemma run. Impact: future elimination artifacts are cleaner and no longer look like they benchmarked deleted candidates alongside the real target.
[R9700 stack research] outcome — the external R9700 Vulkan/ROCm reports reinforce the current local direction more than they overturn it. Vulkan remains the strongest default for single-GPU dense-model work on this host, while the most relevant future A/Bs are targeted rather than global: test `RADV_PERFTEST=nogttspill` on the Vulkan path, keep `-fa on` as the Vulkan default, and explicitly A/B ROCm with `-fa on/off` because upstream reports still show FA regressions on some ROCm workloads. Also do not chase `-DGGML_HIP_ROCWMMA_FATTN=ON` on this gfx1201 + ROCm 6.4 stack yet because upstream reports include a gfx1201 compile break there. Impact: continue eliminations first, then do narrow backend/driver A-Bs after the candidate set is smaller.
[PP benchmark helper] outcome — there is now a dedicated server-path PP helper at `benchmarks/sweep_pp.sh`. It runs the same scratch-server isolation flow as the other sweep scripts, but measures `prompt_n`, `cache_n`, and `prompt_per_second` from the actual `llama-server` API path while forcing decode spillover down to `max_tokens=1`. Verification: on `qwen-3.5-g` at `131072` Vulkan no-warmup, a repeat-count `64` prompt returned `prompt_n=1227` and `prompt_per_second≈1653`, and repeat-count `256` returned `prompt_n=4875` and `prompt_per_second≈2434`. Impact: future PP comparisons can stay on the real server path instead of relying on `llama-bench`, but `predicted_per_second` from this helper should be treated as decode spillover noise because it only measures a single generated token.
[Qwen 27B Q6 retirement] outcome — all Qwen 27B Q6 GGUF candidates were deleted from `models/qwen-3.5-27b`, and the stale `main-qwen-3.5-27b-huihui-q6.env` preset was removed. Impact: the active Qwen 27B branch is now intentionally Q4-only until a surviving finetune proves a real need for Q6.
[Replay timeout hardening] outcome — `benchmarks/transcript_replay/run_replay.py` now records per-turn HTTP timeout/error summaries and aborts only the affected fixture instead of crashing the entire replay/model-eval run. Impact: one bad replay turn no longer invalidates a whole candidate pass, and elimination summaries can be completed even when a fixture times out.
[Qwen 27B Q4 elimination] outcome — the finished Q4 three-way comparison keeps `huihui-claude27-q4` in first place, `gemini27-q4` in second, and eliminates `jackrong-claude27-q4` first. Evidence: replay `29/35` vs `27/35` vs `23/34`; sim `8/8` vs `7/8` vs `7/8`; prompt-only speed effectively tied, with Gemini much heavier on reasoning volume. Impact: the narrowed active Qwen 27B contenders are Huihui Claude Q4, Gemini Q4, and the earlier Huihui abliterated Q4 reference.
[Run-state reporting correction] outcome — a prior three-way wrapper attempt had already stopped when it was reported as still running. Impact: do not claim a benchmark is active unless the actual exec session, process, or live GPU activity has been verified first.
[Huihui Q4 full reference] outcome — the missing full Vulkan pass on `huihui-q4` completed at `131072` with replay `28/35`, `1/5` replay fixtures, sim `7/8` pass, `7/8` scope, `8/8` tool-clean, coding compare `28.74 tok/s`, and barrage `28.86 tok/s`. Impact: the base Huihui model is now fully comparable against `huihui-claude27-q4` instead of sitting as speed-only reference data.
[Huihui vs Huihui-Claude] outcome — `huihui-claude27-q4` remains the better overall Qwen 27B pick over `huihui-q4`. Evidence: replay `29/35` vs `28/35`, replay fixtures `2/5` vs `1/5`, sim `8/8` vs `7/8`, and much lower reasoning volume at essentially the same prompt-only speed. Impact: keep Huihui Claude as the lead Qwen 27B candidate and treat base Huihui Q4 as the cleaner-scope fallback rather than the front-runner.
[Model-eval restore caveat] outcome — `benchmarks/run_model_eval.sh` finished the Huihui Q4 full pass but still left the managed live service down despite `MODEL_EVAL_LOAD_RESTORE=true`, so the preset had to be restored manually afterward. Impact: do not trust wrapper restore semantics blindly; verify `./scripts/current-main.sh` after full eval runs until that path is fixed.
[Huihui Claude live default] outcome — a dedicated `main-qwen-3.5-27b-huihui-claude-q4.env` preset was added and the managed live service was switched to `qwen-3.5-27b-huihui-claude-q4` on Vulkan. Impact: the strongest proven Qwen 27B candidate is now the active default model instead of the base Huihui fallback.
[Qwen 27B tuning strategy] outcome — after narrowing the field to `huihui-claude27-q4`, the next tuning work should be narrow and evidence-driven rather than more model elimination. Current host stack: ROCm `6.4.1`, amdgpu driver `6.12.12`, Vulkan RADV `Mesa 25.2.8`, with Vulkan currently faster than ROCm on both 9B and 27B control passes. Impact: first tuning A-Bs should stay on the winning model and focus on stack/perf knobs in this order: Vulkan baseline vs Vulkan `RADV_PERFTEST=nogttspill`; ROCm re-check only if PP-heavy behavior matters; then `-fa on/off` on ROCm only; then `-b`/`-ub` sweeps above `512/256`; then prompt-cache and `-cram` experiments for real app latency (not fairness benches); and only later build-flag or driver-stack changes if the narrow knobs fail to move results.
[AMD stack upgrade] outcome — the host AMD stack is now partially upgraded: `amdgpu-install` moved to `30.30.1`, ROCm to `7.2.1`, amdgpu DKMS to `6.16.13`, and the broken proprietary apt source was disabled so `apt` is clean again. Vulkan/RADV did not move with this step and remains `Mesa 25.2.8`. Impact: ROCm/HIP needed a rebuild; Vulkan did not get a newer Mesa driver from the AMD installer path.
[Post-upgrade llama.cpp rebuild] outcome — both `~/.local/src/llama.cpp/build-hip` and `~/.local/src/llama.cpp/build-vulkan-r9700` were rebuilt successfully against the updated stack, and both `llama-server --list-devices` calls now work again. The HIP rebuild required resetting the stale `build-hip` cache and reconfiguring it explicitly against `/opt/rocm-7.2.1` because the old cache still pointed at the removed `6.4.1` compiler path. Impact: the local HIP and Vulkan runtimes are again trustworthy for post-upgrade comparisons.
[Post-upgrade Qwen backend check] outcome — the first post-upgrade speed probes on `huihui-claude27-q4` still favor Vulkan strongly. Vulkan baseline at `131072` landed at about `31.14 tok/s` exact-`OK` and `30.78 tok/s` direct probe; Vulkan with `RADV_PERFTEST=nogttspill` was effectively flat to slightly worse at about `30.90` and `30.85`; HIP on ROCm `7.2.1` was much slower at about `25.88` and `25.75`. Impact: keep Vulkan as the active backend and stop treating HIP as a likely winner for Qwen TG on this host.
[Post-upgrade Vulkan batch sweep] outcome — the first `-b`/`-ub` sweep on `huihui-claude27-q4` did not produce a meaningful win over the current `512/256` baseline. `768/256`, `1024/256`, and `1024/512` all stayed in the same `30.7-31.1 tok/s` band; `768/384` was worse because the direct probe hit a `length` finish with zero visible content and heavy hidden reasoning. Impact: keep `-b 512 -ub 256` as the default launch shape for now and treat larger batch sizes as neutral-to-worse until a more targeted PP-focused test shows otherwise.
[Prompt-cache reuse check] outcome — long-prompt PP tests on `huihui-claude27-q4` showed the expected cold baseline (`~578 tok/s` at `~38.4k` prompt tokens and `~448 tok/s` at `~76.8k`), but warm repeats stayed at `cache_n=0` in every configuration tested: cache off, cache on with `8192` and `16384` cache RAM, and even a slots-enabled retry. Impact: current PP measurements are still effectively cold-prefill measurements; before treating prompt cache as an optimization win on this stack, inspect the server-side reuse semantics and request shape because the benchmark harness is not hitting cache reuse yet.
[Prompt-cache root cause] outcome — the repeated-prompt cache miss on `huihui-claude27-q4` is not a simple flag issue. Live server logs show the second identical request is recognized as a perfect prefix match (`selected slot by LCP similarity, sim_best = 1.000`), but the server then hits `need to evaluate at least 1 token`, decrements `n_past`, fails `llama_memory_seq_rm(...)`, and clears the slot memory. The local `llama.cpp` code path is in `tools/server/server-context.cpp` around the `n_past` decrement and the `failed to truncate tokens ... clearing the memory` warning, and the code comments around checkpoint fallback explicitly call out SWA / hybrid / recurrent memory as a reason for forced full reprocessing. Impact: for this Qwen 27B model on the current server path, repeated identical single-turn prompts do not currently yield reusable `cache_n` in the straightforward way; slot-affinity alone is not enough because the rollback path itself fails.
[Prompt-cache workaround] outcome — append-only reuse does work on the same `huihui-claude27-q4` server path. A direct verification with a `~38.4k` prompt followed by the same prompt plus a tiny suffix produced `cache_n=38149` on the second request, leaving only `262` prompt tokens to process. Impact: prompt cache is usable for realistic growing conversations on this model; the broken case is the exact-same-full-prompt replay pattern, not append-only agent sessions.
[multillm local cache audit] outcome — `multillm` local mode already preserves one append-only history per conversation target and uses a stable tool list order, but it does not send any explicit llama.cpp slot/session affinity such as `id_slot`. Impact: alternating apps can still benefit from prompt cache within each stable conversation, but reliable multi-app warm-cache reuse will require either explicit conversation-to-slot pinning in the client layer or acceptance of best-effort server similarity routing.
[Default service clean restart] outcome — the host was reset to a clean two-service baseline by stopping both llama.cpp services, verifying no leftover `llama-server` processes remained, then starting the default main preset and the embedding service again. Verified live state: main is `qwen-3.5-27b-huihui-claude-q4` on `Vulkan0`, embedding is `Qwen3-Embedding-0.6B-Q4_K_M-imat.gguf` on CPU only with `--device none --gpu-layers 0`, and both `8091` and `8092` health endpoints return `{"status":"ok"}`. Impact: the default runtime is clean again, and the embedding service was not actually on GPU before the restart.
[Qwen 27B slot-capacity check] outcome — the current default 27B Q4 model cannot provide two `131072` slots when started as `-c 131072 -np 2`; llama.cpp split that into two `65536` slots. But it does fit two full `131072` slots when started as `-c 262144 -np 2`, with Vulkan logs showing `llama_params_fit: successfully fit params to free device memory`, `n_ctx_seq = 131072`, a `4608 MiB` KV buffer, and `/slots` reporting two idle slots at `131072` each. Impact: this R9700 can keep two independent 128k conversations warm for the default model if the main service is retuned to total context `262144` with `-np 2`.
[Default main live np2 shape] outcome — the default `qwen-3.5-27b-huihui-claude-q4` preset was retuned live to `MAIN_CONTEXT=262144` with `-np 2`, and `/slots` now reports two idle slots at `131072` each on the running service. One current observability quirk remains: `/props` still exposes `default_generation_settings.n_ctx = 131072` even though the active env and `/slots` confirm the true runtime shape is `262144 total / 131072 per slot`. Impact: for multi-slot validation, trust `/slots` over `/props` until that reporting mismatch is understood or fixed.
[Default main kv-q8 single-slot restore] outcome — the live default was restored to a single-slot shape for follow-up feel tests: `MAIN_CONTEXT=131072`, `-np 1`, and KV cache set to `q8_0/q8_0`. Verified live state shows one idle `131072` slot in `/slots`, and the running command line now uses `-ctk q8_0 -ctv q8_0`. Impact: the service is back on a simpler baseline while preserving prompt-cache defaults.
[Default main live np3 shape] outcome — the default `qwen-3.5-27b-huihui-claude-q4` preset was successfully retuned again to `MAIN_CONTEXT=393216` with `-np 3` while keeping KV at `q8_0/q8_0`, and `/slots` now reports three idle slots at `131072` each on the live service. Impact: the R9700 can hold three independent 128k warm conversations for the current default model if you decide the higher parallel-slot shape is worth the feel/perf tradeoff.
[vLLM research] outcome — current official vLLM docs make it a plausible future experiment on this host, not an obvious near-term replacement. The good news is that current vLLM supports AMD ROCm `6.3+`, explicitly lists Radeon RX 9000 series support, and exposes automatic prefix caching plus an OpenAI-compatible server path. The main caveats for this repo are that ROCm wheels are more constrained than the generic CUDA path, and vLLM’s GGUF support is still officially marked highly experimental and under-optimized with single-file-only support. Impact: vLLM is worth a scoped experiment if multi-client serving becomes a priority, but the first clean test should prefer a non-GGUF HF model in a fresh Python `3.12` environment rather than assuming the current llama.cpp GGUF workflow will translate directly.

# Lessons
[Benchmark isolation correction] outcome — when the user flags even brief multi-model GPU overlap or warns that reasoning/token caps are contaminating results, treat that as a hard benchmark-invalidating signal and remove those confounders from the harness before trusting any follow-up ranking. Impact: future candidate passes must default to no managed-model restore between suites and no default `thinking_budget_tokens` injection in diagnostics unless the user explicitly requests capped truncation experiments.
[Replay conversion rule] outcome — raw `opencode export` assistant messages are not replay-safe against `llama.cpp` as-is. The replay converter must normalize finish reasons, drop assistant text that accompanies tool calls from history, and add a continuation user prompt whenever transcript-derived history would otherwise end on a tool result or assistant turn. Impact: transcript-derived replay fixtures should be generated through `export_to_fixture.py`, not assembled manually from raw export JSON.
[Qwen cap rule] outcome — the user clarified on 2026-05-20 that Qwen models do not behave well when tokens are capped. Impact: future Qwen evaluations should avoid `thinking_budget_tokens`, `max_tokens`, and similar caps by default; only use them for explicit truncation/pathology experiments and label those results as non-representative of normal Qwen behavior.
[Evaluation rule] outcome — do not use `max_tokens` caps by default for viability comparisons or real agent tests. Impact: capped runs are valid only when the goal is truncation behavior; otherwise they distort model conclusions.
[Scratch benchmark isolation] outcome — stop the managed main service before scratch GPU tests and keep only one GPU-backed chat model loaded at a time. Impact: this avoids contaminated fit, preload, and VRAM results.
[Simulation suite rule] outcome — sandbox comparisons should copy a fixture repo into a temp workspace, restrict tool paths to that workspace, and normalize safe test-command variants like `python3 -m unittest ...` and `pytest ...`. Impact: repeatable agent comparisons can be run safely even when the model improvises slightly different test command spelling.
[Simulation scoring rule] outcome — harder sandbox tasks should score both task success and patch scope, because “passes tests” alone hides cases where a model explores or edits the wrong file before converging. Impact: aggregate comparison summaries should keep `expected_files_only` or an equivalent scope-discipline signal, not just pass/fail and latency.
[Qwen request shape] outcome — Qwen 3.5 family requests on this host should set `chat_template_kwargs.enable_thinking` explicitly, keep `tool_choice:"auto"` unless a tool is already chosen, and preserve tool-call history exactly across turns. Impact: request-shape mistakes are a common source of false model-quality regressions.
[Text-only preset rule] outcome — text-only presets must set `MAIN_MMPROJ=` explicitly unless `serve-main.sh` is hardened to stop default-projector inheritance. Impact: omitting that variable can accidentally reuse a multimodal projector from a previous preset.
[HF filename collision rule] outcome — some GGUF finetune repos publish generic filenames like `Qwen3.5-9B.Q4_K_M.gguf`, which will overwrite each other if downloaded straight into the shared `models/` directory. Impact: when staging multiple same-family finetunes, download to separate temp directories first and then copy into `models/` under explicit local names before benchmarking.
[Run-state verification rule] outcome — before reporting that a benchmark is still running in the background, verify the actual exec session or process is live. Impact: do not infer “still running” from intent, wrapper behavior, or stale expectations when the GPU is idle or the user reports the run stopped.
[ROCm np2 plain-chat feel check] outcome — on `qwen-3.5-27b-huihui-claude-q4` with HIP/ROCm0, `-np 2`, `131072` per slot, and KV `q8_0/q8_0`, a plain non-tool cold turn on slot `0` measured about `246.6` prompt tok/s and `26.1` decode tok/s over `42` prompt tokens / `152` generated tokens, while an append-only follow-up on the same slot measured about `543.1` prompt tok/s and `26.0` decode tok/s over `116` prompt tokens / `160` generated tokens. `timings.cache_n` remained `0` on both requests, so this quick check confirms normal ROCm decode throughput for the model but does not yet prove prompt-cache reuse on this small plain-chat path. Impact: ROCm does not look catastrophically slow in direct use; remaining latency questions are more about cache behavior than raw TG.
[Vulkan vs ROCm plain-chat feel check] outcome — with the same live `qwen-3.5-27b-huihui-claude-q4` model shape (`-np 2`, `262144` total context, `131072` per slot, KV `q8_0/q8_0`), a Vulkan plain-chat two-turn check on slot `0` measured decode at about `30.7 tok/s` on both turns, versus ROCm at about `26.0 tok/s`. The small-prompt prompt-side timing remained unstable: Vulkan reported a pathological cold-turn `prompt_per_second≈4.0` over only `42` prompt tokens, then `≈422.9` on the append-only follow-up, while ROCm reported `≈246.6` then `≈543.1`. `timings.cache_n` stayed `0` on all four requests. Impact: Vulkan still looks better for decode throughput on the current winner, but small plain-chat PP readings on this server path are too noisy/path-dependent to use as a clean backend ranking signal without deeper request-level instrumentation.
[Qwen 27B PP batch sweep after ROCm 7.2.1 update] outcome — a long-prompt PP-only sweep on `qwen-3.5-27b-huihui-claude-q4` with `np 1`, `131072` context, KV `q8_0/q8_0`, and no warmup showed that larger batch helps prompt processing on both backends. Vulkan measured about `571.4 tok/s` at `prompt_n≈38923` and `440.6 tok/s` at `prompt_n≈77835` with `-b 512 -ub 256`, improving to about `600.3 tok/s` and `461.9 tok/s` with `-b 1024 -ub 512`. HIP/ROCm 7.2.1 measured about `572.8 tok/s` and `418.8 tok/s` with `-b 512 -ub 256`, improving to about `614.8 tok/s` and `444.1 tok/s` with `-b 1024 -ub 512`. Impact: the strongest missing PP knob really was batch size; `-b 1024 -ub 512` is worth keeping in mind for PP-heavy experiments, while Vulkan still remains the better default backend overall because earlier TG checks stayed materially ahead there.
[Default Qwen 27B preset PP retune] outcome — the live default `qwen-3.5-27b-huihui-claude-q4` preset now uses `-b 1024 -ub 512` on Vulkan while keeping `-np 2`, total context `262144`, and KV `q8_0/q8_0`. Verified live state shows `LIVE_SERVICE ok` and `/slots` reports two idle `131072` slots. Impact: the managed default now carries the best PP-focused batch shape we found so far without changing the current model or backend choice.
[Qwen 27B q4_0 preset bring-up] outcome — the newly downloaded `Huihui-Qwen3.5-27B-Claude-4.6-Opus-abliterated-i1-Q4_0-mradermacher.gguf` now has a matching Vulkan preset (`qwen-3.5-27b-huihui-claude-q40`) and loads cleanly in the same live shape as the current default candidate: `-np 2`, total context `262144`, KV `q8_0/q8_0`, and `-b 1024 -ub 512`. Verified live state shows `LIVE_SERVICE ok` and two idle `131072` slots. Impact: `q4_0` can now be compared directly against `q4_k_m` on this host without changing the rest of the runtime shape.
[Qwen 27B q4_0 quick live check] outcome — on the live Vulkan service with the same default shape as `q4_k_m` (`-np 2`, total context `262144`, `-b 1024 -ub 512`, KV `q8_0/q8_0`), the new `q4_0` variant produced decode in the low-30s on quick probes (`predicted_per_second≈34.3` on an exact-OK probe and `≈32.3` on a short direct-answer probe) and about `648.6 tok/s` on a `prompt_n≈38923` PP probe. Short-prompt `prompt_per_second` remained noisy/pathological, so only the long-prompt PP number is trustworthy. Impact: `q4_0` looks promising enough to warrant a direct apples-to-apples comparison against the current `q4_k_m` default before dismissing older quants on this stack.
[Qwen 27B q4_0 vs q4_k_m quick speed check] outcome — using the same live Vulkan service shape for both variants (`-np 2`, `262144` total context, `-b 1024 -ub 512`, KV `q8_0/q8_0`), `q4_0` beat `q4_k_m` on the quick speed probes. Exact-OK decode was about `34.3 tok/s` for `q4_0` versus `32.4 tok/s` for `q4_k_m`; the short direct-answer probe was about `32.3 tok/s` versus `30.5 tok/s`; and the long PP probe at `prompt_n≈38923` was about `648.6 tok/s` versus `602.6 tok/s`. Short-prompt prompt-side timings remained noisy/pathological on both models. Impact: on this stack, the older `q4_0` quant looks materially faster than `q4_k_m` for both TG and long PP, so it is worth giving real chat use and possibly fuller evaluation before assuming modern `K` quants are the best practical choice.
[Qwen 27B q4_0 spot check] outcome — a focused live behavior spot check on `qwen-3.5-27b-huihui-claude-q40` was mixed but not disqualifying. Replay slices: `retry_tool_followthrough` failed on turn 2 by choosing `apply_patch` instead of the expected `run_tests`, and `retry_fix_real` mostly held together but drifted on turn 3 by using `glob` instead of the expected `bash`. Sim spot check: `retry_bugfix` and `retry_review_feedback` were fully clean (`pass=true`, `scope_clean=true`, `tool_error_free=true`), while `session_store_exploration` still solved the task but over-edited into `worker/flush_report.py` and `worker/reporting.py` (`scope_clean=false`). Impact: `q4_0` keeps its strong speed case and looks viable, but it does show some replay/followthrough looseness, so it should not replace the current `q4_k_m` winner without a fuller apples-to-apples behavior pass.
[Qwen 27B q4_0 full-eval verdict] outcome — a near-complete `run_model_eval` pass on `qwen-3.5-27b-huihui-claude-q40` confirmed the tradeoff we suspected: replay quality regressed even though coding sims stayed strong. Replay completed and showed clean `retry_triage_real`, but `retry_fix_real`, `queue_fix_real`, `retry_tool_followthrough`, and `soak_real` all had extra tool-choice drift versus the `q4_k_m` winner. Sim completed cleanly on the core and recovery tasks and only failed scope discipline on `session_store_exploration`, which matches the earlier spot check. Barrage was stopped early because it was low-yield and not needed to answer the quality question. Impact: `q4_0` is a real speed candidate, but it is currently a quality regression on the general-agent replay axis compared with `q4_k_m`, so it should not replace the current winner as default without a user-priority decision that speed matters more than replay fidelity.
[Qwen 27B q4_0 cleanup] outcome — after direct speed checks and a fuller quality pass, `q4_0` was removed from the active local set because it was faster but clearly looser than the `q4_k_m` winner on replay/followthrough. The live default was restored to `qwen-3.5-27b-huihui-claude-q4` on Vulkan with `-np 2`, total context `262144`, and `-b 1024 -ub 512`, the temporary `q40` preset was deleted, and the `Q4_0` GGUF was removed from `models/qwen-3.5-27b/`. Impact: the working set is back to the stronger quality-biased default, with the speed experiment archived in notes rather than kept live in the model inventory.
[Huihui Qwopus3.5 27B v3 candidate] outcome — the new `Huihui-Qwopus3.5-27B-v3-abliterated-i1-Q4_K_M` candidate is a clear improvement over the older Qwopus line but still does not beat the current `huihui-claude-q4` winner. Tuning/speed: the safe optimized shape was `131072` context with `-np 1 -b 512 -ub 256`; the `1024/512` profile caused hidden-reasoning burn and zero visible answer on the direct probe. Speed on the safe shape stayed around `31.0 tok/s` exact-OK and `30.4 tok/s` on the short direct-answer probe. Replay: clean on `retry_triage_real`, but `retry_fix_real`, `queue_fix_real`, `retry_tool_followthrough`, and `soak_real` all showed tool-choice drift and followthrough looseness. Sim: strong on the cleaner coding tasks (`retry_bugfix`, `retry_review_feedback`, `queue_bugfix`, `tool_error_recovery`, `command_denial_recovery`, `batch_tail_recovery`, `flush_report_two_file_fix`) but failed `session_store_exploration`. Impact: this looks like a viable coding-side alternate candidate, not a better default or better general-agent model than `huihui-claude-q4`.
[Qwopus v3 retry-focused coding check] outcome — the model-card thesis partly holds on a narrow uncapped retry/recovery coding slice, but it still does not beat the current default. In a direct `sim_compare` head-to-head over `retry_bugfix`, `retry_review_feedback`, `tool_error_recovery`, `command_denial_recovery`, and `batch_tail_recovery`, the default `huihui-claude-q4` finished `5/5` pass, `5/5` scope-clean, `5/5` tool-error-free, while `huihui-qwopus-v3-q4` also finished `5/5` pass and `5/5` scope-clean but only `4/5` tool-error-free because `command_denial_recovery` incurred one tool error. It was also slower on most of the retry tasks except `batch_tail_recovery`. Impact: Qwopus v3 is competent on retry-heavy coding tasks, but the current default still remains the cleaner and generally better coding-agent choice even on the subset most favorable to Qwopus v3.
[Embedding service 4B upgrade] outcome — the embedding service default was upgraded from `Qwen3-Embedding-0.6B-Q4_K_M-imat` to `Qwen3-Embedding-4B-Q4_K_M` by updating the systemd unit and `serve-embedding.sh` fallback, then reloading and restarting `localllm-embedding.service`. Verified live state shows `8092` healthy and the running process uses `Qwen3-Embedding-4B-Q4_K_M.gguf` with `--device none --gpu-layers 0`. Impact: retrieval quality testing can now happen on the new 4B embedding model without changing the main GPU-backed chat service.
[OmniVoice install path on AMD host] outcome — OmniVoice `0.1.3` depends on `torch>=2.4`, `torchaudio>=2.4`, `accelerate`, `gradio`, `numpy`, `pydub`, `soundfile`, `tensorboardx`, `transformers>=5.3.0`, and `webdataset`. On this host there is no existing local `torch` or `torchaudio`, and a plain `pip install omnivoice` resolves the generic Linux/CUDA-flavored `torch` wheel from PyPI. Safe path: create a dedicated venv, install ROCm PyTorch first from the ROCm wheel index, then install OmniVoice and its remaining deps. Impact: do not install OmniVoice into the base Python or let pip choose `torch` implicitly, or the TTS stack will pull the wrong backend for this AMD machine.
[OmniVoice ROCm smoke test] outcome — the dedicated `~/.venvs/omnivoice-rocm` env works on the R9700 with `torch 2.11.0+rocm7.2`, and `omnivoice-infer` successfully downloaded `k2-fsa/OmniVoice`, loaded it on `cuda`/ROCm, and generated audio. The stock CLI failed only at the final save step because `torchaudio.save` now routes through `torchcodec`, and the installed `torchcodec` wheel tried to load NVIDIA `libnvrtc.so.13` on this ROCm stack. A direct Python call using `OmniVoice.from_pretrained(..., device_map='cuda')`, `model.generate(...)`, and `soundfile.write(...)` produced `/tmp/omnivoice-smoke.wav` successfully at `24000` Hz. Impact: OmniVoice itself is usable on this AMD host, but the current reliable path is Python API plus `soundfile` save rather than the stock CLI save path.
[OmniVoice repo-local API wrapper] outcome — a minimal local TTS service now exists in the repo as `scripts/omnivoice_server.py`, launched via `scripts/serve-omnivoice.sh`, with a matching unit file at `systemd/localllm-tts.service`. The server loads OmniVoice once from the dedicated venv, exposes `GET /health`, and serves `POST /tts` requests as WAV bytes using `soundfile` instead of the broken `torchaudio.save` path. A reusable client helper now also exists at `scripts/omnivoice_client.py` with `healthcheck()` and `synthesize_tts()` so bots can call the local service without duplicating HTTP glue. Verification: shell syntax, `py_compile`, `ruff check`, `pytest`, and `systemd-analyze --user verify` all passed; manual and helper-driven smoke tests both returned valid `24000` Hz mono WAVs. Impact: bots can call a stable local HTTP TTS endpoint or import a small helper module instead of shelling out to the stock OmniVoice CLI.
[OmniVoice systemd service] outcome — the repo-local TTS unit is now linked into the user systemd instance, enabled, and running as `localllm-tts.service` on `127.0.0.1:8094`. The first version used `8093`, but that conflicted with the repo’s existing optional router default, so TTS was moved to `8094` and the unit was restarted there. Verified healthy at `http://127.0.0.1:8094/health` after the expected startup model-load phase. Impact: OmniVoice is now available as a persistent local bot-callable service without colliding with the router port assumptions elsewhere in the repo.
[OmniVoice clone-mode rule] outcome — on this ROCm stack, clone mode works only when both `ref_audio` and `ref_text` are supplied. The real failure was not generic cloning but OmniVoice’s automatic transcription fallback: if `ref_audio` is provided without `ref_text`, OmniVoice calls its ASR pipeline, which falls into the broken `torchcodec` path and crashes on missing NVIDIA `libnvrtc.so.13`. Verification: clone with `/home/j/projects/openwendy/davika.wav` plus explicit `ref_text` returned `200` and produced `/tmp/omnivoice-clone-with-reftext.wav`; clone without `ref_text` now returns a fast `400` with `ref_text_required_for_clone` instead of a deep server traceback. Impact: bots and prompts should treat `ref_text` as mandatory whenever `ref_audio` is used for cloning on this host.
[Main-model CPU usage investigation] outcome — after removing unrelated host load, the current Vulkan main service (`qwen-3.5-27b-huihui-qwopus-v3-q4`, PID `1226`) does show real CPU activity during a request, but it looks like normal host-side orchestration rather than CPU fallback for model layers. Controlled raw `/v1/chat/completions` requests still measured normal decode (`~28.6` to `31.1 tok/s`). In the cleaned `pidstat -t -u -p 1226` run, the main thread briefly rose to about `68%` CPU in the active second (`24% usr`, `44% sys`) with an average near `12%` across the sample, while the other worker threads stayed near zero. Impact: the main Vulkan `llama-server` does use one CPU thread materially during requests, but the pattern is consistent with host-side request/driver/runtime work, not with broad CPU-side inference replacing GPU execution.
[Embedding service persistent GPU switch] outcome — the persistent user unit `localllm-embedding.service` now runs the `Qwen3-Embedding-4B-Q4_K_M` embedding model on `Vulkan0` instead of CPU by setting `EMBED_DEVICE=Vulkan0`, `EMBED_GPU_LAYERS=auto`, and `EMBED_FIT=true` in `systemd/localllm-embedding.service`. Verification: after `daemon-reload` and restart, `8092` returned healthy and the live process command line showed `--device Vulkan0 --gpu-layers auto --fit on`. Impact: the embedding model now stays loaded on GPU under the persistent systemd path instead of only fitting under temporary scratch launches.
[OmniVoice persistent CPU switch] outcome — the persistent user unit `localllm-tts.service` now runs OmniVoice on CPU by setting `TTS_DEVICE=cpu` in `systemd/localllm-tts.service`. Verification: after `daemon-reload` and restart, `8094` returned healthy with `{"device":"cpu"}`, and the systemd status log showed `Loading OmniVoice model k2-fsa/OmniVoice on cpu`. Impact: OmniVoice now stays loaded under the persistent systemd path without consuming GPU VRAM unless the unit is changed back later.
[Qwopus server default sampling] outcome — the persistent Qwopus launch now bakes in the base Qwen 3.5 stricter coding/thinking defaults at the server level by adding `--temp 0.6 --top-k 20 --top-p 0.95 --min-p 0.0 --presence-penalty 0.0` to both `config/localllm-main.env` and `config/presets/main-qwen-3.5-27b-huihui-qwopus-v3-q4.env`. Impact: clients that do not override sampling now inherit the Qwen-style coding defaults directly from the live `llama-server` instead of the old generic llama.cpp defaults.
[R9700 idle fan anomaly] outcome — the repeatable bad state is not a hidden compute load: while the fan was stuck high after a few Qwopus calls, telemetry still showed `0%` GPU use, `36C` edge/junction, `96MHz` mclk, `0MHz` sclk, and only `13W` package power, but the fan controller held `fan1_target≈2422 RPM` / `pwm1≈119` / `47%`. Performance level remained `auto`, and the main/embedding/TTS processes were essentially idle. Impact: this looks like a fan-target/SMU hysteresis or driver quirk after Vulkan compute activity, not an active-workload or temperature problem; the next isolation step is to see whether stopping the GPU-resident services immediately drops the fan target, which would separate a residency-triggered quirk from a general driver fan-control bug.
[Docs sync] outcome — `/home/j/projects/docs/localllm` was rewritten around the current stack instead of the old 9B/CUDA phase. The docs now describe the actual 27B Qwen preset inventory, Vulkan default runtime, 4B embedding model on GPU, OmniVoice TTS on CPU, `8094` TTS port, reserved-but-unused router port `8093`, and the current benchmark flow and launch profile. Impact: future lookups in the external docs folder should match the live repo and service state instead of sending users to stale 9B/3080 instructions.
[AMD power-limit sweep feasibility] outcome — the current R9700 exposes a writable power-cap range of `210W` to `300W`, with the default at `300W`, but changing it still requires root on this host. A direct `rocm-smi --setpoweroverdrive ...` attempt from this session fell into a sudo password prompt, so automated sweep testing from the agent is still blocked unless the user runs the privileged command or configures passwordless access for that exact operation. Impact: the same sweep strategy is valid on AMD, but it cannot be driven end-to-end from this unprivileged session today.
[Stale NVIDIA power hooks] outcome — the old repo-side NVIDIA-only unit `systemd/localllm-gpu-power-limit.service` was obsolete on this AMD host and has been removed from the repo. The unrelated user timer `nvidia-gpu-metrics.timer` was also found enabled and failing every 15 seconds, and it has been disabled. Impact: startup and maintenance state now better matches the current AMD-only machine, and one noisy stale NVIDIA timer is no longer firing in the background.
[Qwen probe rule] outcome — do not use `max_tokens`, small token budgets, or thinking-budget caps for ad hoc Qwen/Qwopus comparisons unless the explicit goal is truncation behavior. The model does not respond cleanly to those constrained probe shapes and they distort speed/quality reads. Impact: future Qwen/Qwopus speed checks should use uncapped or naturally completing prompts, and any benchmark helper that unloads the live main service must restore it before the task is considered complete.
[AMD persistent power cap] outcome — the repo now includes `systemd/localllm-amd-power-cap.service`, a root-level oneshot unit that writes `GPU_POWER_CAP_UW` into `/sys/class/drm/card0/device/hwmon/hwmon1/power1_cap`, defaulting to `210000000` if `/etc/default/localllm-amd-power-cap` is absent. Impact: the current `210W` R9700 cap can now be made persistent in a reproducible way without reusing the deleted NVIDIA-only power-limit unit.
[Qwopus Q6 preset] outcome — the Qwopus v3 Q6 GGUF was still staged locally but had no preset file, so `main-qwen-3.5-27b-huihui-qwopus-v3-q6.env` was added using the same runtime shape, API key, and mmproj as the live Q4 preset. Impact: the Q6 variant can now be loaded through the normal preset workflow instead of manual edits.
[Qwopus Q6 live switch] outcome — the managed main service now runs `qwen-3.5-27b-huihui-qwopus-v3-q6` on Vulkan with the normal `np 2`, `262144` total-context, `-b 1024 -ub 512`, and API-key-protected launch profile. Verification: `localllm-main.service` is healthy on `8091`, `config/localllm-main.env` points at the Q6 GGUF, and the live `llama-server` process alias is `qwen-3.5-27b-huihui-qwopus-v3-q6`. Impact: app and public Funnel traffic now hit the Q6 Qwopus variant until the main preset is switched again.
[Qwopus Q6 context trim] outcome — the live Q6 shape was reduced from `262144` total context to `131072` total context after the Q6 fit looked marginal. Verification: `config/localllm-main.env` and the Q6 preset both now set `MAIN_CONTEXT=131072`, and the restarted live `llama-server` process is running `-c 131072`. Impact: the Q6 variant now runs with lower VRAM pressure, though `np 2` means the effective per-slot context is `65536`.
[Embedding service CPU revert] outcome — the persistent embedding service was moved back to strict CPU mode by restoring `EMBED_DEVICE=none`, `EMBED_GPU_LAYERS=0`, and `EMBED_FIT=false` in `systemd/localllm-embedding.service`. Verification: the restarted live process on `8092` now runs `--device none --gpu-layers 0`, and `/health` returns `ok`. Impact: embedding no longer reserves GPU memory after reboot or service restart.
[Qwopus Q6 context restore] outcome — after the temporary trim, the live Q6 main service was restored to `262144` total context and the Q6 preset was updated back to the same value. Verification: `config/localllm-main.env`, the Q6 preset, and the restarted live `llama-server` process all now show `-c 262144`, with `/health` returning `ok`. Impact: Qwopus Q6 is back to the larger-context shape while embedding has been moved off the GPU.
[Qwopus v3 Q6 narrowed eval] outcome — the current `qwen-3.5-27b-huihui-qwopus-v3-q6` was run only on the same narrowed replay and sim subset previously used for Qwopus v3 Q4. Replay landed at `27/35` matched turns with `2/5` passed fixtures: `retry_triage_real` clean, `queue_fix_real` clean, `retry_fix_real` at `3/5`, `retry_tool_followthrough` at `1/2`, and `soak_real` at `14/19`. Sim landed at `7/8` pass, `7/8` scope-clean, and `8/8` tool-error-free: every scenario passed except `session_store_exploration`, while `flush_report_two_file_fix` stayed scope-clean. Impact: on this subset, Q6 looks a bit worse than the earlier Qwopus v3 Q4 replay read but a bit cleaner on sim scope/tool discipline, which mirrors the older Huihui-Claude Q4-vs-Q6 tradeoff.
[Preset restore auth quirk] outcome — `benchmarks/run_model_eval.sh` successfully restored the managed Q6 main service after the narrowed Q6 subset run, but the follow-up shell pipeline still emitted `curl: (22) ... 401` because the immediate post-restore check hit the API-key-protected live `8091` endpoint without auth. Verification: the restored `localllm-main.service` is healthy and back on the Q6 preset after manual verification with the bearer token. Impact: benchmark wrappers that re-load the managed main service after public/API-key hardening need authenticated health checks if they want quiet logs.
[Qwopus v3 Q6 opencode/coding/barrage] outcome — the missing Q6 OpenCode/prompt-agent harnesses were run at the current live `262144`, `np 2`, Vulkan profile. `opencode_compare` looked serviceable but not especially decisive: `repo_triage` and both `revise_after_feedback` turns answered directly without tool calls, while `tool_followthrough` used the expected `read_file` on turn 1 but drifted to `apply_patch` on turn 2. `coding_compare` stayed very steady at about `22.7-22.8 tok/s` across the uncapped prompts, but hidden reasoning remained heavy (`~5k` to `~10k` chars per prompt). `agentic_barrage` showed the same general shape: correct tool use on `codex_workflow` turn 1 and direct stop on turn 2, reasonable tool restraint, but still large reasoning blocks on planning/evidence prompts (`~4.7k` to `~6.6k` chars, plus `2.1k` on `codex_workflow` turn 2). Impact: Q6 is credible for PI/OpenCode/Cursor-style work, but its missing-harness picture is “competent and disciplined, with heavy reasoning and no obvious knockout win,” not a clean argument that it has surpassed the earlier Q4 candidate.
[GPU fan diagnosis rule] outcome — the main model being loaded is not, by itself, sufficient to explain the high-fan/high-memory-temp state. User correction: after a clean reboot, the same loaded-model stack can sit at low idle fan and low reported memory temperature. Impact: future fan investigations must treat the trigger as a state transition or runtime condition after use, not simply “model loaded in VRAM”.
[Gemma 4 preset drift] outcome — the tracked `gemma-4-31b-iq4nl` preset is now stale on disk: `config/presets/main-gemma-4-31b-iq4nl.env` still points at `models/gemma-4-31b/gemma-4-31B-it-IQ4_NL-unsloth.gguf`, but that file is no longer present. The only immediately testable Gemma 4 file left in `models/` is `models/gemma-4/gemma-4-31B-it-UD-Q4_K_XL-unsloth.gguf` with its matching BF16 mmproj. Impact: Gemma speed/probe work now reflects the remaining UD-Q4_K_XL build unless the IQ4_NL winner is re-downloaded.
[Gemma 4 UD-Q4_K_XL quick speed read] outcome — the remaining on-disk Gemma 4 model (`gemma-4-31B-it-UD-Q4_K_XL-unsloth.gguf`) was run on a temporary Vulkan server at `131072`, `np 1`, `-b 1024 -ub 512` for a quick commit-or-skip speed read. Results: a long natural `ACK` probe landed at about `489.96 tok/s` prompt processing and `24.16 tok/s` decode, while the short exact-`OK` sanity probe landed at about `26.57 tok/s` decode. Impact: the available Gemma 4 build is in the mid-20 tok/s decode band on this host, so it is plausibly testable but not obviously faster than the current Qwopus Q6 path; if you want the historically stronger Gemma quant, the missing IQ4_NL file needs to be restored first.
[Qwopus v3 Q8_0 quick speed read] outcome — the newly downloaded `Huihui-Qwopus3.5-27B-v3-abliterated-Q8_0` was run on a temporary Vulkan server at `131072`, `np 1`, `-b 1024 -ub 512` for a quick speed-only check. Results: a long natural `ACK` probe landed at about `682.06 tok/s` prompt processing but only `14.64 tok/s` decode, and the short exact-`OK` sanity probe landed at about `15.20 tok/s` decode. Impact: Q8_0 looks dramatically slower than the live Q6 path on decode, so it does not look promising as a practical default unless a later quality read shows an unusually large behavior gain.
[Gemma 4 MoE managed preset] outcome — `main-gemma-4-26b-a4b-moe.env` was added for the on-disk `Huihui-gemma-4-26B-A4B-it-abliterated-MXFP4_MOE.gguf` plus matching mmproj, using the managed main-service shape `262144`, `np 2`, Vulkan, `-b 1024 -ub 512`, cache enabled, and the existing main API key. Impact: the Gemma 4 MoE candidate can now be loaded and kept running through the normal main-service path instead of scratch-only launches.
[Gemma 4 MoE quick speed read] outcome — the managed main service was switched from Qwopus Q6 to `gemma-4-26b-a4b-moe` and left loaded for user testing. Sequential uncapped checks on the live service: cold long `ACK` probe at `prompt_n=39124`, `cache_n=0`, `1586.02 tok/s` prompt processing and `69.73 tok/s` decode; short exact-`OK` sanity probe at `81.02 tok/s` decode. Impact: this Gemma 4 MoE build is dramatically faster than the current Qwopus Q6 path on this host, so it is worth real app-side testing before deciding whether to pursue fuller evaluation or alternative finetunes.
[Qwopus Q6 default shape restore] outcome — the managed main service was switched back from Gemma 4 MoE to `qwen-3.5-27b-huihui-qwopus-v3-q6`, and the Q6 preset was restored to the smaller `131072` / `np 1` shape. Verification: the live `localllm-main.service` process is back on the Q6 GGUF at `-c 131072 -np 1`, and `config/localllm-main.env` now matches that shape. Impact: the smaller Q6 configuration is again the active persistent main-service default.
[Gemma 4 E2B fit check] outcome — the newly downloaded `Huihui-gemma-4-E2B-it-abliterated-v2-i1-Q4_K_M-mradermacher.gguf` was load-tested as a separate scratch Vulkan server at `32768` context and `np 1` while the restored Qwopus Q6 main service remained loaded on `8091`. The model fit cleanly and came up healthy on `8095` using about `3254 MiB` Vulkan model buffer, `108 MiB` total Vulkan KV, and `515 MiB` Vulkan compute buffer, then was shut back down after verification. There is no matching `mmproj` file on disk for this checkpoint, so the successful fit result is for text-only use. Impact: this Gemma 4 E2B Q4 model can coexist with the smaller Qwopus Q6 main service if you want to explore it as a second GPU-resident text model.
[Private/shared model split] outcome — the stack is now split into a private main Qwopus service and a separate shared Gemma service. `localllm-main.service` is back on `qwen-3.5-27b-huihui-qwopus-v3-q6` at `131072` / `np 1` on `8091` with no API key, while the new `localllm-share.service` runs `gemma-4-e2b-q4-share` at `32768` / `np 1` on `8095` with `--api-key stupidfatcutebutterbear`. Verification: unauthenticated chat on `8091` returns `200`, unauthenticated chat on `8095` returns `401`, and authenticated chat on `8095` returns `200`. Impact: Qwopus is again private/local-use by default, and Gemma is the isolated shareable model.
[Tailscale share target move] outcome — Tailscale Serve/Funnel was moved off the private main model and now proxies `https://jhome.taila1332f.ts.net/` to `http://127.0.0.1:8095` instead of `8091`. Impact: the public/shared endpoint now reaches only the Gemma share service, while the private Qwopus main service is no longer directly exposed through Tailscale.
[Qwopus Q8 main preset] outcome — a dedicated `main-qwen-3.5-27b-huihui-qwopus-v3-q8.env` preset was added for the on-disk Q8 GGUF, and the managed main service was switched to it for live user testing. Verification: `localllm-main.service` is healthy on `8091` with alias `qwen-3.5-27b-huihui-qwopus-v3-q8`, `131072` context, and `np 1`. Impact: the private default main service can now be moved between the smaller Q6 and Q8 Qwopus variants through the normal preset path.
[Gemma E2B CPU share mode] outcome — the shared Gemma E2B service was moved from GPU to strict CPU mode by setting `MAIN_DEVICE=none`, `MAIN_GPU_LAYERS=0`, and `MAIN_FIT=false` in `config/localllm-share.env`, then restarting `localllm-share.service`. Verification: the live process on `8095` now runs `--device none --gpu-layers 0`, and the authenticated chat endpoint remains healthy. Impact: the shared Gemma service no longer consumes GPU VRAM, leaving the GPU to the private Qwopus main model.
[Gemma E2B CPU quick speed read] outcome — the CPU-only shared Gemma E2B service was measured with sequential uncapped prompts. A medium cold `ACK` probe landed at `prompt_n=10373`, `cache_n=0`, `81.02 tok/s` prompt processing, and `9.01 tok/s` decode; the short exact-`OK` sanity probe landed at `13.33 tok/s` decode. Impact: Gemma E2B is workable on CPU for sharing/light use, but it is far slower there than the earlier GPU fit, so friend-facing expectations should be set accordingly.
[Shared Gemma GPU restore] outcome — after the CPU check proved too slow, the stack was reverted to the earlier private/share split: `localllm-main.service` restored to private Qwopus Q6 on GPU at `131072` / `np 1`, and `localllm-share.service` restored to GPU-backed Gemma E2B on `8095` at `32768` / `np 1` with the existing API key. Verification: unauthenticated chat on `8091` returns `200`, unauthenticated chat on `8095` returns `401`, and Tailscale Serve/Funnel still proxies `https://jhome.taila1332f.ts.net/` to `127.0.0.1:8095`. Impact: the live stack is back on the faster and more useful arrangement for personal Qwopus use plus friend-facing Gemma sharing.
[chat-tui shared auth] outcome — `scripts/chat_tui.py` now accepts an `--api-key` argument and sends the bearer token on both `/props` discovery and `/v1/chat/completions`, while `scripts/chat-tui.sh` now passes `LOCALLLM_CHAT_API_KEY` and defaults it to `stupidfatcutebutterbear`. Verification: discovery against `8095` returns `gemma-4-e2b-q4-share`, and an authenticated chat completion against the shared endpoint returns `200`. Impact: the local TUI can now target the authenticated shared Gemma service without manual header hacks.
[Qwopus Q8 tuning sweep] outcome — with both the private Q6 service and the shared Gemma service stopped, Qwopus Q8 was swept on the `131072` / `np 1` shape across four `-b / -ub` pairs using fixed uncapped probes. Results: `512/256` scored `626.35 tok/s` PP, `14.87 tok/s` medium-prompt TG, `15.14 tok/s` exact-OK TG; `768/384` scored `658.80`, `14.77`, `15.07`; `1024/256` scored `618.39`, `14.82`, `15.16`; `1024/512` scored `688.45`, `14.69`, `15.05`. Impact: decode was effectively flat across the tested range, so `1024/512` is the best Q8 tuning here because it clearly wins on prompt processing without a meaningful TG penalty.
[Qwopus Q8 temporary live load] outcome — after the tuning sweep, the managed main service was reloaded on the Q8 preset and left running for user testing, while the shared Gemma service remained stopped. Verification: `localllm-main.service` is healthy on `8091` with alias `qwen-3.5-27b-huihui-qwopus-v3-q8` and live args `-np 1 -b 1024 -ub 512`; `localllm-share.service` is inactive. Impact: the GPU is currently dedicated to the tuned Q8 main model so the user can judge real interactive feel without a second model loaded.
[Qwopus Q8 96k sweep] outcome — Q8 was re-swept on the smaller `98304` / `np 1` shape with higher batch candidates. Results: `1024/512` scored `710.04 tok/s` PP, `19.25 tok/s` medium-prompt TG, `19.91 tok/s` exact-OK TG; `1280/512` scored `696.92`, `19.19`, `19.57`; `1536/512` scored `700.25`, `19.16`, `19.77`; `1536/768` scored `692.05`, `19.19`, `19.53`; `2048/512` scored `704.47`, `19.25`, `19.84`; `2048/1024` scored `702.88`, `19.16`, `19.55`. Impact: reducing context to `96k` materially improved both PP and TG for Q8, but the best tuning is still `1024/512`; pushing batch higher did not beat it.
[Qwopus Q8 96k live shape] outcome — the managed main service was reloaded on the Q8 preset after updating `MAIN_CONTEXT` to `98304`, and it remains the only active GPU chat model. Verification: `config/presets/main-qwen-3.5-27b-huihui-qwopus-v3-q8.env` and `config/localllm-main.env` now both set `MAIN_CONTEXT=98304`, and the live service on `8091` is running `-c 98304 -np 1 -b 1024 -ub 512`. Impact: the user can now test the tuned Q8 model on the smaller `96k` context shape without Gemma co-loaded.
[Qwopus Q8 full 96k rerun] outcome — the complete `transcript_replay + sim_compare + opencode_compare + coding_compare + agentic_barrage` pass finished cleanly on the tuned `98304`, `np 1`, Vulkan `-b 1024 -ub 512` profile after an earlier interrupted/aborted attempt. Replay landed at `26/35` matched turns with `2/5` passed fixtures: `retry_triage_real` and `queue_fix_real` clean, `retry_fix_real` at `2/5`, `retry_tool_followthrough` at `1/2`, and `soak_real` at `14/19` with no timeout on the rerun. Sim landed at `7/8` pass, `7/8` scope-clean, and `8/8` tool-error-free, failing only `session_store_exploration` just like Q6. OpenCode matched the Q6 shape: direct answers on `repo_triage` and both `revise_after_feedback` turns, then `read_file` followed by drift to `apply_patch` on `tool_followthrough`. Coding compare held a steady `18.68–18.82 tok/s` across the uncapped prompts with heavy hidden reasoning (`~4.2k–7.1k` chars). Barrage stayed coherent but still showed large hidden reasoning blocks and a bad `tool_followthrough` finish where turn 2 answered `2 + 2 = 4.` instead of following the workflow. Impact: Q8 at 96k is viable and sim-clean, but it is the weakest of the Qwopus quants on replay/followthrough and materially slower than Q6 in prompt-agent harnesses, so it is hard to justify as the default unless a separate quality gain shows up in real use.
[llama.cpp upstream refresh] outcome — the local `~/.local/src/llama.cpp` checkout was fast-forwarded from `c5ce4bc22` to `aa00911d1`, and both `build-vulkan-r9700` and `build-hip` were rebuilt successfully to `llama-server version 368 (aa00911d1)`. Impact: the host is now on current upstream code for both Vulkan and HIP backends, including the recent Gemma/server/Vulkan changes in the 76-commit gap.
[AMD Vulkan stack refresh] outcome — the AMD Mesa userspace packages were upgraded from the older 25.x line to the AMD repo’s `26.0.0.70201` set, and the missing `mesa-amdgpu-vulkan-drivers` ICD package was installed so RADV no longer falls back to Ubuntu’s `mesa-vulkan-drivers 25.2.8`. Verification: after restarting the active user services, `vulkaninfo --summary` now reports `driverInfo = Mesa 26.0.0-devel` and `driverVersion = 25.99.99` on the R9700, while both `localllm-main.service` and `localllm-share.service` remain healthy. Impact: future Vulkan-side comparisons now test against the newer AMD-packaged RADV stack rather than the stale Ubuntu ICD.
[Post-update cleanup] outcome — the redundant Ubuntu Vulkan ICD path and stale AMD 25.x leftovers were removed by uninstalling `mesa-vulkan-drivers`, `libllvm19.1-amdgpu`, and the orphaned `libxatracker2-amdgpu`, leaving only the AMD-packaged `mesa-amdgpu-vulkan-drivers` / `libllvm20.1-amdgpu` Vulkan stack. The unused local `llama.cpp` build trees `build-cuda`, `build-cuda-tuned`, `build-vulkan`, and `build-hip-test` were also deleted, leaving only the active `build-vulkan-r9700` and `build-hip` trees. Verification: `vulkaninfo --summary` still reports `Mesa 26.0.0-devel`, and both `localllm-main.service` and `localllm-share.service` remain healthy after the cleanup. Impact: the host no longer carries the stale Ubuntu RADV path or the unused local build artifacts, which reduces ambiguity and disk waste without changing the active runtime paths.
[Gemma share traffic check] outcome — `localllm-share.service` is healthy on `8095`, but its built-in `/metrics` counters still show `prompt_tokens_total=0`, `tokens_predicted_total=0`, and `n_decode_total=0` since startup, while `journalctl` shows only startup logs and no request-processing entries. Impact: the shared Gemma service is loaded and reachable locally, but no real inference requests have reached the model since this boot of the service; any friend-side failure is happening before the request reaches `llama-server` or is being rejected before decode starts.
[Qwen vs Nex equalized comparison] outcome — on the equalized multimodal server shape `212992 / np1 / q8_0-q8_0 / b=2048 / ub=1024 / no-spec`, `Nex N2 mini Q5` beat `Qwen 3.6 35B A3B Unsloth Q6` on both speed and the compact replay slice while tying the compact coding-agent sim slice. Measured speed: `llama-bench` `2872.37 pp / 107.98 tg` for Nex vs `2171.92 pp / 105.60 tg` for Qwen; real 2-paragraph server probe `256.65 pp / 94.44 tg` for Nex vs `169.47 pp / 70.46 tg` for Qwen. Capability slice: replay `2/4` fixtures and `29/33` matched turns for Nex vs `1/4` and `23/33` for Qwen; sim `4/4` pass, `3/4` scope-clean, `4/4` tool-error-free for both. Impact: this is the cleanest fair head-to-head on this box so far, and it favors Nex Q5 if the user wants a shareable Qwen-family-style multimodal alternative to the current Qwen default. The comparison chart was saved to `qwen36_q6_vs_nexn2_q5.png`.
[Qwen vs Nex expanded selection run] outcome — the broader equalized comparison still favored `Nex N2 mini Q5`, but with an important coding-stability caveat. On guarded full replay, both landed at `2/5` passed fixtures, but Nex matched more turns (`29/35` vs Qwen `26/35`) and cleanly passed `retry_tool_followthrough`, while both completed `soak_real` within the external `180s` wall-clock guard. On full `sim_compare`, both scored `7/8` pass, but Nex was cleaner on scope (`7/8` vs `5/8`) with the same `6/8` tool-error-free count. On OpenCode, Nex won cleanly (`3/3` pass-like) while Qwen drifted (`1/3`) due a `revise_after_feedback` timeout and a `read_file -> apply_patch` tool-followthrough failure. On barrage, Nex also edged Qwen (`5/6` pass-like vs `4/6`), though both still collapsed `tool_followthrough` turn 2 into the trivial `2 + 2 = 4` answer. The one major negative for Nex was uncapped coding stability: it ran faster on the prompts it finished (`72.20 tg` average over 2 completed prompts vs Qwen `61.15 tg` over 4/4), but it did not finish the full 4-prompt coding pass cleanly, while Qwen completed all 4. Impact: on this box and equalized multimodal shape, Nex Q5 looks stronger overall for agentic quality and speed, but Qwen Q6 remains the safer default if predictable long coding output matters more than winning most harness slices. The refreshed share image overwrote `qwen36_q6_vs_nexn2_q5.png`.
[Qwen 3.6 27B MTP 76k-token OOM] outcome — the 2026-06-13 OpenWendy failure was a kernel OOM kill of `localllm-main.service`/`llama-server`, not OpenWendy exhausting context and not a VRAM-fit failure. At `13:45:48` llama.cpp tried to update the prompt cache for a `76296` token slot and logged a `2983.834 MiB` state save (`299.487 MiB` draft); at `13:45:56` the kernel killed `llama-server` with about `28.1 GiB` anonymous RSS and nearly exhausted swap. The active server had accumulated `32` context checkpoints around `300-448 MiB` each, plus the `8192 MiB` RAM prompt-cache budget. Impact: fitting weights/KV in VRAM is insufficient for long agent sessions when llama.cpp RAM prompt-cache and many SWA/context checkpoints are enabled.
[Qwen 3.6 27B MTP initial OOM mitigation superseded] outcome — the first post-OOM mitigation set `MAIN_CACHE_PROMPT=false`, `MAIN_CACHE_RAM=0`, and reduced checkpoints, which removed the RAM prompt-cache state-save path but also disabled per-request slot prompt reuse in llama.cpp and forced `n_past=0` on follow-up requests. Impact: do not treat `--no-cache-prompt` as a safe speed-neutral way to disable only the CPU RAM prompt-cache store; it prevents long-prefix reuse and makes long agent turns reread the prompt.
[Qwen 3.6 27B MTP prompt-reuse split] outcome — the active `qwen-3.6-27b-mtp-huihui-q6-fast-128k` env and matching preset now use `MAIN_CACHE_PROMPT=true`, `MAIN_CACHE_RAM=0`, and `--ctx-checkpoints 12`. Verification: live command line shows `--cache-prompt --cache-ram 0 --ctx-checkpoints 12`; startup still logs `prompt cache is disabled - use --cache-ram N to enable it`, meaning the RAM prompt-cache store is off; `--swa-full` was tested and rejected because llama.cpp logged `swa_full is not supported by this model, it will be disabled`. Direct `/completion` probe improved from first-call `21024` prompt tokens to exact-repeat `4` and suffix `13`; OpenAI-compatible chat improved from first-call `10529` prompt tokens to exact-repeat `4` with `10525` cached and appended-turn `1038` with `9501` cached. The test slot was cleared back to a 2-token prompt. Impact: long same-slot agent turns should reuse most of the existing prefix again without reintroducing the multi-GB RAM prompt-cache allocation that caused the OOM.
[Qwen 3.6 preset cache policy] outcome — all retained Qwen 3.6 main presets now standardize on `MAIN_CACHE_PROMPT=true` and `MAIN_CACHE_RAM=0`. Verification: `rg` shows every retained preset plus the active env has prompt reuse enabled and RAM cache disabled; `./scripts/list-presets.sh` reports all retained presets `status=ok`; and `./scripts/status.sh` reports all live stack services healthy. Impact: future 27B/35B preset reloads should keep slot-prefix reuse while avoiding the CPU RAM prompt-cache state-save path that caused the long-session OOM.
[Qwen 3.6 27B plus embedding VRAM fit] outcome — with the active `qwen-3.6-27b-mtp-huihui-q6-fast-128k` main service loaded, current VRAM was `29.57GB / 34.21GB`. A scratch GPU launch of `Qwen3-Embedding-4B-Q4_K_M.gguf` on a separate port with the production embedding shape (`2048` context, `ub=128`, `b=128`, Vulkan auto offload) became healthy and raised VRAM to `32.46GB / 34.21GB`, then returned to baseline after shutdown. Impact: the embedding model fits on GPU beside the current 27B main model, costing about `2.89GB` VRAM and leaving about `1.75GB` decimal headroom; do not assume both embedding and reranker can be moved to GPU together without a separate fit test.
[Embedding/reranker residency flip] outcome — the temporary embedding-GPU move was superseded by the user-requested flip: `localllm-embedding.service` is back on CPU mode (`EMBED_DEVICE=none`, `EMBED_GPU_LAYERS=0`, `EMBED_FIT=false`), while `localllm-reranker.service` now uses `RERANK_DEVICE=Vulkan0`, `RERANK_GPU_LAYERS=auto`, and `RERANK_FIT=true`. Verification: live commands show embedding on `--device none --gpu-layers 0` and reranker on `--device Vulkan0 --gpu-layers auto --fit on`; stack health passes; VRAM is `32.71GB / 34.21GB`; embedding RSS is about `4.58GB`; reranker RSS is about `517MB`; and `/v1/rerank` returned a valid response. Impact: the heavier query-time reranker path is now GPU-resident, while embedding is back in system RAM.
[X/Twitter thread consolidation 2026-06-14] outcome — `social/x` was reduced from nine overlapping thread packages to four active packages after deleting the already-posted `1-fits-in-vram` thread. Active packages are now `1-qwen-shapes.md` with three images, `2-mtp-builds.md` with one image, `3-runtime-checks.md` with two images, and `4-stack-residency.md` with two images. The old dense/sparse/context/context-cliff material is collapsed into thread 1; llama.cpp upgrade plus AMD backend material is collapsed into thread 3; and VRAM budget plus stack map material is collapsed into thread 4 with the current embedding-CPU/reranker-GPU placement. Verification: renderer now emits only the active images, `social/x` has four Markdown files and eight `1600x900` PNGs, all Markdown image references resolve, contact-sheet review passed, and `ruff check` passes for `scripts/social/render_terminal_x_series.py`. Impact: the folder is cleaner for posting and each remaining thread can use one image per relevant tweet without repeating the same local-Qwen lesson across separate posts.
[X/Twitter 1:1 thread/image pass 2026-06-14] outcome — the active `social/x` Markdown drafts were rewritten so every package has a one-to-one tweet/image structure and factual, non-baity wording. Current counts: `1-qwen-shapes.md` has 3 tweets for 3 images; `2-mtp-builds.md` has 1 tweet for 1 image; `3-runtime-checks.md` has 2 tweets for 2 images; `4-stack-residency.md` has 2 tweets for 2 images. Verification: all image references resolve, every inline `[image]` marker matches a tweet, and each tweet's text directly describes the attached chart. Impact: the posting drafts now align visuals and copy cleanly instead of using extra summary tweets without images.
[X/Twitter chart readability pass 2026-06-14] outcome — all active `social/x` chart images were regenerated with larger chart-body labels and numeric values, matching the readability direction used for `3-runtime-backend.png`. The renderer now uses larger shared label/value fonts and adjusted chart geometry for grouped bars, scatter labels, heatmap rows, VRAM bars, and the residency matrix. Verification: `ruff check` passes for `scripts/social/render_terminal_x_series.py`, all eight PNGs regenerate at `1600x900`, and contact-sheet inspection found no obvious clipping or overlap. Impact: the images should be more legible in X/Twitter previews without requiring the tweet text to repeat chart labels.
[R9700 peak chart cleanup] outcome — the dense terminal-grid background was removed from `5a-r9700-peak-split.png` and `5b-r9700-utilization.png`, bar/value margins were tightened, and both images were regenerated without footer text or clipped axis labels. Verification: visual inspection shows clean spacing, the 100% reference line and percentages are readable, `ruff check .` passes, `pytest -q` passes (35 tests), and the full `render_terminal_x_series.py` run rebuilt all 11 PNGs successfully. Impact: the two-tweet R9700 compute/bandwidth thread is ready for posting.
[R9700 chart single-image focus] outcome — the cramped `5a-r9700-peak-split.png` was dropped: the renderer no longer generates it, the file was deleted, and the `social/x/5-r9700-peak.md` draft was rewritten as a single-tweet post around `5b-r9700-utilization.png`. The Vulkan/HIP legend in `5b` was enlarged to the label font and moved to the bottom-left with explicit color squares. Verification: `5b` shows no overlap between the legend, axis labels, or 100% reference callout; `ruff check .` passes; `pytest -q` passes (35 tests); and the full series renderer now produces 10 PNGs excluding the dropped 5a. Impact: the R9700 peak post now has one clean, readable chart instead of two.
[Benchmark standardization plan] outcome — a full survey of tests and benchmark harnesses was completed. Decision: rework the testing barrage to centralize config, add partial-credit and composite scoring, lock environment metadata, store committed summaries, and auto-generate `BENCHMARK_RESULTS.md`. Primary ranking will stay split by family (`transcript_replay` for general-agent, `sim_compare` for coding-agent); diagnostics (`agentic_barrage`, `opencode_compare`, `coding_compare`) will be scored but not blended into primary rank. Impact: future model comparisons should be more reproducible and less brittle.
[Benchmark standardization implementation] outcome — the plan was implemented and committed. `benchmarks/config.json`/`config.py`/`config.sh` now drive all harnesses; `transcript_replay` reports partial-credit metrics (finish-reason match, tool-set Jaccard, tool-count match) alongside exact matches; `sim_compare` reports a composite `agent_score` plus scope details; `agentic_barrage`, `opencode_compare`, and `coding_compare` now emit `summary.json` with scenario rubrics and hidden-test execution respectively; every `run_manifest.json` includes `llama-server` version, `llama.cpp` commit, GPU/driver info, and model artifact fingerprint; `benchmarks/publish_summary.py` copies summaries/manifests into `benchmarks/summaries/` and `benchmarks/generate_results_md.py` regenerates `BENCHMARK_RESULTS.md`. Verification: `ruff check .` passes, `pytest -q` passes (37 tests), and all modified bash scripts pass `bash -n`. Impact: the evaluation pipeline is now centralized, scored, and reproducible.
[Benchmark standardization documentation] outcome — `benchmarks/README.md` was rewritten to document the new shared config, partial-credit/composite scoring, diagnostic scoring, environment metadata, result publishing, and full-stack stop/start behavior; a "Benchmarks" section was added to the top-level `README.md`; and the implementation note in `NOTES.md` was updated. Verification: `ruff check .` passes and `pytest -q` passes (47 tests). Impact: the benchmark rework is now documented for future reference.
