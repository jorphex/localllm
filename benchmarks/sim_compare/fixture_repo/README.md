This is a disposable fixture repo for local model evaluation.

It contains small, realistic Python bugs that coding agents can inspect,
patch, and verify with the included unit tests.

The repo intentionally includes multiple worker modules so some tasks require
searching before editing.
